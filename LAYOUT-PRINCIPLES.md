# 배치 원칙

이 문서는 혼자 섭니다. 특정 컴포넌트 킷도, 프레임워크도, 빌드 도구도 전제하지
않습니다. 배치·그리드·정렬에 대한 규칙만 담고, 색·글꼴·모션은 담지 않습니다.

§1~§6·§8의 **규칙**은 CSS가 없어도 섭니다. 다만 §1의 ⚠️와 §2의 목록은 CSS 그리드 이야기이고, **§7은 통째로 웹 전용입니다.**

## 0. 이 문서를 쓰는 법

1. **먼저 내 경계를 잽니다.** 규칙마다 "이것이 내 상자 안 이야기인가, 바깥 이야기인가"를 먼저 정합니다. 남의 시스템 위에 얹을 때 이 구분이 없으면, 지킬 수 없는 것을 지키려다 호스트 레이아웃을 뒤엎게 됩니다.
2. **가져가는 것은 구조뿐입니다.** 색·글꼴·모서리·그림자는 호스트 것을 씁니다. 호스트의 표면을 내 취향대로 다시 칠하면 테마를 바꾸는 순간, 호스트를 업데이트하는 순간 깨집니다.
3. **경계 밖은 "위반"이 아니라 "적용 불가"로 적습니다.** 적어 두지 않으면 다음 사람이 같은 자리에서 같은 싸움을 합니다.

## 규칙

### 1. 가장자리는 맞춥니다

**규칙** — 한 상자 안에 여러 줄이 쌓이면, 그 줄들의 양 끝은 같은 자리에서 끝납니다.
**내 상자 안** — 폭을 길이로 못 박지 않습니다. 마지막 칸이 상자 안쪽 끝까지 차게 두면 정렬이 저절로 유지됩니다. ⚠️ 줄 전체를 한쪽으로 붙이는 정렬 설정은 이 "끝까지 차게"를 조용히 무력화합니다. 둘을 같이 쓰지 않습니다.
**상자 밖이면** — 내 상자와 **옆 상자**의 끝을 맞추는 것은 대개 내 몫이 아닙니다. 손으로 폭을 맞춰 두면 화면 폭이 바뀌는 순간 어긋납니다.
**세로도 같습니다** — 나란히 선 칸들의 라벨 길이가 서로 달라도, 그 안의 컨트롤은 같은 줄에서 시작하고 끝납니다. 칸 높이가 같은 것으로는 부족합니다: 남는 높이가 컨트롤 **아래**로 가면 칸 높이는 같은데 입력 상자만 어긋납니다. 남는 높이는 **글과 컨트롤 사이**로 보냅니다.
**실패한 자리** — 값 칸의 폭을 200px로 못 박아 둔 카드가 있었습니다. 윗줄 액션 버튼은 카드 안쪽 오른쪽 끝에 붙는데 값 칸은 그보다 훨씬 앞에서 끝나, 카드 전체가 기울어 보였습니다.

### 2. 열 수는 폭이 아니라 개수가 정합니다

**규칙** — 넓은 화면에서 칸이 커지는 이유는 폭이 남아서가 아니라 개수가 고정이라서입니다.
**내 상자 안** — 열 수를 숫자로 박지 말고 "한 칸의 최소 폭"으로 적습니다. 그러면 폭이 변할 때 칸 수가 따라옵니다.
**상자 밖이면** — 내 상자들이 한 줄에 몇 개 서는지는 대개 호스트가 정합니다. 그때 내가 할 수 있는 것은 **내 상자가 어느 폭에서도 무너지지 않게** 만드는 것까지입니다. 호스트의 열 수를 내 쪽에서 되돌리려 하지 않습니다.

- 최소 폭만으로는 칸을 **줄이지 못합니다.** 상한이 "남는 폭을 나눠 가짐"이면 항목이 적을수록 칸이 **넓어집니다.** 줄이려면 상한을 따로 줘야 합니다.
- 상한이 최소보다 작으면 상한이 집니다.
- **상한을 확정 길이로 주면, 칸 수를 정하는 것은 최소 폭이 아니라 그 상한입니다.** 그래서 상한을 **올리면 칸이 줄고 하나하나는 오히려 커집니다** — "뚜껑"이라는 이름과 반대로 움직이는 자리라 값을 정하기 전에 눈으로 재세요.
- **상한을 주면 칸은 언제나 정확히 그 크기가 되고, 남는 폭이 반드시 생깁니다.** 최소와 상한 사이 어딘가로 눕지 않습니다. 그 남는 폭을 줄 어디에 둘지가 짝 축이고, **상한이 없으면 그 축은 아무것도 하지 못합니다** — 나눠 줄 폭이 0이기 때문입니다.
- **비어 보이는 자리가 전부 "남는 폭"은 아닙니다.** 항목이 없어 비어 있는 **칸**과, 칸 바깥에 남은 **폭**은 다릅니다. 정렬 축이 옮길 수 있는 것은 뒤엣것뿐이라, 앞엣것을 정렬로 없애려 하면 아무 일도 일어나지 않습니다.
- **빈 칸을 접는 방식은 항목 수보다 많은 칸을 보여 주지 않습니다.** 항목이 둘이면 아무리 넓은 화면에서도 두 칸입니다. 칸을 더 원하면 항목을 더 넣어야 합니다.
- 큰 최소 폭은 **좁은 화면에서 열을 뺍니다.** 넓은 쪽만 재면 이 손해가 보이지 않습니다.

**실패한 자리** — 화면 2560에서 요약 카드 한 장이 533px이 됐습니다. 원인은 남는 폭이 아니라 열 수가 넷으로 박혀 있던 것이었고, 페이지 폭에 상한을 씌우는 것으로는 고쳐지지 않았습니다.

**같은 종류의 컨트롤 묶음도 개수가 정합니다** — 버튼 두 개짜리 선택 묶음 둘이 나란히 있으면, 그 두 상자의 폭은 **글자 수가 아니라 버튼 개수**로 같아야 합니다. 글자 길이가 폭을 정하게 두면 같은 모양의 줄들이 서로 다른 폭으로 서고, 설정 화면처럼 세로로 훑는 자리에서 그 들쭉날쭉함이 그대로 읽힙니다.

### 3. 최소 폭은 고르는 것이 아니라 역산합니다

**규칙** — 최소 폭은 "n열이 서야 하는 가장 좁은 화면"에서 풀어 나온 값입니다. 고른 값이 아닙니다.
**내 상자 안** — 최소 폭이 필요하면 고르지 말고 풉니다. **n열이 서야 하는 가장 좁은 화면**을 먼저 정하고, 그 화면에서 내 상자의 안쪽 폭을 잰 다음 나눕니다.
**상자 밖이면** — 호스트가 내 상자의 폭을 정하면 "가장 좁은 화면"이 아니라 **"호스트가 나에게 주는 가장 좁은 폭"**에서 풉니다. 그 폭을 모르면 재기 전에는 값을 정할 수 없습니다.

```
n열이 서려면    최소 폭 ≤ (안쪽 폭 − 간격 × (n − 1)) / n
```

**실패한 자리** — 넓은 화면만 보고 최소 폭을 키웠더니 860px에서 열이 하나 사라져, 고치기 전보다 나빠졌습니다.

### 4. 같은 문맥은 같은 높이입니다

**규칙** — 같은 자리에 나란히 서는 컨트롤은 높이와 글자 크기가 같습니다. 아이콘 버튼·탭·빠른 이동 바처럼 종류가 다른 컨트롤은 여기에 매이지 않습니다.
**내 상자 안** — 높이는 값이 아니라 **계층**으로 정합니다. 두세 개의 높이를 정해 두고 문맥마다 하나를 고릅니다. 같은 자리의 저장·취소·삭제가 서로 다른 높이면 그 자체가 결함입니다.
**상자 밖이면** — 호스트가 컨트롤 높이를 정하면 그 값을 **읽어서** 씁니다. 내 상자 안에서만 다른 높이를 쓰면 경계에서 어긋나 보입니다.

### 5. 자리는 값이 없어도 예약합니다

**규칙** — 있을 수도 없을 수도 있는 요소는 비어도 자리를 남깁니다. 화면을 옮겨 다녀도 같은 것이 같은 자리에 있어야 합니다.
**내 상자 안** — 있을 수도 없을 수도 있는 요소에 최소 높이를 줍니다. 값이 있고 없고가 바뀌어도 아래 것들이 움직이지 않아야 합니다.
**상자 밖이면** — 호스트가 내 상자의 높이를 내용에 맞춰 줄이면 이 규칙은 **적용 불가**입니다. 그때는 내 상자 **안에서** 순서가 흔들리지 않게 하는 데까지가 내 몫입니다.

### 6. 손잡이는 세 층이고, 새 축의 기본값은 "지금과 같음"입니다

**규칙** — 전역 기본값, 자주 쓰는 것, 그리고 나머지 전부를 여는 탈출구. 탈출구가 없으면 앞의 둘이 끝없이 늘어납니다.
**내 상자 안** — 남이 쓸 것을 만든다면 세 층을 다 엽니다. 전역 기본값(상속되는 것), 자주 쓰는 것(이름을 준 것), 나머지 전부(임의 스타일을 붙일 통로). **새 축을 열 때 기본값은 반드시 "지금과 같음"이어야 합니다** — 손잡이를 만든 것만으로 모든 소비자의 화면이 움직이면 안 됩니다.
**상자 밖이면** — 정책은 내용을 아는 쪽만 정할 수 있습니다. 무엇이 나란히 설지, 순서가 어떻게 될지를 만드는 쪽이 대신 정하면, 쓰는 쪽은 고쳐 달라고 기다리게 됩니다. **기다림이 생기는 순간 그 자리는 잘못 설계된 것입니다.**
**실패한 자리** — 같은 모양의 요청이 네 번 왔습니다. 최소 폭이 없다 → 최대 폭이 없다 → 남는 폭 배분이 없다 → 임의 스타일을 붙일 통로가 없다. 네 번째에 세어 보니 내보내는 스무 개 중 열 개에 그 통로가 없었습니다.

### 7. 보간되지 않는 것들 (웹 전용)

**규칙** — 애니메이션이 붙는 자리에는 보간되지 않는 속성을 쓰지 않습니다. 한 프레임에 스냅합니다.
**내 상자 안** — 사라지는 것은 `display: none`이 아니라 **`opacity`**로 보냅니다(`display`는 보간되지 않아 한 프레임에 사라집니다). 폭이 있는 것은 `width`/`max-width`를 0으로 보냅니다. 칸의 **개수**가 바뀌는 배치(`grid-template-columns`)는 애니메이션 대상으로 쓰지 않습니다 — 트랙 수가 다르면 보간되지 않습니다. `justify-content`처럼 보간되지 않는 속성으로 정렬을 바꾸지도 않습니다.
**상자 밖이면** — 호스트의 전환 효과는 호스트 것입니다. 내 것과 길이·곡선을 맞출 수 없으면 **내 쪽을 없애는 것**이 어긋난 두 개보다 낫습니다.
**실패한 자리** — 접히는 영역을 줄바꿈 허용으로 두었더니, 접는 도중 중간 폭에서 요소들이 아랫줄로 감겼다가 다시 튀어 올랐습니다.

### 8. 새 호스트에 얹는 절차

**규칙** — 남의 시스템 위에 얹을 때는 호스트의 어휘를 먼저 찾고, 내 경계를 재고, 경계 밖은 "적용 불가"로 적습니다.
**내 상자 안** — 세 단계입니다. ① 호스트의 어휘를 찾습니다(색·간격·모서리를 무엇으로 부르는지, 내 상자를 감싸는 것이 무엇인지). ② 내 경계를 잽니다(내가 폭을 정하는 곳이 어디까지인지). ③ 적용 불가 목록을 적습니다.
**상자 밖이면** — 이 절 전체가 경계를 다루므로 따로 없습니다. 다만 **①을 기억으로 하지 않습니다.** 호스트의 실제 소스나 화면에서 확인합니다.
