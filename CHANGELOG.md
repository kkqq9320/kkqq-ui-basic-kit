# 변경 이력

버전은 태그로 답니다. 소비 프로젝트는 SHA 대신 태그로 걸 수 있습니다:

```bash
npm i github:kkqq9320/kkqq-ui-basic-kit#v0.12.0
```

`0.x`에서는 **minor 자리가 breaking 자리**입니다(semver의 0.x 규칙).

각 버전 제목 아래의 **전체 diff** 링크는 그 릴리스에 들어간 변경을 커밋 단위가 아니라
**한 덩어리로** 보여 줍니다 — 커밋 스무 개를 훑는 대신 그것부터 보세요.

---

## 미출시

**`src/` 이름·자리 정리 2라운드 — 소비자에게 보이는 변화가 0입니다.** 공개 이름이
하나도 안 바뀌었고(`publicApi.test.ts`가 지킵니다) 파일만 갈라졌습니다.

### 더해진 것

- **`Pressable`** — 킷의 모든 `<button>`이 지나가는 자리(내부용, 배럴에 없습니다).
  **옷은 없고 보장만** 있습니다: `type="button"` 기본값.

  🔴 **재 보니 넷이 `type`을 안 적고 있었습니다** — 드롭다운 트리거 셋과 휠 트리거
  하나. 폼 안에서 `<button>`의 기본값은 `submit`이라 **값을 고르는 순간 폼이
  날아갑니다.** 데모 한 화면에서만 그 넷이 **22개**로 렌더됩니다(트리거는 반복됩니다).
  지금은 화면의 버튼 77개 전부 `type="button"`입니다(브라우저 실측).

  `tests/pressable.test.tsx`가 **`src/`에 날 `<button>`이 다시 생기면** 빨개집니다.

- **폴더 안의 되풀이 이름 넷을 걷었습니다.** 폴더가 이미 말한 것을 파일 이름이 다시
  말하면 이름이 아무것도 안 말합니다 — `shortcuts/shortcuts.ts`를 열기 전에는 안에
  무엇이 있는지 알 수 없었습니다.

  ```
  shortcuts/shortcuts.ts        →  shortcuts/combo.ts
  shortcuts/shortcutStorage.ts  →  shortcuts/storage.ts
  theme/themeTokens.ts          →  theme/tokens.ts
  theme/themePalette.ts         →  theme/palette.ts
  ```

  🟢 **공개 이름은 하나도 안 바뀝니다** — 배럴이 같은 이름을 계속 내보냅니다.
  컴포넌트 파일(`ShortcutProvider.tsx` 등)은 접두사가 **내보내는 이름**이라 그대로입니다.
  `tests/`는 평면이라 접두사가 일을 하므로 역시 그대로입니다.

- **`src/`가 폴더로 묶였습니다** — `controls/` · `surfaces/` · `theme/` · `shortcuts/` ·
  `browser/` · `model/`. **축을 새로 만들지 않았습니다**: 배럴이 이미 가진 여섯 역할
  묶음 그대로라 **목차와 파일 트리가 같은 축**입니다.

  🟢 **소비자는 0입니다** — 배럴(`kkqq-ui-basic-kit`)로 import 하면 아무것도 안 바뀝니다.
  ⚠️ deep import(`kkqq-ui-basic-kit/src/Select`)를 쓰던 앱만 경로에 폴더가 붙습니다
  (`…/src/controls/Select`). 실측상 그런 소비자는 없습니다.

### 고친 것

- **`hooks.ts`(542줄)를 셋으로 갈랐습니다.** 이름이 `hooks`라 **무엇이든 들어갈 수
  있는 자리**였고(§15 규칙 4가 금지하는 넓은 이름) 실제로 세 주제가 들어 있었습니다.
  재 보니 셋 사이의 **참조가 0건**이라 이음매가 이미 나 있었습니다.

  | 새 파일 | 무엇 | 내보내는 것 |
  |---|---|---|
  | `src/popupDismiss.ts` | 팝업이 닫히는 경로 — Escape·뒤로가기·깊이 | `PopupDepthContext` · `useEscapeToClose` · `useBackToClose` |
  | `src/scrollDirection.ts` | 스크롤 방향으로 바를 숨기는 판정 | `useScrollDirectionHidden` |
  | `src/visualViewport.ts` | `window.visualViewport`를 감싸는 훅들 | `useVisualViewportBox` · `useVirtualKeyboard` · `useVirtualKeyboardOpen` |

  🟢 **배럴이 같은 이름을 계속 내보내므로 소비자는 0입니다.** deep import(`design-system/src/hooks`)를
  쓰던 앱만 경로를 바꾸면 되는데, 실측상 그런 소비자는 없습니다.

### 눈으로 볼 것

**없습니다** — 순수 이동입니다. 데모에서 다이얼로그를 열고 Escape로 닫아 확인했습니다:
history 표식이 push → 정리(null)까지 그대로 돕니다.

---

## v0.12.0 — 2026-08-18

[전체 diff: v0.11.0...v0.12.0](https://github.com/kkqq9320/kkqq-ui-basic-kit/compare/v0.11.0...v0.12.0)

**라운드 둘이 한 릴리스에 들어갔습니다.**

- **`src/` 이름·자리 정리 1라운드 — 소비자에게 보이는 변화가 0입니다.** 공개 이름
  120개(값 75 · 타입 45)가 하나도 안 바뀌었고, 이제 그 목록을 테스트가 못 박습니다.
- **`Button` 라운드 — 🔴 CSS 수준에서 breaking입니다.** `import`는 한 글자도 안
  바뀌지만 **버튼 클래스가 전부 바뀝니다.** 아래 표부터 보세요.

TypeScript API는 **순수 추가**입니다 — 지워진 공개 이름이 0개이고 `Button` 외 넷이
더해졌습니다(`ButtonProps`·`ButtonSize`·`ButtonVariant`, 그리고 편집기 토큰 셋).

### 🔴 BREAKING — 버튼의 CSS 클래스가 전부 바뀝니다

`Button` 컴포넌트가 생기면서 버튼 옷을 **하나로** 모았습니다. 앱이 이 클래스들을 직접
쓰고 있었다면 고칠 곳입니다(`import`는 한 글자도 안 바뀝니다).

| 옛것 | 새것 |
|---|---|
| `className="primary"` | `<Button variant="primary">` |
| `className="secondary-button"` | `<Button>` (기본값) |
| `className="danger-button"` · 다이얼로그의 `className="danger"` | `<Button variant="danger">` |
| `className="text-button"` | `<Button variant="text">` |
| `className="button-row"` | `className="action-row"` |
| `.link-button` · `.file-button` | **삭제** — 킷 안에서 아무도 안 썼습니다 |

컴포넌트를 안 쓰고 클래스만 쓰려면 `class="action-button" data-variant="…"` 입니다.

🔴 **그리고 이것이 제일 조용한 breaking입니다: 액션 줄 안의 날 `<button>`은 이제 옷이
없습니다.** 예전에는 통이 자식을 대신 칠했습니다(`.button-row button:not(.primary)` ·
`.dialog-actions button` · `.wheel-actions button`). 그 암묵 규칙이 바로 위 결함의
원인이라 없앴고, **각 버튼이 자기 종류를 말해야** 합니다.

```diff
- <div className="button-row"><button>취소</button></div>
+ <div className="action-row"><Button>취소</Button></div>

- <DialogActions><button>취소</button></DialogActions>
+ <DialogActions><Button>취소</Button></DialogActions>
```

안 고치면 **브라우저 기본 버튼**이 나옵니다(실측: `border: 2px outset` · 회색 바탕).
`DialogActions`는 킷 컴포넌트인데 자식은 앱이 주므로 특히 조심하세요 — 옛 README
예제가 정확히 그 마크업이었습니다.

**상태 클래스 넷도 사라졌습니다** — 탭·드롭다운 옵션·사이드바 nav가 같은 상태를
`aria-*`와 클래스로 **두 번** 말하고 있었습니다. 이제 속성 하나만 씁니다:
`.settings-tabs button.active` → `[aria-selected="true"]`,
`.app-select-menu button.selected` → `[aria-selected="true"]`,
`.sidebar nav …​.active` → `[aria-current="page"]`.

### 🔴 고친 것 — 행 안의 삭제 버튼이 위험 색을 잃고 있었습니다

```
.danger-button                    (0,1,0)
.button-row button:not(.primary)  (0,2,1)   ← 이깁니다
```

행이 자식 버튼을 대신 칠했고 그 선택자가 더 구체적이라, **`.button-row` 안의
`danger-button`은 secondary로 그려졌습니다**(데모 실측: `color` = `--text`).
그리고 이 README가 **정확히 그 마크업을 가르치고 있었습니다.** 각 버튼이
`data-variant`로 자기 종류를 말하면서 그 규칙 자체가 없어졌습니다.

### 더해진 것

- **`Button`** — 킷의 유일한 버튼 컴포넌트. `variant` 한 축(`primary`·`secondary`·
  `danger`·`text`), `type`은 기본이 `"button"`(폼 안에서 취소가 폼을 보내던 종류를
  막습니다), `size`는 안 주면 문맥이 정합니다(§2).
- **`PRINCIPLES.md` §16 — 상태를 어떻게 적는가.** ARIA가 이미 말하는 상태는 그 속성을
  칠하고(클래스 사본 금지), 종류는 `data-*` 한 축, 순수 시각 상태만 `data-*`,
  부품 이름만 클래스. **아직 안 지키는 자리 표**를 같이 적었습니다.
- **토큰 셋** — `--on-accent`(강조색 채움 위 글자, 여덟 곳) · `--on-badge`(한 곳) ·
  `--sidebar-bright`(사이드바 밝기 사다리의 맨 윗칸, 열 곳). 전부 `#fff`로 흩어져
  있던 것이고 편집기에 노출됩니다 — 채움만 바꾸고 그 위 글자는 못 바꾸던 상태였습니다.
- **`PRINCIPLES.md` §15 — `src/` 파일 이름과 자리.** 이 저장소는 대소문자로 종류를
  구별하는 관습(대문자 `.tsx` = 컴포넌트, 소문자 `.ts` = 모듈)을 처음부터 지켜 왔는데
  **어디에도 적혀 있지 않았습니다.** 네 규칙과, **아직 그 규칙을 안 지키는 자리 표**를
  같이 적었습니다.
- 배럴이 **목차**가 됐습니다 — 역할별 묶음(컨트롤 → 표면·레이아웃 → 테마 → 단축키 →
  킷 전역 설정 → 훅·순수 헬퍼)에 묶음마다 머리말이 붙습니다.

### 고친 것

- **`selectKeyboard.ts`가 `Select.tsx`를 import 하고 있었습니다.** 순수 계산이
  컴포넌트에 의존하는 유일한 거꾸로 화살표였습니다(`import type`이라 런타임 비용은 0).
  그 계산이 실제로 읽는 것은 `value`·`disabled` 둘뿐이라, **공개 타입을 옮기지 않고**
  최소 모양을 그 파일에 선언해 없앴습니다.
- **`isPrimaryButton`이 `positioning.ts`(팝업 배치)에 있었습니다.** 마우스 버튼 번호
  판정이라 위치와 무관합니다 → `src/pointerButton.ts`.
- **`SectionHeading`이 `SectionTabs.tsx`에 있었습니다.** 탭이 아니라 §7 배치 스택의
  일부입니다 → `src/PageChrome.tsx`. CSS 규칙도 `tabs.css` → `page.css`로 같이
  옮겼습니다(클래스 이름 `.settings-section-heading`은 **그대로** — 앱이 직접 걸 수
  있어 이름을 바꾸면 breaking입니다).

### 눈으로 볼 것

🔴 **하나 달라집니다: 액션 줄 안의 삭제 버튼이 이제 실제로 빨갛습니다.** 위 "고친 것"의
명시도 결함 때문에 그동안 secondary로 그려지고 있었습니다. 고침이지만 **화면이 바뀌는
고침**이라 여기 적습니다.

그 밖에는 **없습니다.** 데모의 액션 버튼 21개 계산값을 바꾸기 전에 찍어 두고 대조했고,
삭제 버튼 하나만 달라졌습니다. 다이얼로그·휠 팝오버도 열어서 확인했습니다
(32px · min-width 64 · 보조는 muted).

`SectionHeading`의 CSS 이사도 **화면은 그대로입니다 — 브라우저에서 세고 확인했습니다.**
CSS 블록이 `tabs.css`에서 `page.css`로
가면서 캐스케이드상 **뒤로** 갔으므로(즉 예전에 지던 동점을 이제 이깁니다) 경쟁자가
있는지 직접 셌습니다: 문서의 규칙 416개 중 섹션 제목의 세 요소에 닿는 것은 보편
셀렉터 `*`와 **옮긴 블록 자신뿐**입니다. computed 값도 선언대로입니다
(`min-height: 57px` · `font-size: 13px` · `line-height: 19px`).

⚠️ **딱 하나 예외가 있습니다 — `css/index.css` 대신 파일을 골라 import 하는 앱.**
전에는 `tabs.css`만 넣어도 섹션 제목이 스타일을 받았고, 이제 **`page.css`가 필요**
합니다. `index.css`를 쓰면 해당 없습니다(둘 다 들어갑니다). README "골라 쓰기"에
적었습니다.

### 검사

1519 통과 / 46 파일(직전 1500 / 43) · TZ=UTC 1519 · `tsc` · `tsc:src`.
새 파일 셋(`publicApi` · `moduleLayers` · `pointerButton`), 변이 열넷으로 전부 빨강 확인.

🔴 그중 하나는 **처음 쓴 형태가 틀려서** 다시 썼습니다: §15의 크기 표를 정확한 줄
수로 대조했더니 무관한 import 한 줄에 빨개졌습니다. 그런 가드는 아무 생각 없이 숫자만
올리는 습관을 만듭니다 — `약 N줄` ±5%로 바꿔 **파일을 실제로 쪼갤 때만** 빨개집니다.

---

## v0.11.0 — 2026-08-16

[전체 diff: v0.10.0...v0.11.0](https://github.com/kkqq9320/kkqq-ui-basic-kit/compare/v0.10.0...v0.11.0)

**커밋 12개. 전부 고침입니다 — 깨지는 것도, 새 API도 없습니다.**

🔴 **넷이 전부 같은 형태였습니다:** 브라우저의 기본 동작을 `preventDefault`로 막아 놓고,
**대신 하겠다던 일을 어떤 조건에서는 못 하는데 그 조건에서 막은 것을 되돌리지 않는** 자리.
하나(오너가 폰에서 잡은 붙여넣기)를 고치고 나서 **같은 형태를 훑어** 나머지를 찾았습니다.

이제 넷이 같은 문장을 말합니다 — **못 할 거면 키를 가져가지 않습니다.**


**전부 고침입니다 — 깨지는 것도, 새 API도 없습니다.** 셋 다 같은 형태였습니다:
**브라우저의 기본 동작을 `preventDefault`로 막아 놓고, 대신 하겠다던 일을 어떤 조건에서는
못 하는데 그 조건에서 막은 것을 되돌리지 않는** 자리.

### 🔴 `Ctrl+V`가 비보안 컨텍스트에서 통째로 죽어 있었습니다

`v0.10.0`이 **알려진 잔여**로 적어 둔 자리인데, 원인이 폴백 부재가 아니라 **우리 코드**
였습니다. 오너 실기기 TRACE(`http://<LAN-IP>:15277`)가 그대로 찍었습니다:

    keydown key="v" mods=Ctrl  tgt=button.wheel-trigger.editing
      ↳ 처리됨=Y (preventDefault 호출됨)
    (paste 줄 없음)

같은 캡처에서 `copy` 줄은 찍혔으므로 계측기는 멀쩡했고, `paste`가 없는 것은 이벤트가
**실제로 안 왔다**는 뜻입니다. `Ctrl+V` 분기가 `preventDefault()`를 **조건 없이** 불렀고,
비보안 컨텍스트에는 `navigator.clipboard`가 없어 **읽지도 못하면서 브라우저의 기본
붙여넣기까지 막았습니다.** 기본 동작이 막히면 `paste` 이벤트 자체가 안 생기므로
**어떤 폴백도 걸 자리가 없습니다.**

이제 그 경로는 **막지 않습니다.** 대신 쓸 수 있는(읽기 전용이 **아닌**) 임시
`<textarea>`에 포커스를 옮겨 브라우저가 거기 붙여넣게 하고 다음 틱에 읽습니다 —
편집 불가 요소(`<button>`)에 `paste`가 오는지에 **기대지 않는 것**이 요점입니다.

⚠️ **`localhost`에서는 이 결함이 재현되지 않습니다** — 거기는 보안 컨텍스트입니다.

### 🔴 다이얼로그에서 Tab이 죽는 자리

포커스 트랩의 `FOCUSABLE` 선택자가 **가시성도 포커스 가능성도 안 봅니다.**
`<input type="hidden">`은 `input:not([disabled])`에 **글자 그대로 매치되는데**(폼 안에
흔합니다) `focus()`를 불러도 조용히 아무 일도 안 합니다. 감싸기가 `preventDefault()`를
**먼저** 부르고 `focus()`를 뒤에 불렀으므로, 옮기기가 실패하면 **그 지점에서 Tab이
죽었습니다.**

**정하고 나서 막습니다** — 실제로 포커스가 옮겨진 뒤에만 `preventDefault`.
그리고 `type="hidden"`은 **경계 계산에서도** 걷습니다: 맨 앞이면
`activeElement === first`가 영영 거짓이라 진짜 첫 요소에서 Shift+Tab이 안 걸려
**포커스가 모달 밖으로 샜습니다.** 열 때의 초기 포커스도 같은 맹점이었습니다
(`?? node` 폴백은 "후보가 **없을** 때"만 발동하지 "후보가 포커스를 **못 받을** 때"는
아닙니다).

### 🔴 뒤집을 수 없는데 `a`/`p`가 눌리는 척했습니다

`min`/`max`가 반대 절반을 통째로 막으면 오전/오후 전환이 실패하는데 `preventDefault`는
이미 걸린 뒤였습니다. **킷은 그 판정을 이미 갖고 있었습니다** — 팝오버의 오전/오후
버튼이 정확히 그 조건으로 `disabled`가 됩니다. **키 경로만 그것을 안 봤습니다.**

대가가 둘이었습니다:

1. 치던 숫자 버퍼를 **버려 놓고** 전환은 실패하는 **파괴적 no-op**
2. `defaultPrevented`를 단축키 모듈이 신호로 읽으므로, 앱이 그 구역에 걸어 둔 **맨
   `a`/`p` 액션까지 삼켰습니다** — 픽커에 포커스가 있는 동안만 그 키가 죽습니다

### 🔴 복사할 것이 없는데도 브라우저의 복사를 막았습니다

빈 픽커에서 `Ctrl+C`를 누르면 아무것도 안 쓰면서 **브라우저의 복사까지 막았습니다.**
페이지에서 드래그해 둔 글자를 복사하려던 사람은 아무것도 못 얻고, 아무 신호가 없어
**복사된 줄 알고 붙여넣습니다.**

이 킷은 `Ctrl+C`를 앱이 **다시 정의하지 못하게** 막아 뒀습니다 — "복사·붙여넣기·
되돌리기는 너무 중요해서"가 그 이유입니다(오너 결정 2026-08-13). **그렇게 보호한 키를
픽커 자신이 빈 값에서 먹고 있었습니다.**

이제 셋이 같은 문장을 말합니다 — `Ctrl+V`, `a`/`p`, `Ctrl+C`: **못 할 거면 키를
가져가지 않습니다.**

### 눈으로 볼 것

위 셋 다 **되던 것이 안 되게 바뀌는 것은 없습니다.** 안 되던 것이 됩니다.

---

## v0.10.0 — 2026-08-16

[전체 diff: v0.9.0...v0.10.0](https://github.com/kkqq9320/kkqq-ui-basic-kit/compare/v0.9.0...v0.10.0)

**커밋 12개. 🟢 깨지는 것이 하나도 없습니다** — `0.x`에서 이런 릴리스는 드뭅니다.
라벨 넷은 **필수에서 선택으로** 여는 것이라 전부 채운 라벨 상수를 그대로 통과시키고,
설정 지속성과 클립보드 폴백은 순수 추가·수정입니다.

⚠️ **그런데도 patch가 아니라 minor입니다.** 이 저장소는 patch를 **문서 전용**으로
씁니다(`v0.4.1`이 그 기준을 세웠습니다: "타입도 런타임도 CSS 값도 바뀐 것이
없습니다"). 이번에는 셋 다 바뀝니다.

**눈으로 볼 것 둘** — 타입은 그대로인데 사용자가 다른 것을 봅니다:

1. **사용자가 한 번 바꾼 설정이 이제 유지됩니다**(12시간제·휠 행 수). 기본값은
   그대로라 아무것도 안 건드린 앱은 지금과 같습니다.
2. **팝오버 확정 줄 위에 10px이 생깁니다** — 경계선이 휠에 딱 붙어 있던 것을 위쪽
   여백과 맞춘 것입니다.


**모델이 "안 그린다"고 말할 수 있는 라벨 넷을 선택으로 엽니다** — `weekdays`,
`meridiem`, `today`, `now`. `v0.9.0`이 **알려진 잔여**로 적어 둔 자리입니다.

⚠️ **`v0.9.0`의 노트는 이것을 `weekdays`·`meridiem` 둘로 적었습니다 — 재 보니 넷이었습니다.**
`today`·`now`도 같은 자리였습니다: `seedAction`이 `null`을 내면 **씨앗 버튼 자체가 안**
**그려지는데** 그 이름만 필수였습니다. 노트를 diff와 맞춰 보는 분을 위해 적어 둡니다.

계약은 이미 그 넷이 없을 수 있다고 말하고 있었습니다: `seedAction`이 `null`을 내고
(씨앗 버튼 없음), `meridiem()`이 `null`을 내고, `hourFromTwelve`는 선택이고, 기간
모델의 `label`은 인자를 둘만 받습니다(요일 없음). **라벨만 필수로 남아 계약과 어긋나**
**있었고**, 그래서 공개 상수 `DEFAULT_DURATION_LABELS`가 기간이 그리지도 않는 이름
넷을 지고 나갔습니다.

### 깨지는 것 — 없습니다

**필수에서 선택으로 여는 것은 소비자를 안 깹니다** — 전부 채운 라벨 상수는 그대로
컴파일됩니다. `labels` prop은 원래도 `Partial<…>`이라 부분 override는 영향이 없습니다.

### 바뀐 것

- **`DEFAULT_DURATION_LABELS`가 안 그리는 이름을 안 싣습니다.** `DEFAULT_WHEEL_LABELS`를
  통째로 펼치던 것을 그만두고 필요한 것만 적습니다. 🔴 **"안 쓰니까 그냥 두자"가 아닌**
  **이유**: 라벨을 영어로 바꾸는 소비자는 이 상수를 복사해 출발점으로 씁니다 — 그러면
  요일 일곱 개를 번역하게 되고, 그 번역은 **아무 데도 안 나타납니다.** 무엇이 틀렸는지
  알 방법이 없는 자리입니다.
- **`DEFAULT_WHEEL_LABELS`의 타입이 `satisfies WheelLabels`가 됐습니다**(주석 타입
  `: WheelLabels`이 아니라). 안 그러면 이 상수의 그 필드들까지 `| undefined`가 되어
  **폴백이 폴백 노릇을 못 합니다.** 값과 적합성 검사는 그대로입니다.
- 기계가 `weekdays`·`today`·`now`를 `?? DEFAULT`로 받습니다. 병합이
  `{ ...DEFAULT, ...override }`라 실제로 비는 길은 **명시적 `undefined`** 하나뿐인데,
  `meridiem`이 이미 같은 자리에 같은 검사를 갖고 있었습니다.

### ⚠️ `units`의 누수 함정은 여기 안 생깁니다 — 병합 방식이 다릅니다

`units`는 **키마다 펼쳐서**(`{ ...DEFAULT.units, ...override.units }`) 연·월·일만 영어로
준 소비자에게 한국어 `"시"`가 남고, 그것이 열의 `aria-label`로 **스크린리더에**
**나갑니다**(화면에는 안 보입니다 — `v0.8.0` BREAKING 5가 그래서 여섯을 다 필수로
닫았습니다). 요일은 **배열 통째 교체**, 오전/오후는 **객체 통째 교체**라 "절반만 영어"인
상태를 만들 수 없습니다.

### 킷 전역 설정이 새로고침을 넘깁니다

**12시간제(`hourFormat`)와 휠 행 수(`wheelRowsPerSide`)가 `localStorage`에 남습니다**
(설계 스펙 §16-3). 행 수는 **오너가 실기기에서 정한 값**인데 새로고침마다 사라지고
있었습니다. 규약은 `themeTokens.ts`의 것을 그대로 씁니다 — `namespace:키`, 읽기·쓰기
**둘 다** `try/catch`(저장소를 전면 차단한 브라우저는 **접근만 해도** 던집니다),
읽은 값은 믿지 않고 검증.

- **더해진 공개 API 둘** — `getHourFormatServerSnapshot`, `getWheelRowsPerSideServerSnapshot`.
  `useSyncExternalStore`의 **세 번째 인자** 전용입니다. 지속성이 없을 때는 그 자리에
  읽기 함수를 그대로 넘기는 것이 맞았는데(서버든 클라이언트든 언제나 기본값), 저장값을
  읽기 시작하면 **서버가 그린 HTML은 기본값인데 하이드레이션 첫 렌더가 저장값**이 되어
  어긋납니다. SSR을 안 쓰면 신경 쓸 것 없습니다.
- ⚠️ **핀을 올리는 소비자에게 보이는 변화**: 사용자가 한 번 바꾼 설정이 이제 유지됩니다.
  기본값은 그대로라 아무것도 안 바꾼 앱은 지금과 같습니다.

### 비보안 컨텍스트의 복사 — 보류분 해제

**`Ctrl+C`가 `http://<LAN-IP>` 같은 비보안 주소에서 통째로 죽어 있었습니다.** 거기서는
`navigator.clipboard`가 **아예 없습니다**(실측: `isSecureContext: false`). 즉 이 기능은
**정확히 필요한 그 환경에서만** 조용히 아무 일도 안 했습니다.

⚠️ **`localhost`에서는 재현되지 않습니다** — 거기는 보안 컨텍스트입니다. 데모를 눈으로
보는 것으로는 못 잡는 종류입니다.

폴백은 `execCommand("copy")` + 임시 `<textarea>`입니다. 추측이 아니라 이 저장소의
데모 TRACE 패널이 쓰는 바로 그 패턴이고 오너가 폰에서 실제로 써 왔습니다. 임시 요소는
**투명하게 제자리**에 두고(화면 밖이면 iOS가 거기로 스크롤합니다) **포커스를 반드시**
**되돌립니다** — 이 컨트롤은 키를 트리거가 받으므로 안 돌려주면 복사 한 번에 키보드
조작이 죽습니다.

🔴 **붙여넣기는 덧대기만 했고 아직 확인 못 했습니다.** 읽기에는 폴백이 없습니다
(`execCommand("paste")`는 웹 콘텐츠에서 막혀 있습니다). 남는 길은 브라우저가 `Ctrl+V`의
기본 동작으로 보내는 `paste` 이벤트뿐인데, 트리거가 `<button>`이라 **편집 가능한 요소가
아니어서** 그 이벤트가 오는지를 확인할 계측 수단이 없었습니다. **이벤트가 안 오면 지금까지와
정확히 같고, 오면 그때부터 동작합니다** — 실기기 확인 항목입니다. 데모 TRACE가 이제
`copy`·`paste`·`cut`을 찍으므로 한 번 눌러 보면 갈립니다.

### 팝오버 아래 여백

**확정 줄의 경계선이 휠에 딱 붙어 있던 것**(오너 리포트). 상자로만 보면 위아래가 둘 다
0이라 안 보입니다 — 눈에 보이는 간격은 각자 다른 데서 나옵니다: 위는 머리말의
`padding-bottom` 10px, 아래는 확정 줄의 `border-top`이 **0px**로 붙고 버튼만 11px
떨어져 있었습니다. 즉 **버튼은 이미 떨어져 있었고 선이 붙어 있었습니다.**


### 곁가지

**`CUSTOMIZING.md`가 컴파일 안 되는 것을 가르치고 있었습니다** — `units{year,month,day}`
처럼 셋만 주라고 적혀 있는데 `v0.8.0`부터 여섯이 다 필수라 tsc가 거절합니다(실측).
왜 여섯이어야 하는지도 함께 적었습니다.

---

## v0.9.0 — 2026-08-16

[전체 diff: v0.8.0...v0.9.0](https://github.com/kkqq9320/kkqq-ui-basic-kit/compare/v0.8.0...v0.9.0)

**커밋 35개. 휠 피커가 시점 전용 컨트롤에서 벗어난 릴리스입니다.** 새 컴포넌트가 둘
(**`TimeWheelPicker`**, **`DurationWheelPicker`**), 열마다의 격자(`step`)가 생겼고,
기계가 **모델을 프롭으로 받게** 되었습니다 — `WheelModel`의 구현이 하나에서 둘이
되면서 계약이 어디서 시점 쪽으로 기울어 있었는지가 드러났습니다.

**minor인 이유 — BREAKING이 여섯입니다.** `0.x`에서 minor가 breaking 자리입니다.

⚠️ **여섯 중 소비자에게 닿는 것은 셋뿐입니다** — 아래 "깨지는 것 — 셋"의 이름 정리
(CSS 클래스 · CSS 파일명 · 공개 타입 셋)입니다. 나머지 셋은 `WheelModel` 계약인데
**그 타입은 배럴에 없습니다**(`src/index.ts`가 일부러 안 내보냅니다) — 자기 모델을
구현한 쪽만 해당하고, 지금 그런 소비자는 없습니다.

**소비자의 `import`는 한 글자도 안 바뀝니다** — `DateWheelPicker`는 이름도 경로도
그대로입니다(이제 래퍼입니다). 실제로 고칠 곳은 **앱 CSS가 킷 내부를 겨냥한 자리**입니다.

**휠 피커에 열마다의 격자(`step`)** — 휠 피커 시간 확장 5단계 중 **4단계**(설계 스펙
`docs/design/2026-08-12-wheel-picker-time-design.md` §8). **순수 추가입니다** — `step`을
안 넘기면 지금까지와 글자 하나 안 바뀝니다(기존 검사 1345개가 그대로 초록).

**휠 피커 5단계 — 이름 정리와 `TimeWheelPicker`.** 이 컨트롤은 `v0.8.0`부터 시·분·초를
다루는데 클래스·파일·컴포넌트 이름만 "date"에 남아 있었습니다. **기능은 하나도 안
늘어납니다** — `<DateWheelPicker fields={["hour","minute"]}>`로 이미 되던 일에 정직한
이름을 주는 것입니다.

### 🔴 깨지는 것 — 셋

1. **CSS 클래스 `.date-wheel-*` → `.wheel-*`** (30종, 481건). 앱 CSS에서 킷 내부를
   겨냥하던 곳은 고쳐야 합니다.
2. **`css/date-picker.css` → `css/wheel-picker.css`.** `css/index.css` 한 줄로 쓰는
   쪽은 영향 없고, **개별 import 하던 소비자만** 경로를 고칩니다. 옛 이름을
   `@import` 한 줄로 남기지 않은 이유는 같은 일을 하는 이름이 둘이 되기 때문입니다.
3. **공개 타입 이름 셋** — `DateWheelLabels` → `WheelLabels`,
   `DEFAULT_DATE_WHEEL_LABELS` → `DEFAULT_WHEEL_LABELS`,
   `DateWheelPickerProps` → `WheelPickerProps`. `DateWheelUnit`은 `WheelUnit`의
   별칭이었을 뿐이라 **없앴습니다**(`WheelUnit`을 쓰세요).
   측정: 소비자 사용 **0곳**(`budget`은 `labels=`를 한 건도 안 넘기고 `homa_gwangju`는
   이 컴포넌트를 안 씁니다).

**기간(duration) 모델과 그것이 드러낸 계약 정리.** 오너 선택으로 **계약을 추측으로 고치지
않고** 두 번째 모델을 먼저 만들었습니다 — 그리고 기계에 실제로 꽂아 보고서야 드러난 것이
둘, 계약이 찢어지는 자리가 셋이었습니다.

### 더해진 것 (기간)

- **`DurationWheelPicker`** — 기간 쪽 래퍼이자 이 릴리스의 새 공개 컴포넌트. 기본
  `fields`는 시·분(`"01:30"` = 1시간 30분)이고, 사다리(연·월·일·시·분·초)에서 잘라낸
  연속 구간이면 무엇이든 받습니다 — `["day","hour"]`처럼 가운데를 잘라도 되고,
  `["year","month"]`로 "2년 3개월"짜리 계약 기간도 됩니다.
  🔴 **시점 래퍼 둘처럼 구간을 나눠 갖지 않습니다** — 값 도메인 자체가 달라 겹칠 일이
  없습니다. 같은 `["hour","minute"]`이라도 `TimeWheelPicker`는 "오전 3시 30분"이고
  이쪽은 "3시간 30분"입니다.
  기본 라벨 **`DEFAULT_DURATION_LABELS`**도 함께 내놓습니다 — `units`가 **자리 이름**이
  아니라 **세는 이름**입니다(`month`가 "월"이 아니라 "개월").
  ⚠️ **알려진 잔여** — `WheelLabels`가 아직 `weekdays`와 `meridiem`을 **필수로** 요구해서,
  기간이 안 쓰는 값이 `DEFAULT_DURATION_LABELS`에 그대로 실려 나갑니다. 그리는 것에는
  영향이 없고(모델의 `meridiem`이 `null`이라 오전/오후 토글 자체가 안 그려집니다)
  계약이 아직 시점 쪽으로 기울어 있다는 표시입니다 — 정리의 다음 후보입니다.
- **`instantModel.durationModel`이 아니라 `src/model/duration.ts`의 `durationModel`** —
  `WheelModel`의 두 번째 구현. 단위는 연·월·일·시·분·초로 시점과 **이름이 같고 뜻이
  다릅니다**(`month`가 "몇 월"이 아니라 "몇 개월"). 그래서 바닥값이 전부 0입니다.
- 값은 고정폭 `YY:MM:DD:HH:MM:SS`(17자). `min`/`max`가 문자열 접두 슬라이스라
  ISO 8601로는 성립하지 않습니다.
- **트리거는 모든 열에 단위 글자, 자리는 안 채웁니다** — `3d 4h 10m` · `2y 3mo` ·
  `1h 30m`. 전부 콜론이면
  `03:04:00`처럼 어느 자리가 일인지 알 수 없습니다(기간은 열 조합이 자유로워 자리만으로
  못 읽습니다). 글자는 **ASCII**(`y`·`mo`·`d`) — 트리거에 한국어를 박으면 영어 라벨을 준
  소비자가 `units` 누수와 같은 자리에 다시 걸립니다. `month`는 **`mo`**입니다 — `m`은 분과
  겹칩니다.
- **트리거는 꼬리의 0을 안 그립니다** — `3d 4h 0m` → `3d 4h`. ⚠️ **꼬리만입니다** —
  트리거의 조각은 누르면 그 열이 활성이 되는 자리이기도 해서(가는 포인터), 가운데를 빼면
  그 열을 마우스로 고를 방법이 없어집니다(`3d 0h 10m`은 그대로). 치는 중에도 안 뺍니다 —
  버퍼가 살아 있는 열은 사용자가 지금 보고 있는 자리입니다. 전부 0이면 가장 작은 열
  하나가 남습니다(`0m`).
- **팝오버의 선택된 행 옆에도 같은 기호가 붙습니다**(`d`·`h`·`m`). 시각 열 사이의 `:`와는 다른
  것입니다 — 저건 두 열을 잇는 **구분자**로 열 앞에 오고, 이건 그 열이 무엇인지 말하는
  **단위 표시**로 열 뒤에 옵니다. `WheelModel.columnMark`(선택)가 값을 정하고 기계가
  `--wheel-unit-mark`로 내려보냅니다 — CSS가 단위 이름을 알지 않게 하려는 것입니다.
- **타이핑이 두 자리를 다 안 채워도 확정합니다** — 분 열의 `6`처럼 상한을 넘길 수 없는
  첫 자리는 그 자리에서. 상한 없는 맨 위 열은 두 자리를 다 받습니다.
- **`WheelModel.typeDigit`이 `fields`를 받습니다**(네 번째 계약 정리). 기간은 상한이 열
  조합에 달려 있어 그것 없이는 조기 확정을 판정할 수 없습니다 — 시점은 상한이 문맥과
  무관해서 이 인자 없이도 됐고, 그래서 계약이 오랫동안 시점 모양이었습니다.
- **자리올림 없음. 그리는 열 중 맨 위만 무제한** — 59분 +1은 0분이고 시는 그대로입니다.

### 🔴 깨지는 것 — `WheelModel` 계약 셋

1. **`now(timeZone, fields)` → `seed(timeZone, fields)`.** 그 이름이 **세 가지 일을**
   **겸하고** 있었습니다(씨앗값 · 씨앗 버튼이 확정하는 값 · `resetTarget`의 기준값).
   기간은 1번만 답할 수 있는데 2번을 거절할 방법이 없어 **`지금` 버튼이 그대로 떴습니다.**
2. **`family(fields): ValueFamily` → `seedAction(fields): "today" | "now" | null`.**
   기계가 그 값으로 실제로 하던 일은 **버튼과 안내 문구의 이름을 고르는 것뿐**이었습니다.
   이름이 답하는 질문과 실제 쓰임이 달라서 기간이 `"time"`이라고 **거짓말**해야 했습니다.
   `null`이면 버튼 자체가 안 그려집니다. `familyOf`는 모델 안에 그대로 있습니다.
3. **`hourFromTwelve`가 선택이 됐습니다.** 90시간짜리 기간에 오전/오후가 없습니다.

### 바뀐 것 (기간)

- **`WheelPicker`가 `model`을 프롭으로 받습니다.** `const model = instantModel`이 박혀
  있었습니다. 기계는 이제 구체 모델을 **import 하지도 않습니다** — 계약 타입만 압니다.
  래퍼가 자기 모델을 넘기므로 소비자에게는 안 보입니다.
- 기계에 남아 있던 하드코딩된 단위 이름(`fields[0] ?? "year"`)이 `model.units[0]`이
  됐습니다. 기간 모델에 `"year"`는 **존재하지도 않는 이름**입니다.

---

### 더해진 것 (5단계)

- **`TimeWheelPicker`** — 시각 쪽 구간의 래퍼. 기본 `fields`는 시·분이고
  `["hour","minute","second"]`로 초를 켭니다(네이티브 `<input type="time">`과 같은 기본).
- **`DateWheelPicker`는 그대로 남습니다** — 이제 **래퍼**이고, 기본 `fields`는 연·월·일.
  **소비자의 import는 한 글자도 안 바뀝니다**(배럴 하나라 이름만 늘어납니다).
- 🔴 **두 래퍼가 구간을 나눠 갖습니다** — 이쪽은 날짜 쪽에서 시작하는 구간, 저쪽은 시각
  쪽에서 시작하는 구간. 겹치는 조합이 없어 **같은 일을 하는 방법이 둘 생기지 않습니다.**
  반대쪽 구간을 주면 개발 모드에서 경고합니다(던지지 않습니다).

### 바뀐 것 (5단계)

- **기계가 `src/WheelPicker.tsx`로 옮겨졌고 `WheelPicker`가 됐습니다.** 배럴에서
  **일부러 안 내보냅니다** — 기계를 직접 쓸 수 있으면 위의 구간 분할이 무의미해집니다.
  `exports["./src/*"]`로 `src/DateWheelPicker`를 직접 import 하던 곳은 그대로 됩니다
  (그 경로에 이제 래퍼가 있고, 옛 이름들을 다시 내보냅니다).
- 기계의 `fields`는 **필수**가 됐습니다. 기본값을 아는 것은 래퍼의 일입니다.

---
### 더해진 것

- **`step?: Partial<Record<WheelUnit, number>>`** — `step={{ minute: 15 }}`처럼 필요한
  열만 줍니다. 15분 단위 예약 같은 자리를 위한 것입니다.
  - **한 노치가 한 칸**입니다 — 휠·화살표·스와이프·± 버튼 전부.
  - 🔴 **격자의 기준점은 그 열의 바닥값**(시·분·초 0, 월·일 1)이지 `min`이 아닙니다.
    `min`으로 잡으면 같은 `step`을 준 픽커가 `min`에 따라 **다른 값 집합**을 내줍니다.
  - **타이핑이 격자에 안 떨어지면 내립니다** — 올림이 아닙니다(친 것보다 큰 값을
    만들어 주지 않는다는 이 컴포넌트의 기존 규칙).
  - **격자가 열을 딱 나누지 못해도 허용합니다** — 분 `step` 7이면 `0, 7 … 56` 다음이
    `0`이고 마지막 간격만 4입니다. 금지하면 시 `step` 5(24를 안 나눔)가 통째로 막힙니다.
  - 🔴 **`min`/`max`는 격자보다 우선이라, 격자 밖이어도 끝점으로 들어옵니다.**
    `min="03:07"`에 분 `step` 15면 열은 `03:07 → 03:15 → 03:30`이고 `03:07` 아래는
    `—`입니다.
  - ⚠️ **그 열의 상한(말일·59분)은 다릅니다 — 끝점으로 안 들어옵니다.** 분 `step` 7의
    열이 `0 … 56` 다음 바로 `0`으로 순환하지 59를 끼워 넣지 않는 것과 같은 규칙입니다.
    2월에 일 `step` 5면 열은 `1·6·11·16·21·26`이고, 여기서 `31`을 쳐도 **26**입니다.
    휠이 내줄 수 없는 값(28)에 앉히면 위로 한 칸이 1로 튑니다.
  - **잘못된 값은 던지지 않고 `1`로 읽습니다** — `0`·음수·`Infinity`·`NaN`. 잘못된
    `fields`를 던지지 않는 것과 같은 결입니다.
- **`instantModel.stepOf` · `snapToStep` · `snapValue` · `WheelStep` 타입** — 값 모델 쪽
  공개 API. `snapValue`는 값 **전체**를 각 열의 격자로 내립니다(`지금` 버튼처럼 여러 열이
  한꺼번에 정해지는 자리가 씁니다).
- **`지금`(`Ctrl+;`)과 빈 값의 기준값도 격자로 내려갑니다.** 안 내리면 분 `step` 15에서
  `47`을 **타이핑하면 45가 되는데** `지금`은 03:47을 남겨, 같은 수에 이르는 두 경로가
  갈립니다. ⚠️ **소비자가 준 `value`는 안 내립니다** — 내리면 `onChange` 없이 화면만
  달라져 트리거가 소비자가 안 들고 있는 값을 그립니다. 격자 밖 값은 `min`/`max` 끝점과
  같은 취급으로 그대로 보여 줍니다.
- **격자점이 하나뿐인 열은 값이 안 바뀔 때 `onChange`를 부르지 않습니다.**
  `{ minute: 100 }`처럼 격자가 열보다 크면 어느 쪽으로 밀어도 같은 값인데, 예전에는
  누를 때마다 아무것도 안 바뀐 `onChange`가 나가고 되돌리기 스택만 쌓였습니다.
  `WheelModel` 계약의 `shift`·`setUnit`이 `step`을 선택 인자로 받습니다.

---
## v0.8.0 — 2026-08-14

[전체 diff: v0.7.0...v0.8.0](https://github.com/kkqq9320/kkqq-ui-basic-kit/compare/v0.7.0...v0.8.0)

**커밋 156개, 가장 큰 릴리스입니다.** 새 모듈이 둘(**단축키**, **세그먼트 컨트롤**),
날짜 피커가 **시각까지** 다루게 되었고, 오너의 실기기 리포트 열여섯 라운드가 들어갔습니다.

**minor인 이유 — BREAKING이 여섯입니다.** `0.x`에서 minor가 breaking 자리입니다.
아래 "🔴 깨지는 것 — 여섯"에 모아 뒀고, **화면이 바뀌는 것은 "눈으로 볼 것"에 따로**
있습니다(둘은 다릅니다 — 타입은 그대로인데 사용자가 다른 것을 보는 경우가 있습니다).

**타입 오류로 바로 드러나는 것은 `DateWheelLabels`를 직접 넘기는 경우**(1·4·5)뿐이고,
`labels=`를 안 넘기면 그 셋은 해당 없습니다. 나머지는 조용히 동작이 달라지는 쪽이라
2·3·6과 "눈으로 볼 것"을 먼저 보세요.

⚠️ **알려진 한계 하나** — 날짜 필드의 `Ctrl+C`·`Ctrl+V`는 **보안 컨텍스트(HTTPS·
localhost)에서만** 동작합니다. 평문 HTTP에서는 `navigator.clipboard`가 아예 없어
**조용히 아무 일도 안 합니다.** 사내망 HTTP로 서비스하는 앱은 이 둘을 못 씁니다 —
`Ctrl+Z`·`Ctrl+S`와 나머지 키는 영향 없습니다. 다음 릴리스에서 고칩니다.

### 더해진 것

- **`DateWheelPicker`가 시·분·초를 받습니다** — `fields`에 `hour`·`minute`·`second`.
  **값 형식이 `fields`를 따릅니다**(날짜만 `YYYY-MM-DD` / 시각만 `HH:MM[:SS]` /
  걸치면 `…THH:MM[:SS]`). 기본 `fields`는 그대로 연·월·일이라 **기존 호출부는 글자 하나
  안 바뀝니다.**
- **12시간제** — 킷 전역 설정 `hourFormat`(`getHourFormat`/`setHourFormat`/
  `subscribeHourFormat`), 기본 `"24"`. **시 열은 24칸 그대로**이고 읽는 방식만 바뀝니다.
  **지속성은 아직 없습니다**(새로고침하면 24로 돌아옵니다 — 테마 설정 작업에 입주 예정).
- **오전/오후 버튼**(열이 아니라 버튼, 시 열 바로 위) + **시 세그먼트의 `a`/`p` 키**
  (팝오버를 안 열고) + **12시간제 타이핑 상한 12**(`15` → `5`로 재해석).
- **휠에 보이는 줄 수 설정** — `getWheelRowsPerSide`/`setWheelRowsPerSide(1|2|3|4)`/
  `subscribeWheelRowsPerSide`. **기본 1**(아래 "눈으로 볼 것" 참고).
- **열 초기화 두 가지** — 휠의 **행을 500ms 길게 누르기**, 또는 **가운데(선택된) 행을
  더블탭**. 모든 열은 바닥값으로, **연도만 지금 연도로**. 오프셋 행 연타는 그대로
  여러 칸 이동입니다.
- **`SegmentedControl` 신설** — 값 하나를 몇 개 중에서 고르는 묶음. `radiogroup`+`radio`,
  roving tabindex, `←`/`→`(`↑`/`↓`)·`Home`/`End`, `disabled` 칸은 건너뜀. `css/segmented.css`.
- **토큰 셋 신설** — `--accent-text`(어두운 면 위의 강조색 글자),
  `--segmented-track`·`--segmented-chip`. **셋 다 팔레트 편집기에 나옵니다.**
- **팝오버 폭이 열 개수를 따라갑니다** — 데스크톱에서 트리거가 좁아도 6열이 구겨지지
  않습니다. **3열 픽커와 시각 전용은 무변화**(기존 바닥 292px가 그대로 이깁니다).
- **시각 열 사이에 `:`**, 날짜 묶음과 시각 묶음 사이에 경계 여백.
- **라벨이 컨트롤을 칸 바닥에 맞춥니다**(`label { align-content: space-between }`) —
  라벨 글이 두 줄인 칸과 한 줄인 칸의 입력 상자가 같은 줄에서 끝납니다.

### 고친 것

- **`labels={{ meridiem: undefined }}`가 픽커를 통째로 터뜨리던 것** — 24시간제에서도,
  날짜 전용에서도 매 렌더 크래시였습니다(`useMemo` 의존성 배열이 본문보다 먼저 평가됨).
- **6열에서 그리드가 넘치던 것** — `.date-wheel-column`의 `width: 100%`가 마진을 무시해
  386px 팝오버에 6열 합계가 392px이었습니다.
- **`units`의 한국어 누수** — 라벨을 영어로 override 한 소비자의 시각 열 이름이
  `"시 03"`으로 읽히던 것(아래 BREAKING 5).
- **팝오버 버튼이 가끔 안 눌리던 것**(실기기, 삼성 인터넷) — 브라우저가 `click`을
  합성하지 않는 경우가 있어 **`pointerup`에서 확정**하도록 바꿨습니다.

### 🔴 깨지는 것 — 여섯

1. **`DateWheelLabels.now` 필수**
2. **`DateWheelUnit`이 3-유니온 → 6-유니온** (`src/index.ts`가 재수출하지 않으므로
   `exports["./src/*"]`로 직접 import 한 소비자만 해당)
3. **`min`/`max` 비교 규칙** — 좋은 방향이고 **렌더 크래시 제거를 포함**합니다
4. **`DateWheelLabels.meridiem` 필수** (`{ am, pm }`)
5. **`DateWheelLabels.units`의 `hour`·`minute`·`second` 필수**
6. **`KIT_RESERVED`에 `Ctrl+S`** — 사용자가 **설정 화면에서** `Ctrl+S`(및
   `Ctrl+Shift+S`·`Cmd+S`)를 **녹음할 수 없습니다.** 함께 등록된 `Ctrl+C`·`V`·`Z`는
   이미 `unbindableReason`의 `native-edit`로 막혀 있던 것이라 **새 손실이 아닙니다**(실측).

   > 🔴 **정정(2026-08-14 감사).** 이 항목은 처음에 *"앱이 `Ctrl+S`를 액션에 걸 수
   > 없습니다"*였는데 **틀렸습니다.** 예약 검사(`findConflict`)를 부르는 자리는
   > 녹음기 하나뿐이고, `defaultCombo`·`overrides`·`storage`로 들어오는 조합의
   > 관문인 `bindingOf`는 `unbindableReason`만 봅니다 — 거기에 `KeyS`는 없습니다.
   > **앱이 코드로 넣으면 그대로 걸립니다.** 다만 날짜 필드에 포커스가 있을 때는
   > 피커가 먼저 `preventDefault`를 부르므로 규칙 1에 걸려 액션이 안 뜹니다.
   > 즉 **깨지는 것은 녹음 경로뿐**입니다. 두 관문의 차이는 설계 스펙 §5.2에
   > 표로 적어 뒀습니다.

> 1~3은 2b(PR #52), 4~5는 그 뒤 라운드. 넷·다섯은 **일부러 필수**입니다 — 선택으로 두면
> 부분 override가 한국어를 남겨 스크린리더로 샙니다. 비용을 재 보니 **소비자 0곳**
> (`budget`은 `DateWheelPicker`를 12개 파일에서 쓰는데 `labels=`를 한 건도 안 넘기고,
> `homa_gwangju`는 안 씁니다).

### 눈으로 볼 것

- 🔴 **`wheelRowsPerSide` 기본값이 1입니다** — 지금까지 2였으므로 **핀을 올리는 소비자는
  다른 휠을 봅니다.** `PRINCIPLES §14`("새 축의 기본값은 지금과 같음")를 **의도적으로 깬
  자리**이고 오너 결정입니다.
- 팝오버가 **5·6열에서 넓어집니다**(3열은 무변화).
- 라벨과 컨트롤의 세로 정렬이 바뀝니다(위 "더해진 것").
- 팝오버 안내 문구가 바뀝니다 — `가운데 두 번 탭하면 초기화`가 들어갔습니다.

---

**날짜 피커 — 필드의 복사·붙여넣기·되돌리기·저장, 그리고 손가락 탭**(오너 리포트
2026-08-14). 이 컨트롤의 필드는 `<span>` + `onKeyDown`이라 고를 텍스트가 없어서
네이티브 `<input type="date">`가 공짜로 갖는 것들이 **전부 안 됐습니다.**

### 더해진 것 (날짜 피커)

- **`Ctrl+C`** — 화면에 보이는 그대로 클립보드에 씁니다(`2026. 07. 12.`). 치던
  버퍼는 먼저 확정합니다 — 안 그러면 본 것과 붙여넣은 것이 다릅니다.
- **`Ctrl+V`** — 클립보드를 읽어 값으로 만듭니다. 구분자를 해석하지 않고 **숫자
  덩어리의 개수와 순서만** 보므로 `2026-8-14`·`2026/08/14`·`20260814`·
  `2026년 8월 14일`이 전부 같은 날로 읽힙니다. **읽을 수 없으면 아무것도 하지
  않습니다**(값을 지우지 않습니다 — 실패가 값을 지우면 되돌릴 것도 없이 잃습니다).
- **`Ctrl+Z`** — 마지막 **조작** 하나를 되돌립니다. 한 항목이 한 조작이라, 서른 칸
  끈 드래그도 휠 한 무리도 **한 번에** 되돌아옵니다. 다시 하기는 없습니다.
- **`Ctrl+S`** — `완료` 버튼과 같습니다. **팝오버가 열려 있을 때만** 먹습니다.
- **`instantModel.parsePasted(text, fields, hour?)`** — 위 붙여넣기를 뒷받침하는 값
  모델의 순수 함수. `WheelModel` 계약에도 들어갔습니다.

### 바뀐 것 (날짜 피커)

- **굵은 포인터(손가락)에서는 세그먼트를 눌러도 그 세그먼트를 고르지 않고 그냥
  여닫습니다.** 세그먼트를 고르는 것은 키보드로 이어서 편집할 사람을 위한 준비인데
  폰에는 그 다음 동작이 없고, 목표가 좁아 오조작만 늡니다. **가는 포인터(마우스)의
  동작은 글자 하나도 안 바뀝니다.**

### ⚠️ 소비자가 알아야 할 것

- **`KIT_RESERVED`에 `Ctrl+S`가 들어갑니다 — 설정 화면에서 `Ctrl+S`를 녹음할 수
  없습니다.** `Ctrl+C`·`Ctrl+V`·`Ctrl+Z`도 함께 등록되지만 그 셋은 이미
  `unbindableReason`의 `native-edit`로 막혀 있던 것이라 **새로 잃는 것은 없습니다.**
  `Ctrl+S` 하나가 새 손실이고, 예약 비교가 `Shift`·`Alt`·`Meta`를 접으므로
  `Ctrl+Shift+S`·`Cmd+S`도 함께 막힙니다.
  ⚠️ **앱이 `defaultCombo`·`overrides`로 코드에 직접 넣는 것은 막히지 않습니다** —
  `bindingOf`는 예약 목록이 아니라 `unbindableReason`만 봅니다(2026-08-14 정정,
  설계 스펙 §5.2의 표). 넣으면 걸리고, 날짜 필드에 포커스가 있을 때만 안 뜹니다.
- **`Ctrl+S`는 브라우저의 "페이지 저장"을 실제로 가져갑니다.** `Ctrl+;`는 실기기로
  확인하고 뺏은 것이지만 이건 **아직 확인하지 못했습니다** — 폰에는 `Ctrl` 키가 없어
  이 저장소가 쓰던 확인 방법을 그대로 쓸 수 없습니다. 그래서 범위를 좁혔습니다:
  팝오버가 닫혀 있으면 `Ctrl+S`는 브라우저 몫입니다.
- **클립보드는 `navigator.clipboard`를 씁니다** — 보안 컨텍스트(HTTPS·localhost)가
  아니면 없고, 권한이 거절되면 **조용히 아무것도 하지 않습니다.** 폰에는 `Ctrl` 키가
  없어 이 경로는 데스크톱 전용입니다.

### 문서

- **README가 아직 커밋 SHA로 핀을 걸라고 가르치고 있었습니다.** 이 CHANGELOG는 맨 위에서
  "버전은 태그로 답니다"라고 말하고 `CUSTOMIZING.md`도 태그로 안내하는데, README의
  "고칠 일이 생기면" 절만 `#<새-커밋-SHA>`에 머물러 있었습니다 — 셋 다 `files`로
  소비자에게 설치되는 문서라 **서로 다른 말을 하고 있었습니다.** 이제 태그
  (`#v<최신-태그>`)와 범위(`#semver:^0.X.0`) 둘을 안내합니다.
  ⚠️ 범위 형태를 `cmd.exe`에서 따옴표 없이 쓰면 `^`가 **말없이 먹혀** `#semver:0.6.0`이
  되고, 그건 **유효한 지정자라 에러 없이 한 버전에 고정**됩니다(실측) — 그래서 예시에
  따옴표가 있습니다.

### 저장소 검사

- **`npm run typecheck:src`** — `src`만 컴파일합니다(`tsconfig.src.json`). 이 킷은
  `src`를 소스 그대로 배포하므로 소비자의 tsc가 보는 것은 `src`뿐인데, 저장소의
  `tsconfig.json`은 `tests`·`demo`까지 봅니다. 그 둘이 끌어오는 타입이 `src`의 구멍을
  덮어 "여기서는 초록, 소비자에게서는 빨강"이 만들어집니다.

---

**단축키 모듈** — 오너의 실사용 개선 5개 중 마지막 항목(설계 스펙:
`docs/design/2026-08-12-shortcuts-design.md`). **순수 추가이고 옵트인**이라
`ShortcutProvider`를 렌더하지 않는 소비 프로젝트는 화면도 리스너도 그대로입니다.

### 더해진 것

- **`ShortcutProvider` · `useShortcutRegistry`** — 단축키 등록·트리거·충돌 검사를
  맡는 컨텍스트. 리스너는 `document`에 **하나**, 버블 단계, effect 안에만 삽니다
  (안 쓰면 0개).
- **`ShortcutSettings`** — 사용자가 조합을 다시 녹음하거나 지우는 설정 UI. 등록
  중인 조합이 킷 컴포넌트나 다른 액션과 겹치면 등록을 막고 이유를 보여 줍니다.
- **등록할 수 없는 조합**(설계 스펙 §6.2) — `Escape`·`Tab`에 더해, **맨 `Enter`·맨
  `Space`**(포커스한 버튼·링크를 누르는 키)와 **`Ctrl`/`Cmd` + `C`·`V`·`X`·`Z`·`Y`**
  (브라우저의 복사·붙여넣기·되돌리기)를 막습니다. 녹음기가 이유를 보여 주고,
  `defaultCombo`·`overrides`로 코드에서 넣어도 똑같이 무시됩니다 — 관문이 한 곳
  (`unbindableReason`)이라 두 경로가 갈리지 않습니다. **`Ctrl+Enter`처럼 수식어가
  붙은 활성화 키는 그대로 걸 수 있습니다.**
  `Ctrl+A`는 **걸 수 있고**, 대신 "텍스트 입력 안에서는 뜨지 않는다"고 알려 줍니다.
- **`sidebarToggleAction(onFire, options?)` · `SIDEBAR_TOGGLE_ID`** — 킷이 기본
  제공하는 유일한 액션. **핸들러는 앱 것입니다** — `Sidebar`가 controlled라 킷이
  쥐는 것은 안정적인 `id`뿐입니다. `options`로 `label`과 `defaultCombo`를 넘길 수
  있고, **킷이 정하는 기본값은 `null`**이라 조합은 앱이 정해야 합니다.
- **`parseCombo` · `formatCombo` · `normalizeCombo` · `findConflict` ·
  `shouldTrigger` · `BARE_KEY_SCOPE_ATTR` · `KIT_RESERVED` · `displayCombo`** —
  조합 표기·매칭·충돌 검사의 나머지 공개 API.
- **`createShortcutStorage(options?)` · `ShortcutStorage` · `ShortcutBindings` ·
  `ShortcutBackup` · `ParsedShortcutBindings`**(Task 7, 저장) — 단축키 덮어쓰기를
  `localStorage`(기본 키 `shortcutBindings`)에 **버전 붙은 봉투**로 저장합니다.
  `write`/`read`는 절대 던지지 않고 `boolean`/`{}`로 실패를 알리고, 빈 맵을 쓰면
  저장된 키 자체를 지웁니다. `null`(사용자가 지움)과 키 없음(기본값을 씀)은 왕복
  에서 섞이지 않습니다. `serialize`/`parse`는 `ThemeColorEditor`의 팔레트와 같은
  모양(버전 붙은 봉투)입니다 — 다만 킷은 액션 id를 모르므로(§3.3), `parse`가
  버리는 것은 **형식에 안 맞는 값**뿐이고 그 값의 id를 `dropped`에 남깁니다.
- **`ShortcutProvider`의 `storage` prop · `ShortcutRegistry.setBinding`**(Task 7) —
  `overrides` 대신 `storage`를 넘기면 킷이 uncontrolled로 저장소를 직접 읽고 쓰고,
  다른 탭의 변경도 구독합니다. `overrides`를 넘기면(controlled) `storage`는 완전히
  무시되고 킷은 저장소를 전혀 안 건드립니다 — 어느 쪽도 안 넘기면 지금까지와 같이
  `defaultCombo`만 쓰고 저장소 접근은 0입니다.
- **`ShortcutSettings`의 `onChange`가 선택이 됨**(Task 7) — 없으면
  `registry.setBinding`으로 커밋합니다(`ShortcutProvider`가 `storage`를 받았을 때만
  실제로 저장됩니다). 둘 다 없으면 저장할 곳이 없다는 뜻이라 조용히 넘어가지 않고
  콘솔에 경고를 남깁니다(인스턴스당 한 번, 개발 모드에서만). **저장소가 있는데
  막힌 경우**(프라이빗 모드·용량 초과)는 다른 사건으로 다룹니다 — 콘솔 경고 대신
  화면에 안내가 뜹니다.
- **`ShortcutRegistry.canPersist`** — `setBinding`이 실제로 저장할 자리가 있는지
  미리 알려줍니다(uncontrolled고 `storage`가 있을 때만 참). `setBinding`의
  반환값(`false`)만으로는 "저장할 곳이 없다"와 "저장소가 막혔다"를 못 가리므로
  `ShortcutSettings`가 이걸로 둘을 구분합니다.
- **`ShortcutRegistry.restoreBindings(bindings)`** — 백업에서 파싱한 맵을 통째로
  커밋합니다. `setBinding`을 항목마다 루프로 부르는 것과 달리 `storage.write`를
  한 번만 부르고, `storage.write`를 직접 부르는 것과 달리 이 탭의 화면 상태도 함께
  갱신합니다(`subscribe`는 다른 탭의 변경만 받으므로). `setBinding`과 같은 경계 —
  controlled이거나 `storage`가 없으면 아무것도 안 합니다.
- **데모에 조합 예시**(`demo/main.tsx`) — 사이드바 접기/펴기가 `Ctrl + \`에 걸려
  있고, 앱이 `defaultCombo`로 그 값을 준 코드가 그대로 보입니다. 이제 저장소도
  킷이 맡습니다(`storage={createShortcutStorage()}`) — **새로고침해도 재녹음한
  조합이 남고**, 다른 탭에서 바꿔도 따라옵니다. `.workspace`에
  `data-kkqq-shortcut-scope`를 붙여 맨 키 허용 구역의 예시도 하나 보여 줍니다.

### 고친 것

- **한글 IME로 조합하는 중에 수식어 단축키가 두 번 돌던 것**(규칙 8, 설계 스펙 §2.5).
  실기기 실측: 조합 중에 `Ctrl+\`를 **한 번** 누르면 keydown이 **둘** 옵니다(같은
  `code`·같은 수식어, 하나는 `keyCode=229`). 둘 다 발사돼서 사이드바 토글은
  **접혔다 펴져 아무 일도 안 한 것처럼** 보였고, 페이지 이동이었다면 두 번
  이동했을 자리입니다. 이제 `isComposing`이거나 `keyCode === 229`면 트리거하지
  않습니다 — 그러면 규칙 6의 `preventDefault`도 IME 자신의 이벤트에는 안 걸려서,
  **조합 중인 커서가 튀던 것도 같이 없어질 것으로 봅니다**(그 인과는 재확인 대상).
  ⚠️ 맨 키는 원래부터 규칙 4가 막고 있었습니다 — 이 고침은 **수식어 조합**만 바꿉니다.
- **허용 구역 안의 `<select>`·라디오·체크박스·슬라이더가 맨 키 단축키에 먹히던 것**
  (규칙 9, 설계 스펙 §2.6). 실브라우저 실측 — `<select>`에서 `b`를 누르면 타입어헤드가
  `apple → banana`로 가는데, 그때 `defaultPrevented`는 **거짓**이라 규칙 1이 못 막고
  규칙 6의 `preventDefault`가 **그 이동을 죽였습니다**(라디오 `ArrowDown`, range
  `ArrowRight`도 같음). 이제 `<select>`·`<input>`에 포커스가 있으면 맨 키를 양보합니다.
  ⚠️ **`<button>`·링크는 일부러 안 막습니다** — 카드 그리드에서 `j`/`k`를 쓰라고 만든
  기능이라서입니다. 대신 맨 `Enter`·`Space`를 바인딩하면 구역 안에서 활성화가 죽습니다.
- `ShortcutSettings`의 녹음·지우기 버튼이 브라우저 기본 모양으로 뜨던 것 —
  킷의 `.secondary-button` 클래스를 씁니다.

### 눈으로 볼 것

- **안 쓰면 아무 화면도 안 바뀝니다.** `ShortcutProvider`를 렌더하지 않는 기존
  프로젝트는 이번 릴리스로 달라지는 것이 없습니다.
- **`defaultCombo`가 없는 액션은 `overrides`·`storage`를 안 주면 조용히 아무 키에도
  안 걸립니다.** 데모처럼 조합을 앱이 정해야 하고, 안 정하면 "켰는데 안 되는데요"가
  됩니다 — 문서가 아니라 설계가 그렇게 되어 있습니다.
- **맨 키 단축키는 `data-kkqq-shortcut-scope`가 붙은 컨테이너 안에서만** 포커스가
  `<body>`가 아니어도 트리거됩니다. 수식어(Ctrl·Alt·Meta) 조합은 이 표식과
  무관하게 어디서나 트리거됩니다. 그 구역 안이라도 **텍스트 입력과 폼 컨트롤
  (`<select>`·`<input>`)에 포커스가 있으면 맨 키는 그 컨트롤 것입니다.**
- **데모의 단축키 패널이 이제 새로고침해도 조합을 기억합니다** — 전에는 `useState`
  뿐이라 새로고침하면 초기화됐습니다.

---

## v0.7.0 — 2026-08-13

[전체 diff: v0.6.0...v0.7.0](https://github.com/kkqq9320/kkqq-ui-basic-kit/compare/v0.6.0...v0.7.0)

**루트에서 내보내는 것은 하나도 안 바뀝니다** — `src/index.ts`가 `v0.6.0`과 **글자 하나까지
같습니다**(실측). 그런데도 minor인 이유는 둘입니다: **편집기에서 색상 카드 두 장이
사라지고**(사용자가 바로 봅니다), **내부 모듈 하나의 파일 경로가 없어집니다**(아래
"눈으로 볼 것"). v0.3.0~v0.6.0과 같은 판단입니다 — 규칙이 타입만 말할 때는 **소비자가
무엇을 보게 되는가**로 판단합니다.

### 고친 것

- **색상 편집기에서 `--deep`·`--gold` 카드가 사라집니다.** 킷 CSS가 그 둘을 `var()`로
  **한 번도 쓰지 않아서**, 사용자가 그 색을 고쳐도 화면이 아무것도 안 바뀌었습니다 —
  고장으로 보이는 자리였습니다. **토큰 자체는 `tokens.css`에 그대로 있습니다**:
  앱이 `var(--deep)`로 쓸 수 있고, 편집까지 하고 싶으면 자기 `groups`(또는
  `createThemePalette`)에 그 항목을 넣으면 예전처럼 카드가 나옵니다.
- **`var(--토큰, 폴백)`의 폴백 둘이 토큰 기본값과 달랐습니다** — `--panel-min`은
  토큰 `400px`에 폴백 `640px`, `--color-card-min`은 `240px`에 `360px`였습니다. 폴백은
  **`tokens.css`를 안 싣는 소비자에게만** 보이는 값이라(README가 폰트를 빼려고 개별
  import를 안내하므로 실제로 있는 경로입니다) 아무도 안 보고 있었습니다. 이제 크기 축
  여덟 개 전부를 테스트가 선언과 대조합니다.

### 눈으로 볼 것

- **편집기의 색상 카드가 두 장 줄어듭니다.** 그 둘을 이미 쓰던 앱은 위 방법으로 자기
  목록에 넣으세요 — ⚠️ **업그레이드와 같이 넣어야 합니다.** 사용자가 예전에 고른
  `--deep`·`--gold` 값은 **올리는 것만으로는 안 지워지지만, 사용자가 다른 색을 하나라도
  고치는 순간 함께 사라집니다** — 저장이 그 테마의 맵을 **통째로 교체**하기 때문입니다
  (실측: 저장소에 `--deep`이 있는 채로 강조색을 한 번 고치면 `{"--accent":"#ff8a3d"}`만
  남습니다). 목록에 넣어 두면 계속 살아 있습니다.
- **`--color-card-min`은 `240px` 그대로입니다.** 주석만 360이던 시절을 설명하고
  있었을 뿐이라, 값과 화면은 안 바뀝니다(목록 폭 1014px·gap 8px 기준 계산상 4열).
- ⚠️ **`src/dateWheelTyping.ts`가 없어졌습니다.** 날짜 피커의 값 모델이
  **`src/model/instant.ts`** 한 곳으로 모였습니다(내부 정리). `package.json`의 `exports`가
  `./src/*`를 열어 두므로 **그 파일을 직접 import 하던 코드는 깨집니다.** 루트에서 쓰는
  정상 경로는 그대로입니다 — `DateWheelPicker`가 `todayIn`과 `DateWheelUnit`을 그대로
  재수출하고, `import { … } from "kkqq-ui-basic-kit"`는 아무것도 안 바뀝니다.
- **날짜 피커의 화면·동작은 그대로입니다.** 값 해석·범위 판정이 컴포넌트에서 모델로
  옮겨갔을 뿐이고, 그 이동을 검사가 각각 지킵니다.

---

## v0.6.0 — 2026-08-12

[전체 diff: v0.5.0...v0.6.0](https://github.com/kkqq9320/kkqq-ui-basic-kit/compare/v0.5.0...v0.6.0)

**타입은 깨지지 않습니다** — 새 함수·새 prop은 전부 추가이고, 안 쓰면 오늘과 같습니다.
그런데도 minor인 이유는 **동작이 하나 바뀌기 때문**입니다: `writeTokenOverrides`가
저장소가 막혔을 때 **던지던 것을 이제 `false`로 돌려줍니다**(`void` → `boolean`).
호출부는 안 깨지지만(실측), 그 예외를 `try/catch`로 받아 "저장 실패"를 알리던 앱은
**그 catch가 이제 안 돕니다** — 반환값을 보도록 고쳐야 합니다. v0.3.0~v0.5.0과 같은
판단입니다: 규칙이 타입만 말할 때는 **소비자가 무엇을 보게 되는가**로 판단합니다.

### 더해진 것

- **`createThemePalette`** — 앱의 토큰 목록을 한 번만 묶는 객체. 읽기·쓰기·적용·직렬화가
  전부 이 목록에 묶여, **목록을 넘길 자리가 없어집니다.** 지금까지는 호출부마다 목록을
  다시 넘겨야 했고 하나만 빠뜨리면 앱이 신설한 색이 말없이 사라졌습니다.
- **백업 형식** — `serialize`/`parse`/`applyBackup`. `parse`는 부작용이 없고 **버린
  토큰 이름을 돌려줍니다.** 봉투가 아니면 `null`이라 "빈 백업"과 구분됩니다.
  `applyBackup(backup, theme)`은 **저장은 두 테마 다, 화면 적용은 넘긴 테마만** 합니다 —
  `:root`가 하나뿐이라 둘 다 적용하면 다크가 라이트를 덮습니다(실측).
- **`ThemeColorEditor`의 `palette`·`overrides`·`onCommit`** — `overrides`를 넘기면
  편집기가 **저장하지 않고** 적용과 알림만 합니다(앱이 서버에 둘 때). `onCommit`은
  손을 둔 뒤 한 번 불립니다.

### 고친 것

- **저장소가 막힌 환경에서 색을 바꾸면 편집기가 터지던 것.** `readTokenOverrides`는
  `try/catch`인데 `writeTokenOverrides`만 안 감싸져 있었습니다. 이제 예외 대신
  성공 여부(`boolean`)를 돌려줍니다.

### 눈으로 볼 것

- **아무것도 안 하면 화면이 안 바뀝니다** — 새 prop은 전부 선택이고 안 넘기면 지금과
  같습니다. `writeTokenOverrides`의 반환 타입이 바뀌었지만 호출부는 안 깨집니다(실측).

---

## v0.5.0 — 2026-08-12

[전체 diff: v0.4.1...v0.5.0](https://github.com/kkqq9320/kkqq-ui-basic-kit/compare/v0.4.1...v0.5.0)

**타입은 깨지지 않습니다** — 새 prop은 선택이고 기본값이 있습니다. 규칙을 엄격히 읽으면
patch입니다. 그런데도 minor인 이유는 **핀을 올린 소비자가 다른 화면을 보기 때문**입니다:
페이지 끝 여백이 **상황에 따라 달라집니다**(내용이 화면에 들어가면 남는 자리만, 이미
넘치면 지금까지와 같이). v0.3.0·v0.4.0과 같은 판단입니다 — **규칙이 타입만 말할 때는
소비자가 무엇을 보게 되는가로 판단합니다.** 태그로 거는 방식이라 minor의 비용은 0이고,
과소 신고는 누군가에게 놀람을 남깁니다.

**기본 동작이 바뀝니다.** 이 저장소 규칙은 "새 축은 기본값이 지금과 같음"인데 이번은
예외입니다 — 소유자 결정입니다(그러지 않으면 아무도 이걸 받지 않습니다). 예전 동작으로
돌아가는 문을 함께 냈습니다: `AppShell`의 `trailingSpace="fixed"`.

### 더해진 것

- **`AppShell`이 `trailingSpace`를 받습니다** (`"adaptive"` 기본 / `"fixed"`).
  `"fixed"`가 예전 동작이고, 그때는 킷이 **재지도 관찰하지도 않습니다.**
- **`--workspace-space-bottom` 토큰**(기본 `80px`) — 끝 여백의 **최대** 크기.
  동작은 프롭, 크기는 토큰입니다.
- **페이지 스크롤바를 감추는 방법을 `CUSTOMIZING.md`에 적었습니다.** 킷 기본은
  **그리는 쪽**이고, 감추는 것은 앱의 선택입니다.

### 고친 것

- **페이지 끝 여백이 스스로 스크롤을 만들던 것.** 여백이 `padding-bottom`이라
  **진짜 공간**이었고, 내용이 화면을 거의 채우면 그 여백만으로 문서가 창을 넘었습니다 —
  실측(실제 크롬): 내용이 **1256.8px**로 끝나고 창이 **1270px**인데 문서가 **1336.8px**,
  즉 **볼 것이 없는 스크롤바가 67px** 섰습니다.

  이제 끝 여백은 **남는 자리를 쓰는 스페이서**입니다. 문서 높이를 셀 때 0으로 잡히고,
  자리가 남으면 최대 `--workspace-space-bottom`까지 자랍니다. 그리고 **내용이 이미
  넘치는 페이지에서는 여백이 그대로 남습니다** — `AppShell`이 내용만의 높이를 재서
  가릅니다. **CSS만으로는 안 됩니다**: 여백이 그 자신이 재는 넘침을 만드는 순환이라
  "항상 그만큼"과 "절대 스크롤을 안 만듦"이 배타적입니다.

- **끝 여백을 두 번 세던 것.** 위 패딩에 마지막 블록의 `margin-bottom`(요약 그리드
  22px, 패널 그리드·패널 20px)이 **더해지고 있었습니다.** 실측으로 마지막 요소 아래
  빈 공간이 세 페이지 전부 **100px**이었습니다.

### 눈으로 볼 것

- **내용이 화면에 들어가는 페이지에서 스크롤바가 사라집니다.** 이 변경의 목적입니다.
- **내용이 넘치는 페이지는 그대로입니다** — 끝까지 내리면 마지막 요소 아래 여백이
  예전처럼 있습니다(실측: 세 데모 페이지 전부 80px).
- ⚠️ **앱이 페이지 끝 여백에 무언가를 기대고 있으면**(직접 띄운 고정 바 자리 등)
  `trailingSpace="fixed"`로 두세요. 안 그러면 내용이 짧은 페이지에서 그 여백이
  남는 자리만큼으로 줄어듭니다.
- ⚠️ **모바일은 아무것도 바뀌지 않습니다.** 그쪽 하단 패딩은 고정 바 자리와 키보드
  보정이 읽는 값이라 이 장치가 아예 꺼져 있습니다.

---

## v0.4.1 — 2026-08-12

[전체 diff: v0.4.0...v0.4.1](https://github.com/kkqq9320/kkqq-ui-basic-kit/compare/v0.4.0...v0.4.1)

**타입도 런타임도 CSS 값도 바뀐 것이 없습니다. 문서만 바뀝니다.** 그래서 patch입니다.

v0.4.0을 minor로 단 것은 `PRINCIPLES.md`의 규칙 **내용이 다른 파일로 옮겨가**
읽던 사람이 찾아가야 했기 때문입니다. 이번에는 **옮겨간 것이 없습니다** — 소유자가
철회한 경로(사본으로 복사해 쓰기)의 안내가 빠졌고, 나머지는 틀렸던 것을 고치거나
없던 것을 더한 것입니다. **이미 사본으로 도는 프로젝트도 그대로 계속 동작합니다**
(설치 방식이 바뀌지 않으므로). 아래 "눈으로 볼 것"만 확인하세요.

### 고친 것

- **폰트 바꾸는 방법이 틀려 있었습니다.** `README.md`가 "`css/fonts.css`를 통째로
  지우라"고 안내했는데, (1) 킷은 `node_modules`에서 오므로 거기서 지운 것은 다음
  `npm install`에 사라지고 (2) `css/index.css`가 그 파일을 `@import` 하므로 지우면
  그 경로가 깨집니다. 세 파일이 서로 다른 말을 하고 있던 것도 맞췄습니다
  (`README.md`는 "지워라", `index.css`는 "줄만 빼라", `fonts.css`는 "교체하라").
  **맞는 방법은 지우는 것이 아니라 import 하지 않는 것입니다** — `index.css` 대신
  개별 파일을 import 하면서 `fonts.css`만 빼고, 자기 `:root`에서
  `--font-family-base`를 덮어씁니다. 실측(v0.4.0을 빈 프로젝트에 설치해
  `vite build`): `index.css` 하나를 쓰면 안 쓰는 폰트라도 **2,057,688바이트**
  woff2가 번들에 실리고, 개별 import로 빼면 **나오지 않습니다.**
- **폰트를 바꿀 때 다시 재야 하는 것 둘을 README에 적었습니다.** `css/fonts.css`에는
  `@font-face` 하나뿐이지만 **파일 밖에 두 가지가 이 폰트에 기대고 있고**, 그 사실이
  소스 주석에만 있어 정작 폰트를 바꾸는 사람이 볼 자리에는 없었습니다.
  (1) `tokens.css`의 `font-synthesis: none` — 킷이 쓰는 굵기 `45·400·600·700·800`의
  글리프가 실제로 없으면 **경고 없이 그냥 안 굵어집니다**(45는 정적 폰트에 대개 없음).
  (2) `DateWheelPicker`의 빈 자리 채움 문자 **U+2012**는 프리텐다드에서 tabular 숫자와
  폭이 ±0이라 고른 것이고, `tabular-nums`는 숫자 글리프에만 적용되므로 **그 등폭은
  CSS가 아니라 폰트의 성질**입니다. 눈으로 보는 법(연도를 한 자리만 치고 뒤 세그먼트가
  밀리는지)까지 적었습니다.
- **`README.md`·`CUSTOMIZING.md`가 더 이상 vendoring(사본으로 복사해 쓰기)을
  정상 경로로 가르치지 않습니다.** 소유자가 "vendoring은 더 이상 문서화된
  경로가 아니다"로 정했고, 스킬 저장소(`kkqq-skills`)는 이미 그렇게 바뀌어
  있었습니다 — 그런데 이 두 문서(둘 다 `package.json`의 `files`에 들어 있어
  설치본에 함께 실립니다)는 반대를 가르치고 있었습니다. `README.md`의
  "복사해서 써야 한다면" 절 전체를 지웠습니다(로컬 절대경로가 박힌 `cp -r`
  복사 명령, `KIT-VERSION` 절차, `diff -r` 동기화, `file:vendor/` 의존성 예시).
  설치 일반에 대한 검증 기록(Vite 확인, 빌드 단계 없음)은 `## 설치` 절로
  옮겨 살렸습니다.
- **README.md에 남아 있던 거짓 진술 하나를 고쳤습니다.** "벤더링한 프로젝트에서는
  그 사본의 `vendor/kkqq-ui-basic-kit/PRINCIPLES.md`가 읽힌다"던 문장 — 현재 스킬의
  탐색 순서는 `node_modules/kkqq-ui-basic-kit/` 다음 공개 raw URL 둘뿐이고,
  `vendor/` 경로는 찾지 않습니다 (`kkqq-skills` 저장소의 `kkqq-ui-basic-kit`·
  `kkqq-layout-principles` SKILL.md로 확인).

  > ⚠️ **두 저장소를 섞어 읽지 마세요.** 마켓플레이스 저장소
  > (`kkqq9320/claude-skills`)에 대한 "비공개" 표시는 **참이라 그대로 둡니다**
  > — 실측하면 익명 API가 404이고, 대조군인 이 킷 저장소는 200입니다.
  > 한 프로젝트가 2.3MB 사본을 커밋하게 만든 거짓은 그 문장이 아니라
  > **"이 킷 저장소가 비공개"라던 스킬 쪽 문장**이었습니다. 이 킷은 공개입니다.
- Claude 스킬 절에 두 번째 스킬(`kkqq-layout-principles` — 킷을 설치할 수
  없는 곳에서 `LAYOUT-PRINCIPLES.md`만 필요할 때)을 추가했습니다.
- "소비 앱은 이제 `vendor/`로 복사해 쓴다"는 문장을 고쳤습니다 — 실측
  결과 소비 프로젝트 하나는 git 의존성(`github:...#<SHA>`)이고, `file:vendor/`를
  쓰는 것은 다른 하나뿐입니다.
- 검증 상태의 테스트 개수 스냅샷을 갱신했습니다(510개·2026-08-10 →
  758개·2026-08-12).
- **킷을 특정 앱에서 "뽑아낸 것"으로 소개하던 문장들을 걷어냈습니다.** 킷은
  독립적으로 서는 물건이고, 그걸 쓰는 프로젝트 이름은 킷 문서가 알 필요가
  없습니다. `README.md`의 소개 문장·SHA 고정 설명·`node_modules` 정션 사고
  기록·클래스 이름 대응 표 서문·"## 출처" 절, `PRINCIPLES.md`의 원본 출처
  줄을 고쳤습니다. `node_modules` 정션 사고 기록과 클래스 이름 개명 이력
  자체는 이 저장소에 유효한 사실이라 이름만 중립화하고 남겨 뒀습니다.

---

## v0.4.0 — 2026-08-12

[전체 diff: v0.3.0...v0.4.0](https://github.com/kkqq9320/kkqq-ui-basic-kit/compare/v0.3.0...v0.4.0)

**타입도 런타임도 CSS 값도 바뀐 것이 없습니다.** 규칙을 엄격히 읽으면 patch입니다.
그런데도 minor인 이유는 **문서 계약이 바뀌기 때문**입니다 — `PRINCIPLES.md`의 다섯 절이
내용을 잃고 다른 파일을 가리키게 됐고, 예전 방식으로 복사해 둔 프로젝트는 그 링크가
끊깁니다(아래 "눈으로 볼 것").

### 더해진 것

- **`LAYOUT-PRINCIPLES.md`** — 배치·그리드·정렬 규칙을 킷 없이도 서는 문서로 뽑았습니다.
  React도 이 킷도 전제하지 않으므로, 남의 프레임워크 위에 얹는 화면에서도 그대로
  씁니다. **이 태그로 핀을 올리면 설치본에 함께 들어옵니다**(`files`·`exports`).

### 눈으로 볼 것

- **`PRINCIPLES.md`의 다섯 절이 줄었습니다.** §2·§7·§8은 일반 규칙을 새 문서로 보내고
  킷 고유의 값만 남겼고, §13·§14는 요약 한 줄과 링크가 됐습니다. **읽던 규칙이
  사라진 것이 아니라 옮겨간 것입니다** — 문서 맨 위의 "먼저 읽을 것"을 보세요.
- `css/tokens.css`의 주석에서 일반 그리드 사실이 빠졌습니다. 각 토큰이 왜 그 값인지는
  그대로 있습니다. **값은 하나도 바뀌지 않았습니다.**
- ⚠️ **파일을 복사해서 쓰는 프로젝트는 복사 목록에 `LAYOUT-PRINCIPLES.md`를 넣으세요.**
  안 넣으면 사본의 `PRINCIPLES.md`에 **끊어진 링크가 다섯 개** 생깁니다(§2·§7·§8·§13·§14).
  의존성으로 거는 정상 경로에서는 해당 없습니다 — `files`에 이미 들어 있습니다.

---

## v0.3.0 — 2026-08-11

[전체 diff: v0.2.0...v0.3.0](https://github.com/kkqq9320/kkqq-ui-basic-kit/compare/v0.2.0...v0.3.0)

**타입이 깨지는 것은 없습니다** — 새로 생긴 prop은 전부 선택입니다. 그런데도 patch가
아니라 minor인 이유는 **핀을 올리면 넓은 화면의 배치가 실제로 바뀌기 때문**입니다.
2560에서 요약 카드가 533px → 260px, 패널이 2102px → 1077px로 다시 깔립니다.
좁은 화면(1440 이하)은 거의 그대로입니다. 아래 "눈으로 볼 것"을 먼저 보세요.

### 더해진 것 (깨지는 것 없음 — 둘 다 선택 prop)

- **`AutoGrowTextarea`가 `disabled`를 받습니다.** `Select`·`DateWheelPicker`에는 있는데
  이것만 없어서, 폼 전체를 잠그면 **메모 칸 하나만 살아 있는 "반쯤 잠긴 폼"**이 됐습니다.
- **`AutoGrowTextarea`가 `id`를 받습니다.** 라벨을 컨트롤 바깥에 두고 `<label htmlFor>`로
  묶는 배치가 가능해집니다. 바깥 라벨과 `ariaLabel`을 같이 넘기면 `aria-label`이 이기므로
  **둘 중 하나만** 쓰세요.

### 고친 것

- **macOS에서 `DateWheelPicker`가 마우스로 연 뒤 키보드로 조작되지 않던 것.** 맥
  브라우저는 `mousedown`의 기본 동작으로 버튼에서 포커스를 **걷어냅니다.** 이 컨트롤은
  키 핸들러를 **트리거 하나에만** 걸고(스펙 §6.2) 브라우저가 포커스를 줄 것을 전제하고
  있어서, 맥에서는 **방향키·숫자·`Enter`·`Cmd+;`가 전부 안 먹었습니다.** 이제 트리거가
  `click`에서 포커스를 직접 가져옵니다. 근거와 실측은 설계 스펙 §6.4에 있습니다.

  **덤: `Cmd+;`(오늘로 설정)가 맥에서 동작하는 것이 이걸로 확인됐습니다.** 그동안
  "macOS `Cmd+;`는 미확인"으로 문서에 남아 있었는데, 막고 있던 것은 조합이 아니라
  포커스였습니다. README·PRINCIPLES·설계 스펙에서 그 미검증 표시를 걷어냈습니다.

- **`Select`는 트리거를 다시 눌러 닫을 때 포커스가 사라지던 것**(맥 한정). 여는 경로는
  원래 멀쩡했습니다 — `Select`는 열릴 때 포커스를 메뉴 안 활성 옵션으로 **명시적으로**
  옮기기 때문입니다. 닫는 클릭만 아무 데도 포커스를 두지 않았습니다.

  **Windows·안드로이드 사용자에게는 보이는 변화가 없습니다**(그쪽은 원래 브라우저가
  포커스를 줬습니다). 맥 사용자에게는 지금까지 안 되던 것이 됩니다.

### 넓은 화면 배치 (새로 생긴 것)

- **`PanelGrid`·`FieldGrid` 신설, `SummaryGrid`는 반응형으로.** 넓은 화면에서 카드·패널이
  화면을 등분해 가져가던 것을 고쳤습니다 — 2560에서 요약 카드가 533px, 패널이 2102px,
  그 안 메모 칸이 2056px이었습니다. 세 통이 모두 `min`/`max`/`justify`를 토큰과 prop으로
  받습니다. **기본값은 지금까지와 같아서 아무것도 안 하면 화면이 안 바뀝니다.**
- **`PanelGrid`의 `stretch`** — 같은 행 패널의 높이를 맞춥니다. 어느 패널이 같이 설지·
  순서·높이 맞춤은 전부 앱이 정합니다(PRINCIPLES §14).
- **내보내는 컴포넌트 스물이 전부 `className`을 받습니다**(전에는 열 개만). 자주 쓰는
  것만 prop으로 열고 나머지는 이 문으로 겁니다.
- **색 리터럴 열아홉 곳이 토큰이 됐습니다** — 사이드바 열(글자·표면 색을 리터럴로 들고
  있어서 `--sidebar`를 바꿔도 그 위 글자가 안 따라왔습니다), `.error`/`.success`/링크 아홉.
  `--badge`도 같이 생겼습니다. 편집기 묶음이 다섯(바탕·글자·강조·사이드바·상태)이 됐고,
  `tests/colorLiterals.test.ts`가 새 리터럴을 막습니다.
- **색상 카드가 RGB·HEX 두 줄**이 됐습니다. 값 칸은 처음부터 RGB를 받았지만 그걸 알 방법이
  없었습니다.

⚠️ **사이드바 두 곳의 색이 아주 조금 바뀝니다** — 비슷한 회색 셋 중 둘(접기 버튼 `#aaaac3`,
아이콘 버튼 `#a0a0b9`)을 `--sidebar-muted`(`#9c9cb8`)로 합쳤습니다. 나머지는 값 그대로입니다.

### 눈으로 볼 것

- **비활성 입력·메모 칸의 모습이 바뀝니다.** `css/controls.css`의 `:disabled` 규칙이
  버튼 셋만 덮고 있어서, 잠긴 `<input>`·`<textarea>`는 **킷의 모습이 아예 없이**
  브라우저 기본값(플랫폼마다 다른 회색)으로 그려지고 있었습니다. 이제 킷의 다른 비활성
  표면과 같은 `opacity: .55` + `cursor: not-allowed`를 씁니다.
  **지금 이걸 받는 소비자는 없습니다**(실측 2026-08-11): `budget/frontend`는
  `#baf5b22b5f…` SHA 핀이고, `homa_gwangju`는 `file:vendor/…`로 저장소에 커밋된
  사본을 씁니다. 다음에 핀을 올릴 때 잠긴 입력이 있는 화면을 한 번 보세요 —
  **없던 흐리기가 새로 생깁니다**(원래는 브라우저 기본값이었습니다).

---

## v0.2.0 — 2026-08-11

[이 릴리스의 트리](https://github.com/kkqq9320/kkqq-ui-basic-kit/tree/v0.2.0) — 첫 태그라 비교 대상이 없습니다

첫 태그입니다. 그전까지는 커밋 SHA로만 걸 수 있었고, 소비 프로젝트 둘 다
`baf5b22b`(2026-07-30)에 묶여 있었습니다. **이 릴리스는 그 지점에서 206커밋**이고,
`src`·`css`만 18파일 +3737줄입니다.

### 타입이 깨지는 것은 딱 하나입니다

`DateWheelPicker`의 `ariaLabel`이 **필수**가 됐습니다(`ariaLabel?` → `ariaLabel`).
기본값 `"날짜"`도 사라졌습니다.

```diff
- <DateWheelPicker value={date} onChange={setDate} />
+ <DateWheelPicker ariaLabel="거래 날짜" value={date} onChange={setDate} />
```

스크린 리더 때문만이 아닙니다 — 이 값이 **팝오버 머리말로 화면에 그려집니다.**
안 넘기면 한 폼의 날짜 필드가 전부 같은 머리말을 답니다.

머리말과 접근성 이름을 갈라야 하면 `heading`을 쓰세요. 292px 머리말에는 `"날짜"`가,
이름으로는 `"거래 발생 날짜"`가 맞을 때입니다. 기본값이 `ariaLabel`이라 안 쓰면 그대로입니다.

> 안 넘긴 자리는 `tsc`가 전부 잡아 줍니다 — 옮기고 나서 `npm run typecheck` 한 번이
> 곧 완료 조건입니다. `AutoGrowTextarea`와 `MobileQuickBar`의 `ariaLabel`은 **선택
> 그대로**입니다(감싸는 `<label>`이 이미 이름을 주거나, 화면에 자국이 없어서).

### 동작이 바뀌어 눈으로 확인할 것

**날짜 피커가 네이티브 날짜 필드처럼 동작합니다.** 키보드 모델이 통째로 다시 설계됐습니다.

- `Tab`이 **컨트롤을 떠납니다.** 전에는 연·월·일 열을 걸어 다녔습니다.
- `←`/`→`가 세그먼트를 옮깁니다.
- **팝오버를 열지 않고도** 숫자를 쳐서 날짜를 넣습니다.
- 치는 중인 숫자가 트리거에 **자리를 지켜** 보입니다(`20‒‒. 07. 12.`).
- 포커스는 언제나 트리거 하나입니다. 팝오버로 옮기지 않습니다.
- `Enter`·`Escape`·`Delete`·`Ctrl+;`(오늘)가 동작합니다.

**⚠️ 트리거의 접근성 이름이 바뀌었습니다** — 이제 `"${ariaLabel}, ${보이는 값}"`입니다
(`"거래 날짜, 2026. 07. 12."`). **이름으로 트리거를 찾는 소비자 테스트가 깨집니다.**
이름 전체와 맞추지 말고 `ariaLabel` 부분으로 찾도록 고치세요.

**팝오버·드롭다운이 위로 열릴 때의 위치가 고쳐졌습니다.** 남은 공간이 아니라 상자
자신의 높이 기준으로 앉으므로, 트리거에서 멀리 떨어져 뜨던 것이 사라집니다.
날짜 피커는 최대 396px, 포털 `Select`는 최대 163px 어긋나 있었습니다.

**모바일에서 날짜 피커는 위로 뒤집지 않습니다.** 손가락이 필드 아래에 있어서입니다.
아래 공간이 모자라면 자리를 만들어 아래로 엽니다.

**`Select` 키보드가 roving tabindex로 바뀌었습니다.** `Tab`이 옵션마다 멈추지 않습니다.

**값을 확정하면 짧은 피드백이 재생됩니다**(230ms, `Select`·`DateWheelPicker` 양쪽).
`prefers-reduced-motion`이면 이동 없이 색만 돌아옵니다.

### 더해진 것

- `onViewportChange(handler)` — 앵커드 팝업 좌표를 다시 재야 하는 네 신호를 한 자리에서
  등록하고 해제 함수를 돌려줍니다. **`visualViewport`의 `scroll`이 새로 들어갑니다** —
  이게 없으면 핀치줌으로 확대한 채 패닝할 때 좌표가 낡습니다.
- `heading` (위 참고)

### 라이선스

`LICENSE`가 번들 폰트를 MIT 밖으로 뺐습니다. `fonts/PretendardVariable.woff2`는
SIL OFL 1.1이고 전문은 `fonts/OFL.txt`입니다. 조건은 그 파일이 폰트와 함께 다니는 것뿐이라
**소비자가 할 일은 없습니다.** `package.json`의 라이선스 식은 `MIT AND OFL-1.1`입니다.

### 옮기는 순서

1. 참조를 올립니다 — `npm i github:kkqq9320/kkqq-ui-basic-kit#v0.2.0`
   (pnpm이면 `pnpm add`, `vendor/` 사본이면 README `복사해서 써야 한다면` 절)
2. `npm run typecheck` — `ariaLabel` 누락이 여기서 전부 나옵니다
3. 테스트 — 날짜 피커 트리거를 **이름으로** 찾는 것들이 깨집니다
4. 날짜 필드와 드롭다운을 눈으로 한 번 — 특히 화면 아래쪽에서 위로 열리는 것

### 아직 확인되지 않은 것

정직하게 적어 둡니다. 기기가 없어 못 봤습니다.

- macOS `Cmd+;`(오늘) — **동작 안 한다는 보고가 있고 원인 미확정**입니다. Windows는 확인됨
- iOS Safari, 외장 키보드 iPad
- 확정 피드백의 시작색이 반전 칩 위에서 흐린 것(라이트 2.53:1)
- 빠른 플릭에서 칸이 유실되는지(`moveSwipe`가 이벤트당 한 칸)
- 숫자를 반쯤 친 상태에서 `disabled`를 켜면 확정되는지 — 설계 스펙 §7.1에 미해결로 명시

---

## baf5b22b 이전

태그가 없습니다. `git log`를 보세요.
