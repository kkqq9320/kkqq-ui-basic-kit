# 이 저장소에서 일하는 법

`kkqq-ui-basic-kit` — 오너(JJ)의 React 디자인 시스템. **공개 저장소**입니다.
빌드 산출물이 아니라 **`src`를 소스 그대로** 배포합니다.

> 📖 **처음이면 [`docs/AGENT-HANDOFF.md`](docs/AGENT-HANDOFF.md)부터 읽으세요** — 지금
> 무엇이 끝났고 다음에 무엇을 할지, 이 저장소가 값을 치르고 배운 것이 거기 있습니다.
> 이 파일은 **어기면 안 되는 규칙**만 담습니다.

## 🧪 검사 넷 — `npm test`만 돌리면 안 됩니다

```bash
npx vitest run          # 1729+ 검사
TZ=UTC npx vitest run   # 날짜 모델이 있어 시간대를 바꿔 한 번 더
npx tsc --noEmit        # 저장소 전체 (tests·demo 포함)
npm run typecheck:src   # 🔴 배포되는 것만 — 소비자가 실제로 컴파일하는 범위
```

🔴 **`npm test`는 타입을 안 봅니다.** 그리고 마지막 하나가 따로 있는 이유: `tests`와
`demo`가 끌어오는 타입이 `src`의 구멍을 덮어 **여기서는 초록인데 소비자에게서 빨간**
상태가 만들어집니다. 커밋 전에 넷을 다 돌리세요.

**CI 매트릭스는 Node가 아니라 React 18/19입니다.** React 19 전용 기능은 CI만 잡습니다.

## 🔴 검사에 대한 이 저장소의 기준

**새 검사는 그 고침을 지우고 빨개지는 것을 본 뒤에야 검사입니다.** 초록은 증거가
아닙니다. 이 저장소는 못 실패하는 검사를 **열 번 넘게** 출하했고 전부 기록돼 있습니다.

- **변이 전에 `git status --porcelain`이 비었는지 확인하세요.** `git checkout --`에
  일곱 번 당했습니다. 예외 없음 — 한 줄짜리 `node -e`에도.
- **개수가 아니라 이름으로 세세요.** *"열셋 중 열하나 죽었다"* 로는 어느 검사가
  공허한지 모릅니다.
- **혼자 빨개지지 않는 단언은 아무것도 안 지킵니다.** 늘 다른 검사와 같이 빨갛다면
  걷어내세요.
- **0 red면 감시자를 쓰기 전에 그 경로가 밟히는지 프로브로 먼저 보세요.** 0 red의
  이유는 다섯입니다 — 등가 · 안 밟힘 · 삼켜짐 · 배터리 고장 · 환경이 가림.
- **빈 입력으로 만족되는 단언은 검사가 아닙니다.** glob·정규식·필터 앞에 전제를 다세요.

## ✍️ 산문도 주장입니다

파일 머리말 · 구역 주석 · 릴리스 노트 · 커밋 메시지에 적는 사실은 **코드와 같은 잣대**를
받습니다. *"X가 Y를 씁니다" · "쓰는 곳은 …뿐" · "이 파일 안"* 은 전부 grep 한 번입니다.

🔴 **검사가 없는 숫자는 적지 마세요.** 세는 법을 붙여도 썩습니다 — 조각별 줄 수 표에
`세는 법`까지 붙여 심었는데 **바로 다음 커밋이 두 칸을 낡게 만들었습니다.**

## 📐 지켜지는 관습 (기계가 봅니다)

| 무엇 | 어디서 |
|---|---|
| 파일 이름·폴더·의존 방향 (§15) | `tests/moduleLayers.test.ts` |
| 배럴 둘의 공개 이름 | `tests/publicApi.test.ts` |
| 상태를 클래스가 아니라 속성으로 (§16) | `tests/stateClasses.test.ts` |
| 문서가 가리키는 경로가 실재하는가 | `tests/docPaths.test.ts` |

- `PascalCase.tsx` = 컴포넌트 / `camelCase.ts` = 그 밖의 것.
- **배럴의 묶음이 곧 폴더입니다** — `controls` · `surfaces` · `theme` · `shortcuts` ·
  `browser` · `model`.
- **상태는 클래스가 아니라 `data-*`/ARIA로 적고, 속성은 항상 값을 싣습니다**
  (`data-open="true"`, 꺼지면 속성 자체가 없음). `PRINCIPLES.md` §16.
- 자세한 규칙과 *"아직 안 지키는 자리"* 표는 `PRINCIPLES.md` §15·§16.

## 🤝 오너와 일하는 규칙

```
push · PR · 머지 · 머지된 브랜치 삭제      묻지 말고 하세요
태그 · GitHub 릴리스                      🔴 오너 확인 필요
소비자 핀(budget · homa_gwangju) 변경     🔴 오너 확인 필요
```

- **호환 별칭·shim을 만들지 마세요.** 옳은 모양으로 바꾸고, 소비자는 핀 올리는 날
  한 번에 고칩니다.
- **버그는 추측해서 고치지 말고 계측으로 근본 원인을 확정한 뒤** 고치세요.
- **실기기에서만 나는 버그에 에뮬레이션을 붙이지 마세요** — 데모의 TRACE 패널을
  요청하세요(`docs/AGENT-HANDOFF.md`의 *데모* 절).
- **결정을 오너에게 올릴 때는 고를 수 있는 상태로 올리세요.** 초안 없이 물으면 답할 수
  없습니다. 그리고 **승인은 결정이 아닙니다** — 기록할 때 누구의 논리인지, 어떤 조건이
  붙었는지 같이 적으세요.

## 🛠 이 환경에서 자주 밟는 것

- **셸이 백틱과 역슬래시를 먹습니다.** 문서·코드를 고치는 스크립트는 `node -e "…"`가
  아니라 **`.mjs` 파일로** 쓰세요. 한 세션에 일곱 번 당했습니다.
- **저장소 파일은 CRLF입니다.** 앵커를 `\n`으로 이으면 안 맞습니다.
- **`str.replace`는 패턴이 없으면 조용한 no-op입니다.** 치환 전에 **앵커를 세세요.**
- `PowerShell`의 `Set-Content`는 ANSI가 기본이라 한국어 주석을 깨뜨립니다. 커밋
  메시지는 `git commit -F <파일>`로.
