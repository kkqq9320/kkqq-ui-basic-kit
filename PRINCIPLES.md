# 디자인 원칙

이 파일이 디자인 시스템의 계약입니다. 새 화면이나 컨트롤을 만들 때는 아래 토큰과
컴포넌트를 **확장**하세요. 페이지 전용 오프셋, 자체 드롭다운, 자체 팝오버,
자체 스크롤바 스타일을 새로 만들지 않습니다.

**먼저 읽을 것:** [`LAYOUT-PRINCIPLES.md`](LAYOUT-PRINCIPLES.md) — 배치·그리드·정렬의
일반 규칙입니다. 이 파일은 그 규칙을 **이 킷에서 어떻게 구현했는가**를 적습니다.

---

## 1. 토큰이 유일한 진실

모든 컴포넌트 CSS는 `css/tokens.css`의 커스텀 프로퍼티만 참조합니다. 색상 리터럴을
컴포넌트 CSS에 직접 쓰지 마세요. 테마를 바꿀 때 `tokens.css` 한 파일만 고치면
되는 상태를 유지합니다.

- 라이트/다크는 `<html data-theme="dark">`로 전환하고 브라우저별로 저장합니다.
- 다크 전용 예외가 필요하면 `[data-theme="dark"] .클래스` 한 줄로 끝냅니다.
- 강조색은 `--accent`(`#575bd4`) 하나입니다. 상태색(`--green`, `--orange`,
  `--red`)은 의미가 있을 때만 씁니다.
- `--gold`·`--deep`은 킷이 쓰지 않고 **앱에 열어 둔 색**입니다 — 그래서 색상 편집기에도
  안 나옵니다(v0.7.0). 안 쓰는 색을 편집기에 얹으면 사용자가 고쳐도 화면이 안 바뀌어
  고장으로 보입니다. 앱이 그 색을 쓰고 사용자가 고르게까지 하려면 자기 `groups`나
  `createThemePalette`에 넣으세요(`CUSTOMIZING.md`).

## 2. 높이 계층

| 높이 | 용도 | 토큰 |
|---|---|---|
| 41px | 입력·드롭다운·날짜 필드 | (컴포넌트 고정) |
| 38px | 표준 액션 버튼 | `--action-height` / `--action-min-width` 88px |
| 32px | 조밀한 목록·팝오버 액션 | `--compact-action-height` / `--compact-action-min-width` 64px |

규칙 자체는 [`LAYOUT-PRINCIPLES.md`](LAYOUT-PRINCIPLES.md) §4입니다. 이 표는 킷이
고른 값입니다. 입력과 나란히 놓이는 버튼만 41px을 써서 줄을 맞춰도 됩니다.

## 3. 드롭다운 기하학

- **12px 인라인 패딩** + **18px 아이콘 슬롯 안의 13px 셰브런** 하나로 통일합니다.
- 왼쪽 정렬: 텍스트의 왼쪽 패딩 = 셰브런의 오른쪽 패딩.
- 가운데 정렬: 양쪽에 18px 슬롯을 둬서 텍스트가 수학적으로 중앙에 오게 하고,
  셰브런은 표준 오른쪽 패딩을 유지합니다.
- 열 때 셰브런은 **제자리에서 180° 회전만** 합니다. 이동시키지 않습니다.
- 네이티브 `<select>`를 쓰지 않습니다. OS마다 메뉴가 달라 표면을 통일할 수 없습니다.

### 열림 방향과 배치

- 트리거 아래 공간이 메뉴 높이보다 좁고 위가 더 넓으면 **위로 엽니다**.
- 배치는 옵션 개수, `visualViewport`(가상 키보드 포함), 하단 고정 바 높이를
  모두 계산에 넣습니다. 메뉴에 닿으려고 페이지를 스크롤하게 만들면 안 됩니다.

> ⚠️ **위 두 문장은 모바일의 `DateWheelPicker`에서 덮힙니다**(설계 스펙
> `2026-08-06-date-picker-keyboard-design.md` §7.0, 오너 실기기 지시). 조용히
> 어긋나게 두면 다음 사람이 드롭다운과 통일하려 들기 때문에 여기 적습니다.
>
> - **위로 열지 않습니다.** 폰에서는 손가락이 필드 아래에 있어 위로 뜬 팝오버를
>   **손이 가리고**, 뒤집힘 자체가 같은 필드를 두 번 열 때 **자리를 오락가락**하게
>   만들어 조준을 다시 하게 합니다. 데스크톱은 그대로입니다 — 포인터는 내용을
>   가리지 않고 화면이 넓어 뒤집혀도 조준이 안 흔들립니다.
> - **아래가 모자라면 컨트롤이 스스로 스크롤해 자리를 만듭니다.** 두 번째 문장이
>   금지한 것은 **사용자에게** 스크롤을 시키는 것입니다. 여기서 스크롤하는 주체는
>   컨트롤이고 사용자는 아무것도 더 하지 않습니다 — **그 구분이 이 예외의 전부이고,**
>   **구분이 흐려지면 예외도 성립하지 않습니다.** 모자란 만큼만 움직이고, 필드를
>   화면 위로 밀어내지 않으며, 끝까지 가도 모자라면 뒤집는 대신 `maxHeight`로 줄입니다.
>
> 닫을 때 스크롤을 **되돌리지 않습니다** — 그 사이 사용자가 스크롤했을 수 있고,
> 컨트롤이 사용자의 자리를 뺏으면 안 됩니다(`apple-design` §16.2 Agency).
- 카드 오버플로나 스태킹 컨텍스트에 잘릴 수 있는 메뉴는 `document.body` 포털에
  `position: fixed`로 렌더합니다(`Select`의 `portal` prop). 포털 좌표는 스크롤·
  리사이즈·`visualViewport` 변화마다 다시 계산합니다. 잘리는 조상은 눈에 잘 띄지
  않으니 아래를 의심하세요:
  - `overflow: hidden`인 접힘/펼침 컨테이너 — **사이드바 슬롯이 대표적입니다.**
    높이를 애니메이션하려면 클리핑이 필수라 이 둘은 원리상 양립할 수 없습니다.
  - 모바일 사이드바(`overflow-y: auto`), 스크롤 카드, 테이블 래퍼
  - `transform`·`filter`가 걸린 조상 — 새 스태킹 컨텍스트가 생겨 `fixed`도 갇힙니다
- **포털 메뉴는 바깥 클릭 감지에서 별도로 제외해야 합니다.** 컴포넌트 루트 밖에
  있으므로, 루트만 검사하면 옵션을 누르는 순간 메뉴가 먼저 닫혀 선택이 죽습니다.
- 760px 이하에서는 뷰포트 폭에서 좌우 8px을 뺀 폭을 쓰고 하단 고정 바 위에서 멈춥니다.

### 닫힘과 포커스

- 바깥 클릭/탭, Escape, 같은 트리거 재클릭으로 닫습니다.
  (**날짜 필드에는 `Escape`에 예외가 하나 있습니다** — 닫힌 채 치던 숫자를 들고
  있으면 그 `Escape`를 버퍼 버리는 데 씁니다. §11 참고.)
- **스크롤로 트리거가 화면에서 멀어져도 닫힙니다.** 단 메뉴 안에서 시작해 아직
  끝나지 않은 포인터 제스처가 만드는 스크롤은 예외입니다 — 그 스크롤은 메뉴
  자신을 움직일 뿐, 트리거를 움직인 게 아닙니다. 판정을 스크롤의 타깃이 아니라
  제스처가 시작된 위치로 하는 이유: 메뉴가 스크롤 끝에 닿거나 애초에 짧아
  스크롤할 내용이 없으면 같은 터치가 조상(페이지)으로 체이닝돼 트리거가 실제로
  움직이는데, 타깃만 보면 그 체이닝된 스크롤(타깃이 `document`/`#root`)과
  진짜 바깥 스크롤을 구분할 수 없기 때문입니다. 메뉴의 `overscroll-behavior:
  contain`은 메뉴 자신이 스크롤 가능할 때 그 체이닝 자체를 막아 줍니다.
- **뒤로가기로도 닫힙니다.** 열려 있는 팝업은 종류를 가리지 않고 history 표식을
  쌓고, 뒤로가기는 **맨 위 것부터** 닫습니다(§10 참고). 이게 없으면 다이얼로그
  안에서 드롭다운을 열어 둔 채 뒤로가기를 눌렀을 때 드롭다운이 아니라
  **다이얼로그가 통째로 닫혀 입력하던 내용이 날아갑니다.** 대가로 팝업을 닫을
  때마다 `history.back()`이 한 번 일어나므로, 앱이 자체 `popstate` 로직을
  갖고 있다면 그 호출과 자기 것을 구분할 수 있어야 합니다.
- 선택하면 포커스를 트리거로 되돌립니다.
- **모바일에서 옵션을 누르기 직전에 스크롤 위치를 기록**해 두고, `preventScroll`로
  포커스를 되돌린 뒤 그 위치를 복원합니다. 선택 때문에 보던 화면이 움직이면 안 됩니다.
- **메뉴가 열릴 때 선택된 옵션이 보이도록 메뉴 자신의 `scrollTop`을 옮깁니다.**
  옵션이 많으면(`Select`는 옵션 개수를 제한하지 않습니다) 선택된 항목이 첫 화면
  밖에 있을 수 있고, 그러면 사용자 눈에는 아무것도 선택 안 된 것처럼 보입니다.
  `Element.scrollIntoView()`는 조상 스크롤 컨테이너까지 함께 움직이므로 쓰지
  않습니다 — 위 규칙과 같은 이유로, 열려는 메뉴가 다이얼로그 뒤 페이지를 움직이면
  안 됩니다. 이미 보이면 점프시키지 않고, 안 보이면 가운데로 옮기되 목록 끝을
  넘기지 않습니다.

### 트리거 상태

드롭다운 트리거와 날짜 필드는 **같은 41px 티어의 팝오버 트리거**이므로 상태 피드백이
같습니다 — 호버·키보드 포커스·열림 모두 **강조색 테두리 + 3px 강조색 12% 아웃라인**.
한쪽만 반응하면 같은 줄에 놓인 두 필드가 다른 컨트롤처럼 보입니다.

`:hover`는 비활성 요소에도 매칭되므로 반드시 `:not(:disabled)`로 빼야 합니다. 안 그러면
흐려진 필드가 마우스만 올려도 살아납니다.

### 옵션 상태

- 호버/키보드 포커스: **1.015배 중심 확대 + 불투명 강조색 배경 + 흰 글자**, 140ms.
  transform만 쓰므로 이웃 옵션이 밀리지 않습니다.
- 선택됐지만 호버되지 않은 상태: **조용한 처리**(강조색 글자 + 연한 표면).
- 비활성 옵션은 활성 스타일도 확대도 받지 않습니다. **선택된 값이 마침 비활성이면
  조용한 처리도 받지 않습니다** — 비활성 표시가 이겨야 "고를 수 없다"가 먼저 읽힙니다.
- **고를 수 있는 옵션이 하나도 없으면**(전부 비활성이거나 목록이 비었으면) 드롭다운
  자체를 비활성으로 렌더합니다. 열리되 아무것도 못 고르는 상태를 만들지 않습니다 —
  목록이 아직 안 불러와진 경우도 같습니다(내용 없는 빈 메뉴 대신 비활성).
- `prefers-reduced-motion: reduce`에서 transform과 transition을 제거합니다.

## 4. 글래스 표면

열린 메뉴, 정보 디스클로저, 작은 앵커드 팝오버는 **전부 같은 표면 하나**를 씁니다:

```
30% 표면 틴트 + 18px 블러 + saturate 145% + 얇은 테두리 + 그림자
```

`css/surfaces.css`의 `.dropdown-menu-surface` / `.glass-popover`가 그것입니다.
새 팝업에 자체 배경·그림자를 정의하지 마세요.

모바일의 떠 있는 컨트롤(빠른 바, 페이지 카드, 탭 카드)은 조금 더 옅은
22% 틴트 + 12px 블러를 씁니다. 이 표면들의 **선택 상태는 불투명 강조색으로 채우지
않고 반투명 강조색 + 블러**를 씁니다.

## 5. 날짜 필드

모든 날짜 입력은 `DateWheelPicker` 하나를 씁니다.

- **팝오버가** 3열(연/월/일) 구조입니다. 각 열은 **30px 이전 버튼 / 150px 클리핑
  뷰포트 / 30px 다음 버튼** 고정입니다. 값이 움직여도 화살표를 밀거나 가리면 안 됩니다.
  (열은 값을 **고르는** 자리입니다 — 값을 **치는** 자리인 세그먼트는 트리거에 있고,
  아래 활성 세그먼트 조항이 그 구분을 씁니다.)
- **연·월·일은 독립**입니다. 연도 열은 연도만, 월은 그 해 안에서 1↔12 순환,
  일은 그 달 안에서 1↔말일 순환. 연·월이 바뀌어 일이 무효가 되면 목적지 달의
  말일로 **잘라내기만** 하고, 절대 다른 열로 자리올림하지 않습니다.
- **활성 세그먼트는 "트리거가 가리키는 자리"입니다 — "포커스된 열"이 아닙니다.**
  포커스는 언제나 트리거 하나에 있고 팝오버의 열은 포커스를 받지 않습니다(§11).
  트리거가 그 자리를 강조색 20% 배경(4px 반경)으로 그리되, **트리거가 포커스를 가진
  동안에만** 보입니다 — 포커스 없는 필드에 활성 표시가 남아 있으면 그 필드가 입력을
  받는 중으로 읽힙니다.
- **팝오버를 열지 않아도 값을 고칠 수 있습니다.** 숫자·`←`/`→`·`Backspace`·`Delete`·
  `Ctrl+;`가 닫힌 채로 동작합니다(키 표는 §11). 기준선은 네이티브
  `<input type="date">`이고, 거기서 달력은 값을 고르는 또 하나의 방법일 뿐입니다.
  치던 숫자(입력 버퍼)도 닫힌 채로 살아 있으므로, 트리거는 그 버퍼를 자리를 지켜
  그립니다(`20‒‒. 07. 12.`).
- 데스크톱 휠은 **커서가 있는 열**을 바꾸면서 그 열을 활성으로 만듭니다. 모바일
  스와이프와 키보드 `↑`/`↓`는 **활성 세그먼트만** 바꿉니다 — 키보드에는 "커서가 있는
  열"이라는 것이 없습니다. 활성 표시를 옮기는 길은 넷입니다: `←`/`→`, **트리거의
  세그먼트를 직접 누르기**, 팝오버의 열을 누르거나 스와이프하기, 그리고 자릿수가 차서
  다음 세그먼트로 자동으로 넘어가는 것.
- **`disabled`가 켜지면 열려 있던 팝오버를 닫습니다.** 렌더 조건에 `!disabled`를
  더해 감추는 것으로는 안 되고, **끊는 자리는 `open` 자신**입니다 — 감추기만 하면
  `aria-expanded`가 `true`로 남고 치던 버퍼도 살아남습니다. 이유는 둘입니다.
  ① 키만 막으면 **휠·스와이프·± 버튼은 여전히 값을 바꿔** 반쪽 잠금이 됩니다.
  ② 브라우저가 비활성이 된 트리거를 blur하는데 팝오버 안에는 tab 정거장이 없으므로
  (§11), 키보드로는 그 팝오버에 다시 닿을 방법이 없습니다.
  닫으면서 **값을 확정하지는 않습니다** — 비활성화는 소비자의 조작이지 사용자의
  "완료"가 아닙니다(§12). 치던 버퍼도 확정하지 않고 버립니다.
  - ⚠️ **버퍼 쪽은 jsdom에서만 확인됐고 실브라우저에서 반대로 갈 수 있습니다.**
    실브라우저는 비활성이 된 트리거를 blur하는데, 이 컨트롤은 트리거의 `blur`에서
    버퍼를 **확정**합니다(다른 곳을 클릭해 떠나는 경로를 위해 필요한 규칙입니다).
    그 blur가 먼저 돌면 버리려던 버퍼가 확정됩니다. jsdom에서는 그 blur가 아예 나지
    않아 판정할 수 없습니다 — **미해결로 적어 둡니다.** 실기기에서 볼 것: 팝오버를
    열고 숫자를 한 자리만 친 뒤 소비자가 `disabled`를 켰을 때 값이 그대로인가.
- **휠로 페이지가 같이 스크롤되는 것은 네이티브 `{ passive: false }` 리스너로 막고,
  React의 `onWheel` 안에서는 `preventDefault()`를 부르지 마세요.** React는
  `wheel`·`touchstart`·`touchmove`를 루트(와 포털 컨테이너)에 passive로 등록하므로
  합성 이벤트에서의 `preventDefault()`는 **아무 효과가 없고** 콘솔에
  `Unable to preventDefault inside passive event listener invocation`만 남깁니다.
  React 핸들러는 값 변경만 담당합니다.
  (`keydown`은 passive가 아니므로 화살표 키의 `preventDefault()`는 정상 동작합니다.)
- 포인터 이동은 **주 버튼을 누르고 있는 동안에만** 값을 바꿉니다(호버만으로 안 바뀜).
- 스와이프는 **30px 경계를 넘을 때마다 즉시 한 칸 커밋**하고 남은 거리는 손가락을
  계속 따라갑니다. 손을 뗄 때 여러 칸이 한꺼번에 튀면 안 됩니다.
- 변화는 진행 방향으로 210ms 슬라이드, 가운데 값은 축소에서 1.09까지 커졌다가
  선택 배율로 안착합니다. 보이는 양끝 바깥으로 한 칸씩 미리 렌더합니다.
- min/max 밖의 값은 비활성. `비우기`는 선택형 날짜에만 노출합니다.
- **열마다 격자(`step`)를 줄 수 있습니다** — `step={{ minute: 15 }}`. 안 넘긴 열은 1이라
  기본 동작은 그대로입니다. **격자의 기준점은 언제나 그 열의 바닥값**(시·분·초 0,
  월·일 1)이지 `min`이 아닙니다 — `min`을 기준으로 잡으면 같은 `step`을 준 픽커가
  `min`에 따라 다른 값 집합을 내줍니다. 한 노치가 한 칸이고(휠·화살표·스와이프·±),
  타이핑이 격자에 안 떨어지면 **내립니다**. **`min`/`max`는 격자보다 우선이라 격자 밖이어도
  끝점으로 들어옵니다** — `min="03:07"`에 분 `step` 15면 열은 `03:07 → 03:15 → 03:30`이고
  `03:07` 아래는 갈 수 없습니다. ⚠️ **그 열의 상한(말일·59분)은 끝점이 아닙니다** — 격자가
  그냥 순환하고 값은 격자 위로 내려옵니다(2월에 일 `step` 5면 `31`을 쳐도 26). 분 `step` 7의
  열이 `0 … 56` 다음 바로 `0`인 것과 같은 규칙입니다.
- 닫힌 상태의 날짜 컨트롤도 전역 12px 패딩과 18px 아이콘 슬롯을 씁니다.

## 6. 스크롤바와 레이어

- `html`은 `scrollbar-gutter: stable`을 유지해, 오버플로가 생겨도 문서 폭이
  흔들리지 않게 합니다.
- 모든 스크롤 컨테이너는 Firefox `scrollbar-width: thin` + 공용 4px
  `--scrollbar-size`를 씁니다. `::-webkit-scrollbar`를 지원하는 엔진에서는
  표준 속성을 `auto`로 되돌려야 Chromium이 더 굵은 네이티브 thin 폭을 예약하지
  않습니다. 트랙은 투명, 기본 썸은 24% 틴트, 호버 썸은 40%.
  컴포넌트는 `overflow` 방향과 최대 크기만 정하고 **스크롤바 외형은 바꾸지 않습니다**.
- **760px 이하 또는 coarse 포인터에서는 문서 스크롤을 끄고 `#root`를 세로 스크롤
  호스트로 씁니다.** 터치 스크롤은 살리고, **그 페이지 스크롤 호스트
  (`html`/`body`/`#root`)의 스크롤바만** 숨깁니다 — 고정 하단 바가 주소창 변화에
  흔들리지 않게 하는 전제 조건은 이 호스트에만 해당하기 때문입니다. Select
  메뉴·다이얼로그·날짜 팝오버·사이드바 서랍처럼 안쪽에서 스스로 스크롤하는
  표면은 이 숨김 대상이 아니며, 위(이 절 앞부분)의 공용 4px thin 처리(트랙 투명,
  기본 썸 24%, 호버 40%)를 데스크톱과 동일하게 그대로 씁니다.
- `body`와 `#root`에 하드 최소 폭을 주지 마세요. 높은 확대, 폴더블, 접근성 텍스트
  확대는 실효 뷰포트를 320px 아래로 만들 수 있습니다. 문서 레벨 가로 오버플로
  없이 줄어들거나 줄바꿈돼야 합니다.
- **데스크톱에서는 페이지 스크롤바를 그립니다.** 감추고 싶은 앱은 감출 수 있고
  방법은 `CUSTOMIZING.md`에 있습니다 — 다만 **감추는 대상은 페이지 스크롤 호스트
  셋(`html`·`body`·`#root`)뿐이어야 합니다.** `*`로 넓히면 안쪽에서 스스로
  스크롤하는 표면(Select 메뉴, 다이얼로그, 사이드바 서랍)까지 잃고, 그 표면들은
  **실제로 스크롤되는데도 "더 있다"가 보이지 않게 됩니다.** 페이지는 잘린 내용
  자체가 그 신호를 주지만 안쪽 표면은 그렇지 않습니다. 이 킷이 실제로 한 번 그렇게
  만든 적이 있고, 위 모바일 규칙이 처음부터 셋으로 좁혀져 있는 이유가 그것입니다.

### z-index 계층 (고정)

| 범위 | 용도 |
|---|---|
| 0–10 | 기본 콘텐츠 |
| 50–60 | 사이드바, 모바일 내비게이션 |
| 80 | 일반 드롭다운 |
| 100–110 | 앵커드 정보 팝오버 |
| 200 | 모달 백드롭 |
| 400–450 | 포털로 띄운 메뉴·피커 |

## 7. 페이지 배치

이 절은 **앱 셸 안의 콘텐츠 페이지**에만 적용됩니다 — `PageHeader` → (탭) →
`SectionHeading` → `Panel` 스택. 다이얼로그·독립 카드·팝오버처럼 셸 밖에 서는
표면은 여기에 매이지 않습니다. 그쪽은 `Dialog`(머리말 없이 children만)나
`Panel`(`title`·`hint`·`actions`가 전부 없으면 머리말을 아예 안 그림)처럼 머리말이
통째로 optional입니다.

그 스택 안에서 데스크톱 세로 순서는 **고정**입니다:

```
eyebrow → 페이지 제목 → 설명 → (탭) → 섹션 제목 → 섹션 설명 → 콘텐츠 카드
```

- **eyebrow와 제목은 페이지의 앵커라 늘 있습니다**(`PageHeader`에서 필수).
  제목이 없다면 그건 앱 페이지가 아니라 다른 표면이라는 신호입니다 — `Panel`이나
  `Dialog`를 쓰세요. "제목 없는 페이지"를 위해 이 스택을 부수지 마세요.
- **설명은 값이 없어도 자리는 예약합니다.** `PageHeader`·`SectionHeading`의
  `description`은 optional이지만, 비어도 빈 자리가 남아 높이를 지킵니다.
  왜 그런지는 [`LAYOUT-PRINCIPLES.md`](LAYOUT-PRINCIPLES.md) §5에 있습니다.
- 탭이 없는 페이지는 탭 줄만 뺍니다. 페이지 전용 음수 마진을 쓰지 않습니다.
- **페이지 끝 여백은 스스로 스크롤을 만들지 않습니다.** 내용이 화면에 들어가면
  남는 자리만 쓰고, 이미 넘치는 페이지에서는 `--workspace-space-bottom`만큼
  그대로 남습니다. **패딩으로는 이렇게 못 합니다** — 패딩은 진짜 공간이라 그 자신이
  넘침을 만드는 순환이고, 그래서 "항상 그만큼"과 "절대 스크롤을 안 만듦"이 CSS
  안에서 배타적입니다. 그래서 `AppShell`이 **내용만의 높이**를 재서 가릅니다.
  앱이 끝 여백에 무언가를 기대고 있으면 `trailingSpace="fixed"`로 예전 동작(항상
  그만큼)으로 돌아갑니다 — `CUSTOMIZING.md` 참고.
  ⚠️ **모바일은 해당 없습니다.** 그쪽 하단 패딩은 숨 쉴 공간이 아니라 고정 바 자리와
  키보드 보정이 읽는 값이라(§9) 성격이 다릅니다.
- 헤더의 세 줄은 각각 최소 높이를 예약합니다(같은 §5).
- 섹션 설명은 **19px 3줄, 최소 57px**을 예약합니다. 탭마다 설명 길이가 달라도
  첫 콘텐츠 카드가 항상 같은 세로 좌표에서 시작합니다.
- 콘텐츠는 작업 영역 **전체 폭**의 `.panel`에서 시작합니다. 반응형 2·4열 그리드는
  이 부모 폭을 나눠 쓰고, 더 좁은 페이지 컨테이너를 새로 만들지 않습니다.

배치를 조금 다르게 하고 싶으면(문구·유무·순서) 페이지에서 마크업을 새로 짜지 말고
이 컴포넌트들의 **props로** 조절하세요. 어떤 prop으로도 안 되는 변형이 정말
필요하면 이 저장소에서 컴포넌트에 prop을 더하고(색상 편집기의 `groups`,
`DateWheelPicker`의 `labels`·`timeZone`처럼) push 하세요. 소비 앱에서 손으로 다시
짜면 원본과 갈라진 사본이 생깁니다.

### 오른쪽 위 아이콘 줄

페이지와 무관하게 항상 닿아야 하는 액션(화면 모드 전환, 설정)은 작업 영역
오른쪽 위 `.page-actions`에 38px 아이콘 버튼으로 둡니다. 사이드바 푸터가 아니라
여기입니다 — 사이드바는 모바일에서 닫혀 있고 접히면 아이콘만 남기 때문입니다.

- 데스크톱 `top: 34px`, 모바일 `top: 28px`. 모바일에서는 다른 떠 있는 컨트롤과
  같은 22% 틴트 + 12px 블러 글래스를 씁니다.
- **모바일 탭 카드는 같은 줄에서 아이콘 줄 왼쪽에 붙습니다**
  (`right: 16 + 82 + 6 = 104px`). 두 컨트롤이 같은 자리를 다투면 안 됩니다.
- **모바일에서는 이 줄이 한 줄을 통째로 차지하고 헤더가 그 아래에서 시작합니다**
  (`.workspace`의 `padding-top: 74px`). 헤더 오른쪽을 깎아 컨트롤을 피하게 하지
  마세요 — 제목 폭이 줄면 페이지마다 감기는 줄 수가 달라져 첫 콘텐츠 카드의
  세로 위치가 흔들립니다(§7의 기본 규칙과 충돌).
- 데스크톱은 폭이 넉넉하므로 헤더를 내리는 대신 eyebrow·제목에만
  `padding-right: 96px`로 아이콘 줄을 피합니다.
- 같은 액션을 사이드바 푸터에도 둘 수 있습니다. 사이드바가 닫히거나 접혀도
  닿을 수 있게 하는 것이 목적이므로 중복 자체는 문제가 아닙니다. 다만 **같은
  화면으로 가는 내비게이션 항목을 사이드바 메뉴에 또 두지는 마세요.**

### 탭

- 데스크톱은 밑줄형 탭 바. 모바일은 탭 바를 감추고 **페이지 헤더 오른쪽 위에
  현재 탭 카드 하나**만 띄웁니다. 카드를 누르면 바로 아래에 전체 목록이 열리고,
  선택·바깥 조작·스크롤·Escape로 닫힙니다.
- 선택형으로, 오른쪽 아래에 페이지 이동용 플로팅 카드를 둘 수 있습니다. 이 카드는
  강조색 없이 중립 글래스를 유지하고 목록을 **위로** 엽니다.
  이 카드도 **바깥 조작·Escape·스크롤로 닫혀야 합니다.** 떠 있는 컨트롤은 자기
  DOM 밖에 있는 것을 눌러도 닫히지 않으면 화면에 남아 다른 버튼을 가립니다.
  카드와 하단 빠른 바는 한 덩어리로 취급해 둘 다 "바깥" 판정에서 제외해야,
  토글 버튼을 누르는 순간 닫혔다 다시 열리지 않습니다.
- 350px 이하에서는 플로팅 카드만 가운데 빠른 바 위로 올려 겹침을 막습니다.

## 8. 사이드바

- 데스크톱 238px, 접으면 76px(아이콘만). 접힘 상태는 브라우저에 저장합니다
  (저장은 쓰는 쪽 책임 — 컴포넌트는 controlled).
- 접히면 라벨·그룹 제목·상단 슬롯이 사라지고, 그룹 제목은 얇은 구분선으로 바뀝니다.
- 마지막 nav 그룹은 `pinToBottom`으로 바닥에 붙입니다.
- **모바일 서랍은 뒤로가기로 닫힙니다.** 화면을 덮는 오버레이는 종류를 가리지 않고
  이 계약을 따릅니다(§3의 "뒤로가기로도 닫힙니다"). 표식은 `Sidebar`가 밀어 넣습니다 —
  `AppShell`도 같은 `mobileOpen`을 받지만 둘 다 밀면 서랍 하나에 표식이 두 개 쌓여
  뒤로가기를 두 번 눌러야 합니다. **한 상태에 한 곳에서만 밀어야 합니다.**
  데스크톱 폭에서는 서랍을 열 버튼이 숨겨지지만, 열어둔 채 창을 넓히면 `mobileOpen`이
  참으로 남아 표식도 남습니다 — 뒤로가기 한 번이 보이지 않는 서랍을 닫는 데 쓰이고,
  상태는 실제로 닫힌 것이 맞습니다.

### 접기 애니메이션 (지키지 않으면 화면이 움찔거립니다)

- **폭은 `.sidebar`의 `width`가 구동합니다.** 셸 그리드는 `auto minmax(0, 1fr)`로
  두고 트랙이 따라오게 합니다. `grid-template-columns`를 238px↔76px로 바꾸지
  마세요 — 트랙 값은 즉시 스냅하는데 안쪽 패딩·라벨만 트랜지션을 타서 어긋납니다.
- **접힘/펼침에 `display: none`을 쓰지 마세요.** 왜 안 되는지는
  [`LAYOUT-PRINCIPLES.md`](LAYOUT-PRINCIPLES.md) §7입니다. 폭이 있는 것은
  `max-width`/`width`를 0으로, 보이는 것은 `opacity`를 0으로 보냅니다.
- **트랙 개수가 바뀌는 grid는 접기 대상으로 쓰지 마세요**(같은 §7). 푸터처럼
  접히는 영역은 flex + `flex-wrap`으로 둡니다.
- **단, 접기 대상인 데스크톱 사이드바 푸터는 `flex-wrap`을 쓰지 않습니다.**
  시도해 봤더니 접는 도중 중간 폭에서 액션들이 아랫줄로 감겼다가 다시 튀어
  오르는 스냅이 생겼습니다(`css/sidebar.css:143-146`). 대신 `nowrap`을 유지해
  이름칸(`flex: 1 1 auto`)이 폭 변화를 흡수하고, 접힘 상태는
  `flex-direction: column`으로 따로 세웁니다 — 다음에 이 규칙대로 "고치지"
  마세요. (모바일은 접기 자체가 없으므로 — 아래 "모바일에서는 접힘 상태를
  무시" 참고 — 이 예외 대상이 아니고, 드로어 푸터는 다시 `flex-wrap`을
  씁니다.)
- 폭·패딩·라벨·구분선은 **같은 duration과 같은 easing**을 씁니다
  (`--sidebar-motion`, `--sidebar-ease`). 라벨의 `opacity`만 절반 길이로 먼저
  사라져서, 글자가 잘리는 순간이 보이지 않게 합니다.
- **nav 라벨에는 `text-overflow: ellipsis`를 쓰지 않습니다.** 넣으면 "…"이 접기
  시작과 동시에(0프레임째부터) 보여, 바로 위에서 라벨을 절반 길이 만에 먼저
  페이드아웃시켜 잘리는 순간을 감추는 장치를 무력화합니다. 대신 긴 라벨은 아무
  표시 없이 그냥 잘립니다 — 대체 수단은 `title`뿐이고, 터치에는 `title`의 호버
  등가물이 없습니다.
- `justify-content` 같은 비보간 속성으로 정렬을 바꾸지 마세요(같은 §7). 라벨이 0폭으로
  줄어들면 `justify-content: center` 하나로 확장·접힘 양쪽이 해결됩니다.

### 선택 피드백

내비게이션 항목의 배경은 요소가 아니라 **`::before`가 그립니다**
(`z-index: -1` + `isolation: isolate`로 글자 뒤에 놓임). 호버와 선택이 같은
레이어를 공유하므로 상태가 겹치지 않습니다.

- 선택되지 않음: `opacity: 0`, `scaleX(.82)`
- 호버: `opacity: .5`, `scaleX(1)` — 더 짧은 duration
- 선택됨: `opacity: 1`, `scaleX(1)` (280ms, 왼쪽 기준으로 펼쳐짐)
- 선택됨에는 왼쪽에 3×18px 강조 막대(`::after`)가 높이 0에서 자라납니다
- 선택되는 순간 아이콘이 `scale(.82 → 1.14 → 1)`로 한 번 튕깁니다 (320ms)

`prefers-reduced-motion: reduce`에서는 위 전부 — 트랜지션, `scaleX`, 아이콘 pop —
가 제거되고 상태만 즉시 바뀝니다.
- 모바일에서는 **접힘 상태를 무시**하고 항상 완전한 오프캔버스 서랍으로 열립니다.
  서랍 폭은 `min(310px, 86vw)`, 백드롭은 3px 블러, 푸터는 sticky.
- 사이드바는 어두운 표면(`--sidebar`)입니다. 그 안에 넣는 밝은 컨트롤은
  `.sidebar-slot` 안에 두면 자동으로 대비가 맞습니다.
- **슬롯 안의 드롭다운·팝오버는 포털로 띄워야 합니다.** 슬롯은 접기 애니메이션
  때문에 `overflow: hidden`이고, 모바일에서는 사이드바 자체가 `overflow-y: auto`라
  포털 없이는 메뉴가 잘립니다(§3 참고).
- **푸터도 `overflow: hidden`입니다.** 높이(`.sidebar-footer`의 `height`)를
  애니메이션하려면 클리핑이 필수라는 §3의 원칙이 여기도 그대로 적용됩니다 —
  `.sidebar-slot`이 그 선례입니다. 따라서 푸터 안에 팝오버를 놓는다면 슬롯과
  똑같이 `portal`을 켜야 합니다.
- 라우터에 의존하지 않습니다. `active` + `onSelect`(또는 `href`)만으로 동작합니다.

## 9. 모바일 규칙

- 하단 가운데 떠 있는 빠른 바(216×60, 64px 3칸)가 서랍을 엽니다.
- 떠 있는 컨트롤들은 **아래로 18px 연속 스크롤하면 사라지고**, 위로 스크롤하거나
  맨 위에 닿으면 돌아옵니다. 가상 키보드가 뷰포트를 120px 넘게 줄이면 숨고,
  키보드가 닫히면 돌아옵니다.
  (키보드 판정은 높이 변화만 보지 말고 **편집 요소 포커스 여부를 함께** 봐야
  주소창 축소를 오인하지 않습니다.)
- **가상 키보드가 열리면 작업 영역은 가려지는 만큼 하단에 여유 패딩을 예약하고,
  지금 포커스된 필드(페이지 안, 다이얼로그가 아닌)를 그 키보드 위로 스크롤
  합니다.** 가려지는 영역은 `visualViewport` 기준으로 계산합니다 — 안정 높이
  대비 임계값이나 고정 상수에 기대지 않는다는 점에서 §10이 다이얼로그 배치에
  이미 요구하는 것과 같은 원칙입니다. **키보드가 닫혀도 뷰포트는 이전 위치로
  돌아가지 않습니다.** 사용자가 그 자리로 되돌려 달라고 요청한 적이 없고,
  요청하지 않은 이동을 강제하면 인터페이스가 사용자와 씨름하는 것처럼
  느껴집니다. 예약해 둔 패딩도 닫히자마자 걷어내지 않고, 지금 스크롤 위치가
  그 여백에 아직 기대고 있는 만큼만 남기며 **지연 해제**합니다 — 한 번에
  걷어내면 브라우저가 스스로 `scrollTop`을 새 최댓값으로 clamp해 화면이 뚝
  떨어지기 때문입니다. 다이얼로그는 이 규칙의 대상이 아닙니다 — 다이얼로그의
  배치는 §10을 따릅니다.
- **얼마나 스크롤할지는 비주얼 뷰포트가 멈춘 뒤에 한 번만 잽니다.** 키보드가
  올라오는 동안 브라우저가 레이아웃을 줄이지 않고 **비주얼 뷰포트를 패닝**하는
  플랫폼이 있습니다(안드로이드). 그 패닝 자체가 필드를 드러내므로, 패닝이 끝나기
  전 지오메트리로 재서 스크롤하면 브라우저와 **이중으로** 밀어 올립니다. 닫을 때
  되돌리지 않는다는 위 규칙 때문에 그 초과분은 영구히 남고, 열고 닫기를 반복할
  때마다 페이지가 조금씩 위로 밀립니다. 그래서 뷰포트 변화가 멎을 때까지 기다렸다
  마지막 한 번만 계산합니다 — 기다리는 동안 브라우저의 패닝이 이미 필드를
  보여 주고 있으므로 지연으로 느껴지지 않습니다. 패닝하지 않는 플랫폼은 리사이즈가
  곧 끝나므로 사실상 즉시입니다. **키보드가 올라온 채로 다른 필드로 포커스가
  옮겨갈 때도 같은 규칙입니다** — 그때도 키보드 높이가 바뀌며 다시 패닝될 수
  있으므로, 이 창구를 거치지 않는 경로가 하나라도 있으면 그 문으로 같은 누적이
  들어옵니다.
- **닫히는 동안에는 스크롤 호스트의 높이도 붙듭니다.** 인셋을 유지하는 것만으로는
  부족합니다 — 브라우저가 강제하는 스크롤 상한은 `scrollHeight - clientHeight`라,
  높이가 `100dvh`면 닫히는 전환 중 `clientHeight`가 부풀어 읽히는 것만으로 상한이
  줄고 뷰포트가 요청 없이 깎입니다. 그래서 그 창구 동안에는 직전까지 관측된
  높이를 그대로 고정합니다(관측값과 열려 있을 때의 값 중 **작은 쪽** — 큰 쪽을
  고정하면 부풀린 값을 붙드는 셈이 됩니다).
- **닫히는 렌더에서는 아무것도 계산하지 않습니다.** 직전 열림 인셋을 그대로
  붙들고 있다가, 지오메트리가 안정된 뒤(~120ms) 남길 여백을 처음 한 번 재고
  거기서부터 지연 해제를 시작합니다. 스크롤 호스트가 `100dvh`라, 키보드가
  닫히는 전환 도중 `clientHeight`가 실제보다 큰 뷰포트 기준으로 한 프레임
  부풀려 읽힙니다(실기기 트레이스: 실제 928인데 1192로 읽고 22ms 뒤 스스로
  교정). 그 값을 믿고 여백을 줄이면 `scrollHeight`가 함께 줄어 브라우저가
  `scrollTop`을 clamp합니다 — 위와 같은, 요청한 적 없는 이동입니다.
- **키보드 위로 올리는 대상은 기본이 "포커스된 필드"지만, 소비 앱이 그 필드를
  감싸는 조상에 `data-keyboard-keep-visible`을 붙이면 그 블록 전체입니다.**
  필드 아래에 취소·삭제·저장처럼 같이 보여야 하는 줄이 있을 때 씁니다. 추론이
  아니라 **명시적 opt-in**입니다 — 어떤 블록이 함께 보여야 하는지는 킷이 알 수
  없습니다. 두 규칙이 이 확장을 양쪽에서 묶습니다.
  - **한도** — 블록이 키보드 위 공간보다 크면, **마커 때문에 추가로** 올리는
    몫은 포커스된 필드 자신의 윗변이 보이는 영역 밖으로 밀려나지 않는 선까지만
    입니다. 지금 타이핑하는 자리를 못 보는 것이 버튼 하나 가려지는 것보다
    나쁩니다.
  - **바닥** — 마킹은 스크롤을 **늘릴 수만 있습니다**. 마킹하지 않았을 때
    보장되던 양보다 덜 스크롤하는 결과가 나와서는 안 됩니다. 한도만 두면 필드
    자신이 이미 보이는 영역보다 큰 경우(자라는 텍스트영역) 마킹이 오히려 필드
    바닥을 가리는 역효과가 납니다.
  - **둘이 충돌하면 바닥이 이깁니다.** 필드 자신이 이미 보이는 영역보다 커서
    최소 요구량이 한도를 넘는 경우가 그렇습니다 — 그때는 필드 윗변이 화면 밖으로
    나가더라도 아래쪽(캐럿 쪽)을 지킵니다. 즉 위 한도는 "마커가 만드는 추가분"의
    상한이지, 최종 스크롤량의 상한이 아닙니다.
  - **마커는 필드의 조상에, 그리고 스크롤 호스트 안쪽에 붙여야 합니다.** 필드
    자신에 붙이거나 스크롤 호스트 바깥 조상(예: `document.body`로 포털된
    다이얼로그 안)에 붙이면 **조용히 무시됩니다** — 이 규칙이 움직이는 것은
    스크롤 호스트의 스크롤 위치뿐이라, 그 바깥 요소는 애초에 기준이 될 수 없기
    때문입니다.
- 로그인 카드는 키보드가 열려도 제자리 세로 위치와 여백을 유지합니다.
  상단 정렬 압축 레이아웃으로 바뀌지 않습니다.
- 다이얼로그는 **지금 보이는 영역의 정중앙**에 놓입니다. 키보드가 열리면 그 영역이
  키보드 위까지로 줄어들 뿐, 규칙은 그대로입니다.
- `env(safe-area-inset-*)`를 하단 고정 요소와 작업 영역 패딩에 반영합니다.

## 10. 팝오버와 다이얼로그

- 작은 정보 표면은 `.glass-popover`와 뷰포트 클램프 배치를 재사용합니다.
  클릭형 디스클로저는 바깥 조작·포커스 이탈·Escape·같은 트리거 재클릭으로 닫힙니다.
  호버 전용 오버레이는 주변 레이아웃 높이를 바꾸면 안 됩니다.
- 작업 다이얼로그는 고정 백드롭, z-index 200, 58% 검정 틴트, 8px 블러,
  12px 반경, 뷰포트 가장자리 20px 여유를 공유합니다.
- 긴 다이얼로그는 내부에서 `overflow-y: auto`로 스크롤하고 액션은 바닥에 sticky.
- 모든 다이얼로그는 `role="dialog"`, `aria-modal="true"`, 고유한 접근성 이름을 갖습니다.

- 다이얼로그는 **포커스를 안에 가둡니다.** Tab이 마지막 요소를 넘어가면 첫 요소로
  돌아오고, 닫히면 열기 직전 요소로 포커스를 되돌립니다. 모달인데 뒤 콘텐츠로
  포커스가 새면 스크린 리더 사용자는 자기가 어디 있는지 알 수 없습니다.
- **열면 바로 첫 입력칸에 포커스를 줍니다.** 곧바로 타이핑할 수 있어야 합니다.
  특정 요소를 잡아야 하면 그 요소에 `autoFocus`를 주세요.
- **"키보드가 열렸나"를 판정하지 마세요. 백드롭을 보이는 영역에 맞추세요.**
  백드롭의 `top/left/width/height`를 `visualViewport`에 맞춰 넣으면, 키보드가
  열릴 때 영역이 알아서 줄어들고 다이얼로그는 그 안에서 가운데 놓입니다.
  위아래 여백도 자동으로 생기고, 키보드를 가릴 수 없습니다.
  - 임계값 방식(안정 높이 대비 N px 축소)은 **주소창이 접히고 펴지는 높이**가
    휴대폰에서 100~140px이라 키보드와 구분되지 않아 오판합니다.
  - `:has(input:focus)` 류는 더 나쁩니다 — 프로그램 포커스만으로 참이 되고,
    사용자가 뒤로가기로 키보드만 내려도 계속 참입니다.
  - 백드롭이 `position: fixed`인데도 필요한 이유: 안드로이드 기본값
    (`resizes-visual`)에서 키보드는 레이아웃 뷰포트를 줄이지 않아 fixed 요소가
    키보드 뒤까지 뻗습니다. `100dvh`도 키보드를 반영하지 않습니다.
- **넘칠 때는 가운데가 아니라 위에 붙여야 합니다.** `align-items: safe center`를
  쓰세요. 그냥 `center`면 다이얼로그가 영역보다 클 때 위쪽이 잘려 나가 스크롤로도
  닿을 수 없습니다. 다이얼로그에 `max-height: 100%` + `overflow-y: auto`를 줘
  영역을 넘지 않고 안에서 스크롤되게 하면, 키보드는 계속 보입니다.
- 백드롭 닫기는 **`mousedown`으로 판정합니다.** `click`으로 하면 다이얼로그 안에서
  드래그를 시작해 백드롭에서 손을 떼는 것만으로 닫혀 버립니다.
- **닫기 제스처는 주 버튼(`button <= 0`)만 인정하세요.** 이건 백드롭뿐 아니라
  바깥 클릭으로 닫는 **모든** 팝업에 적용됩니다(`isPrimaryButton` 헬퍼).
  마우스의 뒤로/앞으로·가운데 버튼도 `mousedown`·`pointerdown`을 일으키기 때문에,
  가드가 없으면 **뒤로가기 버튼 한 번에 두 가지가 겹칩니다**: 팝업이 먼저 닫히면서
  정리 코드가 history 표식을 써버리고, 뒤이어 브라우저의 뒤로가기가 그 표식을
  못 찾아 **페이지를 나가버립니다.** 커서가 팝업 안이었는지 밖이었는지에 따라
  결과가 갈려서 "포커스 때문인가" 같은 엉뚱한 방향으로 새기 쉽습니다.
- 파괴적 확인 다이얼로그는 백드롭 닫기를 끄고 명시적 버튼만 남깁니다.
- **뒤로가기는 뒤 페이지가 아니라 다이얼로그를 닫습니다.** 열 때 history에 표식을
  push 해 두고 `popstate`에서 그 표식이 사라졌으면 닫습니다. 사용자가 버튼으로
  닫았을 때는 표식을 걷어내야 뒤로가기 횟수가 밀리지 않습니다. 여러 겹 쌓을 수
  있으므로 표식은 스택으로 관리하고, 뒤로가기는 맨 위 것부터 닫습니다.
  - 모바일에서 키보드가 떠 있으면 **첫 뒤로가기는 OS가 키보드를 닫는 데** 씁니다
    (브라우저에 `popstate`가 오지 않습니다). 다이얼로그는 그다음에 닫힙니다.
  - 정리 단계의 `history.back()`은 **한 틱 미뤄서** 부르고, 곧바로 다시 마운트되면
    취소하세요. StrictMode는 effect를 mount → cleanup → mount로 돌리는데, 즉시
    부르면 재마운트 직후 `popstate`가 도착해 다이얼로그가 열리자마자 닫힙니다.
  - **표식 push는 `useLayoutEffect`에서 해야 합니다. `useEffect`면 안 됩니다.**
    크롬은 사용자 제스처 없이 추가된 history 항목에 "뒤로가기에서 건너뛰기"
    표시를 답니다(history manipulation intervention). `useEffect`는 페인트 이후
    별도 태스크라 팝업을 연 클릭의 태스크가 이미 끝나 있어 그 표시가 붙고,
    뒤로가기가 항목을 건너뛰어 **이전 페이지로 나가버립니다.**
    `useLayoutEffect`는 클릭과 같은 태스크 안에서 커밋되므로 제스처 안에서
    push됩니다. 증상이 헷갈리는 이유: 크롬은 **사용자가 아무 데나 다시 클릭하면
    그 표시를 해제**하므로, "입력칸 포커스 상태에선 나가고, 딴 데 한 번 누르면
    잘 닫힌다"처럼 보입니다.
- 다이얼로그 안의 드롭다운은 **포털을 켜세요.** 백드롭이 z-index 200이라
  포털 메뉴(450)만 그 위로 올라옵니다. 포털 메뉴는 백드롭 바깥에 렌더되므로
  메뉴를 눌러도 다이얼로그가 닫히지 않습니다.

구현: `src/Dialog.tsx`(`Dialog` · `DialogHeading` · `DialogActions`),
`css/dialog.css`(`.dialog-backdrop` / `.dialog` / `.dialog-wide` /
`.dialog-scroll` / `.dialog-actions`).

## 11. 접근성

- `ariaLabel`이 **필수인 컨트롤과 선택인 컨트롤이 갈립니다.** 여기 한동안 "모든 커스텀
  컨트롤에 필수입니다. 선택적이지 않습니다"라고 적혀 있었는데, **코드가 한 번도 그렇지
  않았고**(넷이 `ariaLabel?`이었습니다) 그 규칙은 스스로를 정당화하지도 못했습니다 —
  셋을 같은 이유로 묶을 수가 없습니다. 가르는 기준은 **이름이 없을 때 무엇이 망가지는가**
  하나입니다.
  - **필수 — 이름이 화면에 그려지거나, 한 화면에 같은 이름이 여럿 생길 수 있을 때.**
    `Dialog` · `SectionTabs` · `Select` · `DateWheelPicker`.
    휠 피커가 이 규칙이 왜 있는지를 그대로 보여줍니다: `ariaLabel`이 팝오버 머리말로
    **눈에 보이게 그려집니다**(`.wheel-heading`). 안 넘기면 스크린리더가 아니라
    **화면이** 망가집니다 — 한 폼의 날짜 필드가 전부 같은 머리말을 답니다. 그리고 이름이
    겹치면 `getByRole`이 "Found multiple elements"로 터져 **소비자의 테스트가** 먼저
    막힙니다. 둘 다 보조기술과 무관한 비용입니다. 네 컨트롤 모두 `ariaLabel: string`
    (물음표 없음)이라, 값을 아예 안 넘기는 실수는 손으로 볼 필요 없이 `npm run typecheck`가
    그 자리에서 잡습니다.
  - **선택 — 감싸는 `<label>`이 이미 이름을 주거나, 순수하게 `aria-label` 속성으로만
    나가고 화면에 아무 자국도 남기지 않을 때.** `AutoGrowTextarea`와 `MobileQuickBar`.
    앞엣것은 실사용에서 `<label>메모<AutoGrowTextarea/></label>` 꼴로 쓰여 암묵적 label이
    이미 이름을 주고, 뒤엣것은 화면당 하나뿐이라 겹칠 대상이 없습니다.
    ⚠️ **`AutoGrowTextarea`가 `id`를 받게 되면서 이 "선택"에 함정이 하나 생깁니다** —
    `id` + 바깥 `<label htmlFor>`로 이름을 주면서 `ariaLabel`까지 넘기면, `aria-label`이
    `<label>`을 **이겨서** 화면에 보이는 글자와 읽히는 이름이 갈립니다. 이름을 주는
    방법은 감싸는 `<label>` · 바깥 `<label htmlFor>` · `ariaLabel` 셋 중 **하나만**입니다.
  **휠 피커에서만은 `ariaLabel`이 이름의 전부가 아니라 접두사입니다** — 아래 별도 조항.
- **보이는 머리말과 접근성 이름은 갈라놓을 수 있습니다.** 휠 피커의 `heading`이
  그 자리입니다. 292px 팝오버 머리말에는 `"날짜"`가 맞고 이름으로는 `"거래 발생 날짜"`가
  맞는데, 안 나눠 두면 한쪽을 맞추는 순간 다른 쪽이 망가집니다. 기본값은 `ariaLabel`이라
  안 쓰면 지금과 똑같이 동작합니다.
- 커스텀 드롭다운은 `aria-haspopup="listbox"` + `aria-expanded` 트리거와
  `role="listbox"` / `role="option"` / `aria-selected` 메뉴로 구성합니다.
- 휠 피커의 각 열은 `role="group"`에 현재 값이 포함된 이름을 갖습니다.
  **열은 포커스를 받지 않습니다** — 키보드 위/아래를 받는 것은 트리거이고, 이 컨트롤의
  tab 정거장은 트리거 하나입니다(아래 키 표).
- 탭은 `role="tablist"` / `role="tab"` / `aria-selected`.
- 포커스 링은 3px 강조색 아웃라인입니다. `outline: none`만 남기고 대체 표시를
  주지 않는 코드는 넣지 마세요.

**휠 피커 트리거의 접근성 이름은 `ariaLabel` 뒤에 지금 보이는 값을 잇습니다** —
`"거래 날짜, 2026. 07. 12."`. 값이 비면 placeholder를 잇습니다(`"거래 날짜, 날짜 선택"`).
`ariaLabel`이 필수인 것은 그대로이고, 없애는 것이 아니라 앞에 남는 것입니다.
포커스가 트리거를 떠나지 않으므로(아래 키 표) 값을 읽히게 할 자리가 여기밖에
없습니다 — `ariaLabel`만 이름으로 쓰면 **값이 바뀌어도 읽히는 것이 하나도 없습니다.**

- 이름은 화면과 **같은 출처**에서 만듭니다. 그래서 둘이 갈라질 수 없고, 대신 숫자를
  치는 동안 **매 타마다 이름이 바뀝니다.** 그것이 이 방식의 값이자 비용입니다.
- **자리를 채우는 `‒`(U+2012)만은 이름에서 뺍니다** — `"거래 날짜, 20. 07. 12."`.
  그 문자를 고른 이유는 **오직 어드밴스 폭**이고(§5의 자리를 지키는 표시), 빈 자리마다
  반복되므로 네 자리 연도를 치는 동안 이름이 정보 없이 길어졌다 짧아집니다.
  **화면은 그대로 그립니다** — 빼는 것은 이름 쪽 하나뿐입니다.
- ⚠️ **읽히는 것은 "값"이지 "활성 세그먼트"가 아닙니다.** 지금 어느 세그먼트를 치고
  있는지는 안 읽힙니다. `aria-activedescendant`는 `role="button"` 트리거에서 정식
  지원되지 않아 `role="combobox"` 이관이 필요한데, 그 이관은 아래에서 `Select`가
  **실기기 측정 후 기각한** 경로입니다. **알면서 남기는 구멍**이고 별건입니다.

### 커스텀 컨트롤의 키보드 조작

`Select`는 WAI-ARIA listbox 패턴을 따릅니다. 옵션은 **tab 순서에 개별로 들어가지
않습니다** — 옵션이 50개인 컨트롤에서 다음 필드까지 Tab을 50번 누르게 되기 때문입니다.

| 상태 | 키 | 동작 |
|---|---|---|
| 닫힘 | `↓` `↑` `Enter` `Space` | 연다. 활성 = 현재 값, **현재 값이 없거나 비활성이면** 첫 선택 가능한 옵션 |
| 열림 | `↓` `↑` | 다음/이전 **선택 가능한** 옵션. **끝에서 감싸지 않습니다** |
| 열림 | `Home` `End` | 첫/마지막 선택 가능한 옵션 |
| 열림 | `Enter` `Space` | 선택하고 닫고 포커스를 트리거로 |
| 열림 | `Escape` | 값을 바꾸지 않고 닫고 포커스를 트리거로 |
| 열림 | `Tab` | 닫고 포커스가 트리거를 거쳐 다음 요소로 |

`DateWheelPicker`는 네이티브 `<input type="date">`를, `TimeWheelPicker`는
`<input type="time">`을 비춥니다 — **세그먼트는
팝오버의 열이 아니라 트리거에 있고**, 그래서 팝오버를 열지 않아도 값을 고칠 수 있습니다.
세그먼트 개수가 `fields`에 따라 1~3개로 달라지므로, 아래 표는 "연·월·일"이 아니라
**첫 세그먼트 / 마지막 세그먼트**로 규칙을 씁니다.

**키는 언제나 트리거가 받습니다. 팝오버가 열려 있어도 그렇습니다** — 팝오버 안에는
tab 정거장이 하나도 없고, 포커스가 그 안으로 들어가는 일도 없습니다. 그래서 아래 표는
"닫힘·열림" 행이 대부분이고, **상태로 갈리는 키는 세 무리뿐입니다.** 셋 다 "팝오버가
없으면 할 수 없는 일"이라는 한 가지 이유로 갈립니다 — `Enter`·`Space`(확정할 것이
열려 있어야 있음), `↑`·`↓`(열 것이 없으면 값을 움직임), `Escape`(닫을 것이 있음).

| 상태 | 키 | 동작 |
|---|---|---|
| 닫힘·열림 | `0`~`9` | 활성 세그먼트에 입력. 자릿수가 차면 **다음 세그먼트로 저절로** |
| 닫힘·열림 | `Backspace` | 입력 버퍼에서 한 자리 지우기 |
| 닫힘·열림 | `→` | 다음 세그먼트. **마지막에서는 제자리** |
| 닫힘·열림 | `←` | 이전 세그먼트. **첫 번째에서는 제자리** |
| 닫힘·열림 | `Tab` `Shift+Tab` | **버퍼를 확정하고, 열려 있으면 닫고, 컨트롤을 떠남** |
| 닫힘·열림 | `Delete` | 값 전체 비우기. **`allowClear`일 때만** |
| 닫힘·열림 | `Ctrl+;` (macOS `Cmd+;`) | 오늘로 설정 |
| 닫힘·열림 | `Ctrl+C` | **화면에 보이는 그대로** 클립보드에 복사. 치던 버퍼는 먼저 확정 |
| 닫힘·열림 | `Ctrl+V` | 클립보드를 읽어 값으로. **읽을 수 없으면 아무것도 안 함**(지우지 않음) |
| 닫힘·열림 | `Ctrl+Z` | 마지막 **조작** 하나 되돌리기. 다시 하기는 없음 |
| 열림 | `Ctrl+S` | **완료** — 값 확정, 닫음. **닫힘에서는 안 먹음** |
| 닫힘 | `↓` `↑` `Enter` `Space` | **연다.** 활성 세그먼트도 치던 버퍼도 그대로 둠 |
| 닫힘 | `Escape` (버퍼 있음) | 버퍼를 버림. **전파를 멈춤** |
| 닫힘 | `Escape` (버퍼 없음) | **아무것도 하지 않고 전파시킴** |
| 열림 | `↑` `↓` | 활성 세그먼트의 값 ±1 |
| 열림 | `Enter` `Space` | **완료** — 값 확정, 닫음 |
| 열림 | `Escape` | 버퍼를 버리고, 값을 바꾸지 않고 닫음 |

**이 표의 "버퍼"는 언제나 화면에 보이는 버퍼입니다.** 소비자가 런타임에 `fields`를
줄이면 사라진 세그먼트의 버퍼가 상태에 남을 수 있는데, 그것은 **없는 것으로 셉니다** —
안 그러면 사용자가 취소할 것이 화면에 하나도 없는데 `Escape`가 먹힙니다. 판정은
"상태에 버퍼가 있는가"가 아니라 **"지금 보이는 세그먼트의 버퍼인가"**입니다.

**`Home`/`End`는 일부러 뺐습니다.** 드롭다운에는 있지만 그쪽은 목록이라 처음과 끝이
있습니다. 연도 세그먼트에는 끝이 없으므로 뜻이 정해지지 않습니다. 누락이 아닙니다.

**드롭다운과 휠 피커의 `Tab`은 이제 같습니다 — 둘 다 컨트롤을 떠납니다.** 여기
한동안 "둘이 다른 것은 의도"라고 적혀 있었고, 그 전제는 **세그먼트가 팝오버 안에
있다**는 것이었습니다. 세그먼트가 트리거로 올라오면서 자리를 옮기는 일은 `←`/`→`가
전담하게 됐고, `Tab`이 안에 머무를 이유가 사라졌습니다. 그래서 이 컨트롤도 폼에서
**정거장 하나**입니다 — 날짜 필드 셋을 훑는 데 `Tab`을 아홉 번 누르지 않습니다.
**`Tab` 하나에 대한 이야기입니다** — 나머지는 여전히 다릅니다(열림 `↑`/`↓`가
한쪽은 옵션을 고르고 한쪽은 값을 ±1 하며, 휠 피커에는 `Home`/`End`가 없습니다).

**방향키는 안에서 돌아다니는 키, `Tab`은 떠나는 키입니다.** 마지막 세그먼트에서 `→`는
제자리이고, `Tab`은 컨트롤을 떠나면서 열려 있던 팝오버도 닫습니다. (닫는 길이 `Tab`
하나라는 뜻은 아닙니다 — `Enter`·`Space`·`완료`·`Escape`·바깥 클릭도 닫습니다.)

**닫힌 날짜 필드가 치던 숫자를 들고 있으면 첫 `Escape`를 먹습니다.** 이것은 위 표가
정한 계약이지만 **대가를 치르는 쪽이 바깥 오버레이**라 여기 따로 적습니다 —
다이얼로그를 `Escape`로 닫으려던 사용자가 한 번 헛치고, 두 번째에 닫힙니다.
§10의 겹침 규칙("가장 안쪽 하나만 닫힘")은 `useEscapeToClose`의 **깊이 스택**이
정하는데, 닫힌 날짜 필드는 그 스택에 **등록조차 되어 있지 않습니다.** 트리거의
`onKeyDown`(React 합성 이벤트)이 document 리스너보다 먼저 실행되고, 거기서 전파를
멈추기 때문입니다. 버퍼가 없으면 아무것도 하지 않고 그대로 흘려보냅니다 — **이쪽을
빠뜨리면 날짜 필드에 포커스가 있는 동안 다이얼로그가 `Escape`로 안 닫힙니다.**

`Ctrl`이나 `Meta`가 눌린 키는 처리하지 않습니다 — 브라우저·OS 단축키를 가로채지
않기 위해서입니다. `Alt`는 검사하지 않으므로 네이티브 `<select>` 습관대로 `Alt+↓`를
눌러도 열립니다.

**예외는 휠 피커의 `Ctrl+;`(오늘) 하나입니다.** 이 규칙의 이유는 "이미 쓰이는
조합을 뺏지 말 것"인데, `Ctrl+;`는 스프레드시트가 "오늘 날짜 입력"으로 쓰는 관례이고
주요 브라우저에서 비어 있는 조합이라는 것이 이 예외의 근거입니다. **Windows에서
실기기로 확인했습니다(2026-08-09)** — `Ctrl+;`는 브라우저가 가져가지 않고 페이지에
도달합니다. **macOS `Cmd+;`도 확인했습니다(2026-08-11) — 동작합니다.**
그전까지 안 되던 원인은 **조합이 아니라 포커스**였습니다: 맥은 `mousedown`의 기본
동작으로 버튼에서 포커스를 걷어내서 `Cmd+;`는 물론 방향키·숫자까지 전부 안 닿고
있었습니다(캡처의 `tgt=body`, `처리됨=N`). 트리거가 `click`에서 포커스를 직접
가져오도록 고치자 같은 캡처에서 바로 동작했습니다 — 설계 스펙 §6.4.

**`Ctrl+C`·`Ctrl+V`·`Ctrl+Z`는 뺏는 것이 아니라 없는 것을 채우는 것입니다.** 이 규칙이
막으려는 것은 "이미 뜻이 있는 조합을 가로채는 것"인데, 이 셋은 **네이티브 입력 필드에서
바로 그 뜻**입니다. 이 컨트롤의 필드는 `<span>`이라 고를 텍스트가 없어 브라우저가 그
뜻을 실행할 대상이 없고, 그래서 구현하지 않으면 **아무 일도 안 일어납니다** — 사용자
입장에서는 "날짜 필드에서만 복사가 안 되는" 결함입니다.

**진짜 예외는 `Ctrl+S` 하나입니다.** 이건 브라우저의 "페이지 저장"을 실제로 가져갑니다.
`Ctrl+;`처럼 실기기로 확인하고 뺏은 것이 아니라 **아직 확인하지 못한 채** 뺏은 것이라,
둘을 같은 줄에 두면 안 됩니다 — 폰에는 `Ctrl` 키가 없어 이 저장소가 쓰던 실기기 확인
방법을 그대로 쓸 수 없습니다. **데스크톱 확인 항목으로 남아 있습니다.** 대신 범위를
좁혔습니다: 팝오버가 **열려 있을 때만** 먹으므로, 필드에 포커스가 있어도 닫혀 있으면
`Ctrl+S`는 브라우저 몫입니다.

**이 넷은 `KIT_RESERVED`에 등록돼 있습니다**(`src/shortcuts.ts`). 예약 비교가
`Shift`·`Alt`·`Meta`를 접으므로 소비자 앱은 `Ctrl+Shift+Z`(다시 하기)를 포함해 이 넷의
모든 변형을 액션에 걸 수 없습니다 — 안전한 쪽으로 넓게 틀리는 쪽을 택한 대가입니다.
**"조합이 막혔나"를 파기 전에 "키가 도착은 하나"를 먼저 봤어야 했습니다.** 판정은 `event.code === "Semicolon"`으로
합니다 — 배열에 따라 `;`가 Shift 조합이 되는 키보드가 있습니다.

**포커스만으로는 열지 않습니다.** 선택 직후 포커스가 트리거로 돌아오므로 포커스가
여는 조건이면 곧바로 다시 열리고, 열 때마다 history에 표식이 쌓입니다.

고를 수 없는 옵션은 **눈으로 구분됩니다**(흐린 처리 + `not-allowed` 커서). 방향키가
건너뛰는 항목이 똑같이 생기면 건너뛰기가 결함으로 보이기 때문입니다. 고를 수 있는
옵션이 아예 없으면 컨트롤 자체가 비활성이므로(§3) 위 표의 어느 키도 닿지 않습니다.

**활성 옵션은 진짜 포커스를 받습니다**(roving tabindex) — 활성 옵션만 `tabindex="0"`을
갖고 나머지는 `-1`입니다. 대안이던 `aria-activedescendant`(포커스를 트리거에 두고
id로 가리키는 방식)는 실기기 측정 후 기각했습니다. 두 방식 모두 키보드에서는 옳게
동작했지만, roving은 강조를 기존 `:focus-visible` 규칙 하나로 처리해 새 CSS 상태를
만들지 않고, `role="button"` 트리거가 ARIA상 `aria-activedescendant`를 정식 지원하지
않아 다른 쪽은 `role="combobox"` 이관까지 필요했습니다.

**터치 기기에서 탭으로 열면 활성 옵션이 강조되지 않는 것이 정상입니다.** 브라우저는
스크립트가 옮긴 포커스에 `:focus-visible`을 주지 않으며, 마지막 입력이 터치였다면
더욱 그렇습니다. 키보드가 없는 기기에는 "키보드 활성 옵션"이라는 상태 자체가 없고,
그때 보이는 것은 선택된 값의 조용한 처리(§3 옵션 상태)입니다.

## 12. 모션

- 빠른 상태 전환은 `--motion-fast`(140ms), 셰브런 회전은 160ms,
  휠 이동은 210ms, 떠 있는 바는 160/210ms.
- **날짜 픽커 팝오버의 진입(`wheel-enter`)은 280ms**이고 열마다 40ms 시차를 둡니다
  (총 360ms). 휠이 한 행만큼 굴러 내려와 멎습니다 — 오너가 "드르륵"이라고 부른 것입니다.
  ⚠️ **이동(210ms)과 길이·커브를 일부러 다르게 둡니다.** 속도를 올려 달라는 요청이
  왔을 때도 이 간격은 지켰습니다 — 지속시간과 시차에서 **나눠** 깎았습니다.
- **진입처럼 "끝나는 시각"을 CSS와 JS가 나눠 갖는 값은 테스트가 한쪽에서 유도해**
  **비교하게 하세요.** 두 곳에 같은 수를 적어 두면 조용히 갈라지고, 그 어긋남은
  화면에서 "가끔 애니메이션이 이상하다"로만 보입니다.
- **진입과 이동은 서로 구분돼야 합니다. 이것이 계약입니다.** 가르는 축은 넷입니다:
  **모든 열이 함께 움직인다**(이동은 한 열만 — 눈에 가장 먼저 들어오는 차이) · 다른 이름 ·
  다른 게이트 · 다른 길이와 커브. **대상은 가를 수 없습니다** — 굴러야 하는 것이 행이라
  둘 다 같은 요소에 걸리고, 대신 **이동 규칙을 파일에서 뒤에 두어** 커밋 프레임에서
  이동이 이기게 합니다.
  이 구분이 흐려졌던 동안 **"아무것도 안 움직인 열림에서 이동 신호가 재생되는"** 결함이
  있었습니다(오너가 그것을 기능으로 보고 리포트했습니다). 한쪽을 손볼 때 다른 쪽과
  같아지지 않는지 보세요.
- **예외: 사이드바 선택 아이콘 튐(`scale(.82 → 1.14 → 1)`, 320ms, §8)은 이 절의
  다른 값보다 깁니다.** 사이드바가 접히면 라벨이 사라지므로, 이 튐이 선택
  피드백으로 남는 유일한 신호이기 때문입니다.
- `prefers-reduced-motion: reduce`에서 **이동과 확대를 모두 제거**합니다.
  단순히 duration을 줄이는 것이 아니라 transform/animation 자체를 끕니다.

### 완료(커밋) 피드백

`Select`와 `DateWheelPicker`는 값이 **실제로 바뀐 채 확정됐을 때만** 트리거 값에
신호를 켭니다 — "적용은 되는데 바뀌는 순간이 티가 안 난다"는 오너 리포트에 대한
응답입니다. `css/surfaces.css`의 `.dropdown-value-commit`이 그 신호이고, 230ms
`cubic-bezier(.18, 1.15, .35, 1)`(휠의 `wheel-selected-pop`과 같은 커브) 위에서
`opacity`·`transform: translateY`·`color`가 한 타임라인으로 함께 움직입니다. `color`는
65% 지점(약 150ms)에 먼저 강조색에서 제자리로 돌아오고, `opacity`/`transform`은
100%까지 계속됩니다 — 색과 이동이 동시에 끝나면 마지막 순간에 "반짝임과 자리잡음"이
겹쳐 보여 무엇이 신호인지 흐려지므로, 색을 먼저 끝내 두면 눈에 마지막으로 남는 건
값이 제자리에 앉는 움직임 하나뿐입니다.

**자기 `color`를 선언하는 자식은 그 신호에서 빠집니다 — 그러면 명시로 다시 받아야
합니다.** 위 타임라인은 컨테이너의 `color`를 애니메이션하고 자식은 그것을 **상속**받아
함께 반짝입니다. 자식이 자기 `color`를 선언하면 그 상속이 끊깁니다. 그래서 오랫동안
계약은 "값 안쪽 조각은 `color`를 선언하지 않는다"였고, `DateWheelPicker`의 활성 세그먼트
표시가 글자색이 아니라 배경으로만 그려진 이유가 그것이었습니다.

그 세그먼트가 **반전 칩**(라이트 = 어두운 칩에 흰 글자 / 다크 = 밝은 칩에 검은 글자)이
되면서 자기 `color`를 갖게 됐습니다. 한동안은 **명시로 펄스를 함께 걸어** 해결했는데,
표시가 "편집 중"에만 그려지도록 바뀌면서 **더 단순해졌습니다: 확정하며 닫는 것이 곧**
**편집의 끝이라 칩과 펄스가 겹치지 않습니다.** 펄스가 재생되는 순간 조각은 자기 색을
갖지 않은 상태여서 상속이 끊기지 않습니다.

> **계약:** 값 안쪽 조각은 `color`를 선언하지 않습니다. 선언해야 한다면 확정 펄스와
> **겹치지 않거나**, 겹친다면 이 타임라인을 **명시로 함께 받아야** 하고 그 규칙은 그
> 조각의 표시를 그리는 규칙과 **같은 조건으로 게이트**돼야 합니다(표시가 안 보이는
> 상태에서 색만 애니메이션되면 글자가 사라집니다).
>
> ⚠️ **"겹치지 않는다"는 CSS만 봐서는 알 수 없습니다 — DOM 테스트로 고정하세요.**
> 그리고 확정 신호 클래스가 **한 번 붙고 안 떨어지는** 종류라면(이 킷의
> `.dropdown-value-commit`이 그렇습니다) 명시 규칙은 확정과 무관하게 계속 매칭되어,
> 조각이 바뀔 때마다 엉뚱한 순간에 신호가 재생됩니다.

**무엇이 켜는가:** `Select`는 옵션을 골라 메뉴가 닫히는 순간(`choose()`).
`DateWheelPicker`는 **`Enter`·`Space`** 또는 완료 버튼으로 팝오버가 닫히는 순간
(`commitAndClose()`) — 이 둘만 "완료"입니다. (열린 상태의 `Space`도 확정입니다 —
§11의 키 표에 `Enter`와 한 행으로 있습니다.)

**무엇이 의도적으로 켜지 않는가:** 화살표 키, 휠, 스와이프, 숫자 타이핑(자릿수가 차서
즉시 확정되는 경우 포함), `오늘`·`비우기` 버튼. 휠 피커는 화살표 한 번, 스와이프
한 칸, 휠 한 칸마다도 값을 커밋합니다 — 그 경로마다 반짝이면 "완료됐다"는 신호가
아니라 조작할 때마다 뜨는 소음이 됩니다. 목록·휠을 훑는 동작과 "다 골랐다"를 시각적으로
구분하는 것이 이 신호의 존재 이유이므로, 훑는 동작 자체에 붙이면 그 구분이 사라집니다.

**확정된 값이 세션 시작 값과 같으면 신호가 없습니다.** 값이 안 바뀌었는데 반짝이면
"뭔가 바뀌었다"는 신호가 거짓이 되기 때문입니다. 어느 쪽도 **커밋하는 그 순간의
`value`**와 비교하지 않습니다(이유는 아래). 갈라지는 것은 **세션의 시작을 언제
찍느냐** 하나뿐입니다.

- **`Select` — 메뉴가 열린 그 순간의 `value`.** 고른 값이 그것과 같으면(이미 선택된
  옵션을 다시 고름) 신호가 없습니다.
- **`DateWheelPicker` — 트리거가 포커스를 얻은 순간(`focusin`)과 팝오버가 닫힌 순간,
  이 둘.** **여는 순간에는 절대 찍지 않습니다.** `commitAndClose`가 끝내 확정하는
  값(타이핑 버퍼를 반영한 값, 또는 빈 값을 채우는 `baseValue` 폴백, 또는 그대로인
  `value`)이 그 기준값과 같으면 신호가 없습니다.

**왜 휠 피커만 다른가 — 둘을 "통일"하지 마세요.** 휠 피커는 **팝오버를 열지
않고도 값이 바뀌는** 컨트롤입니다(§5·§11: 닫힌 채 숫자·`Delete`·`Ctrl+;`가
동작합니다). "열린 순간"에 찍으면 그 조작이 통째로 기준값에 흡수됩니다:

```
닫힌 채 2027 타이핑  →  value가 2027 (신호 없음 — 타이핑은 확정이 아니므로 정상)
Enter                →  팝오버가 열리며 기준값 = 2027   ← 방금 친 것이 기준이 됩니다
Enter (완료)         →  확정값 == 기준값  →  신호 없음
```

키보드만 쓰는 사용자가 — 닫힌 채 조작이 겨냥한 바로 그 사용자가 — 자기가 친 날짜에
대해 확정 신호를 **영영 못 봅니다.** 시작을 포커스로 옮기면 타이핑 **이전** 값이
기준이 되어 신호가 정상으로 돌아옵니다. `Select`는 트리거 포커스와 메뉴 열림이
사실상 같은 순간이고 닫힌 채로 값이 바뀔 길이 없으므로, 그쪽은 그대로 둡니다.

**닫힘에도 다시 찍는 이유는 따로 있습니다.** 안 찍으면 한 번 포커스한 동안 두 번째로
완료할 때 아무것도 안 바꿨는데 신호가 뜹니다(기준값이 아직 첫 진입 값이라). 닫힘에
찍으면 그 창이 닫히고, **닫힌 채 타이핑한 것은 다음 열림까지 기준값과 달라진 채
남습니다** — 그게 이 규칙이 지키려는 바로 그 경우입니다. **`focusout`에는 아무것도
하지 않습니다** — "컨트롤을 떠났다"와 "컨트롤 안에서 옮겼다"를 구분하지 못하는
이벤트라, 거기서 기준값을 버리게 만들면 세션 도중에 기준값이 사라집니다. 찍기만
하고 버리지 않으면 두 상태 모두에서 옳습니다.

**왜 커밋 시점이 아니라 세션 시작 시점과 비교하는가:** `DateWheelPicker`는 화살표
한 번, 휠 한 칸마다도 `onChange`를 불러 `value`를 곧바로 바꿉니다. "커밋하는 순간의
`value`"와 비교하면, 화살표로 다른 날짜로 옮긴 뒤 완료를 눌러도 그 사이 `value`가
이미 옮겨진 값으로 갱신돼 있어 "아무것도 안 바뀌었다"고 잘못 읽습니다 — 그러면 이
컨트롤은 화살표·휠로 값을 바꾼 뒤 완료해도 신호가 전혀 뜨지 않게 됩니다(타이핑
버퍼가 완료 순간에 플러시되는 드문 경우만 예외). 세션 시작 값과 비교해야 "이번
세션에 실제로 뭔가 바뀌었는가"를 묻는 것이 됩니다. `Select`는 방향키가 `onChange`를
부르지 않고 옵션을 고를 때 한 번만 커밋하므로 이 차이가 나지 않지만, **"커밋 시점이
아니라 세션 시작 시점과 비교한다"는 규칙 자체는 두 컨트롤이 공유합니다** — 위에서
갈라진 것은 그 시작을 언제 찍느냐 하나뿐입니다.

이 규칙의 알려진 결과: 값이 이미 오늘이면 `오늘` 버튼을 눌러도(오늘 버튼 자체는 애초에
신호를 켜지 않지만, 뒤이은 완료도 값이 세션 시작 값과 같으므로 켜지 않으므로) 아무
반응이 없습니다 — 오너에게 확인받은 의도된 동작이고, 보완하지 않습니다.

**`prefers-reduced-motion: reduce`에서는 이동(`translateY`)만 제거하고, 옅어짐
(`opacity`)과 색 신호(`color`)는 그대로 둡니다.** §12의 "이동과 확대를 모두 제거"
원칙을 따르되, 완료 피드백은 이동 하나만 전정기관 자극 대상이고 옅어짐·색은 대상이
아니므로 이동만 골라 뺍니다 — 이동이 없다고 신호 자체를 끄면 reduced-motion
사용자에게는 "완료돼도 티가 안 난다"는 원래 문제가 그대로 남습니다.

## 13. 가장자리는 맞춘다

> **요약:** 한 상자 안에 여러 줄이 쌓이면, 그 줄들의 양 끝은 같은 자리에서 끝납니다.

왜 그런지, 그리고 킷이 없는 환경에서 어떻게 지키는지는
[`LAYOUT-PRINCIPLES.md`](LAYOUT-PRINCIPLES.md) §1에 있습니다.

킷에서 이 규칙이 깨졌던 자리(오너 지적, 2026-08-11): 색상 카드의 값 칸이 `200px`로
**못 박혀** 있어서, 윗줄 액션 버튼은 카드 오른쪽 안쪽 끝에 붙는데 값 칸은 그보다 훨씬
앞에서 끝났습니다. 마지막 트랙을 `1fr`로 바꿔 고쳤습니다.

## 14. 킷은 기계를 주고, 앱이 정한다

> **요약:** 전역 기본값, 자주 쓰는 것, 그리고 나머지 전부를 여는 탈출구. 탈출구가 없으면 앞의 둘이 끝없이 늘어납니다.

규칙 전체와 그 근거는 [`LAYOUT-PRINCIPLES.md`](LAYOUT-PRINCIPLES.md) §6에 있습니다.

이 킷에서의 세 층은 이렇습니다:

1. **토큰** — 앱 전체 기본값. `--panel-min`처럼 `:root`에 두고, 상속되므로 어느 조상에
   걸든 그 아래만 바뀝니다.
2. **prop** — 그 통 하나만. `min`·`max`·`justify`처럼 자주 쓰는 것에만 둡니다.
3. **`className`** — 나머지 전부. **내보내는 컴포넌트는 예외 없이 받습니다**
   (`tests/classNameContract.test.ts`가 지킵니다).

**기본값은 "지금과 같음"이어야 합니다.** `--*-max: 1fr`, `--*-justify: normal`이 그래서
그 값입니다 — 아무도 안 쓰면 아무 일도 안 일어납니다.

**정책을 킷이 대신 정하지 않습니다.** 어느 패널이 나란히 설지, 패널 순서, 높이를 맞출지는
전부 내용을 아는 쪽만 판단할 수 있습니다. 킷은 통과 규칙을 주고 물러납니다.

---

## 15. `src/` 파일 이름과 자리

**파일 이름이 그 파일의 유일한 주제를 말합니다.** 대소문자가 종류를, 배럴의 묶음이
역할을 말합니다. 넓은 이름은 잡화점이 되므로 만들지 않습니다.

⚠️ 이 절에 `> **요약:**` 줄이 없는 것은 의도입니다 — 그 표식은
[`LAYOUT-PRINCIPLES.md`](LAYOUT-PRINCIPLES.md)의 규칙 줄을 글자까지 대조하는 **검사되는
사본**을 뜻하고 `tests/layoutPrinciples.test.ts`가 그 개수를 셉니다. 이 절은 대응하는
코어 문서 절이 없으므로 그 표식을 쓰면 뜻이 흐려집니다.

이 규칙들은 **처음부터 지켜지고 있던 관습**이었는데 어디에도 적혀 있지 않았습니다.
그동안 위반이 하나 생겼고(아래 규칙 3) 눈으로는 몇 달 동안 안 보였습니다.
`tests/moduleLayers.test.ts`가 규칙 1~3을, `tests/publicApi.test.ts`가 배럴의 공개
이름을 잽니다.

### 규칙 1 — 확장자와 첫 글자가 같은 것을 말합니다

```
PascalCase.tsx    컴포넌트를 내보내는 파일
camelCase.ts      컴포넌트가 아닌 것 — 순수 계산 · 훅 · DOM 헬퍼 · 상수
model/*.ts        값 모델(시점·기간). 화면도 DOM도 모릅니다
```

### 규칙 2 — 소문자 모듈은 컴포넌트를 내보내지 않습니다

훅과 컨텍스트는 `.ts`에 있어도 됩니다(`popupDismiss.ts`의 `PopupDepthContext`). 렌더하는
것이 생기면 그 순간 `.tsx`이고 이름이 대문자가 됩니다.

### 규칙 3 🔴 — 방향: 소문자 모듈은 컴포넌트 파일을 import 하지 않습니다

**타입이라도 안 됩니다.** `import type`은 런타임 비용이 0이라 무해해 보이지만, 층을
거꾸로 만들고 그 사실을 숨깁니다 — 실제로 `selectKeyboard.ts`가 `Select.tsx`에서
`SelectOption`을 가져오고 있었고, 그 파일 머리말은 *"표현을 고르기 전에 이 파일이
먼저 있었다"* 고 적고 있었습니다. 둘 다 참일 수 없습니다.

고치는 값은 대개 쌉니다: **그 계산이 실제로 읽는 필드만** 그 파일에 선언하면 구조적
타입이라 호출부는 그대로입니다. 공개 타입을 옮길 필요가 없습니다.

예외는 **배럴 하나**입니다 — `src/index.ts`는 공개 API 목록이라 모든 컴포넌트를 다시
내보내는 것이 그 파일의 일 전부입니다. 그래서 배럴에는 **재수출 말고 아무것도 두지
않습니다**(그 조건도 검사합니다).

### 규칙 4 — 심볼은 파일 이름이 설명하는 파일에 삽니다

판정 기준은 **"이 파일은 한 문장으로 설명된다"** 입니다. 설명에 "그리고"가 들어가면
파일이 둘입니다.

`isPrimaryButton`(마우스 버튼 번호 판정)이 `positioning.ts`(팝업 배치)에 있던 것이
그 예입니다 — 위치와 아무 관계가 없고, 파일 이름을 읽고는 거기 있을 것을 짐작할 수
없습니다. `SectionHeading`(페이지 안 섹션 제목)이 `SectionTabs.tsx`에 있던 것도
같습니다 — 탭이 아니고, §7의 배치 스택에서는 `PageHeader`·`Panel`과 한 줄에 섭니다.

⚠️ **넓은 이름을 새로 만들지 마세요.** `hooks` · `utils` · `helpers` · `common`처럼
무엇이든 들어갈 수 있는 이름은 **앞으로도 계속 자랄 자리**를 만듭니다. 한 함수짜리
파일이 잡화점보다 낫습니다.

### 아직 이 규칙을 안 지키는 자리 — 알면서 남겨 둔 것

적어 두지 않으면 "이미 정리됐다"로 읽힙니다.

| 자리 | 무엇이 어긋나는가 | 잰 이음매 |
|---|---|---|
| `controls/WheelPicker.tsx` (약 2235줄) | 규칙 4. 컴포넌트 안 최상위 선언 **87개**(들여쓰기 두 칸의 `const`·`function`·`let`) | 남은 조각 **둘**: 스와이프 3 · 홀드 7. ⚠️ 둘을 막고 있는 것은 `suppressColumnClickRef`가 **아니라** `swipeRef`입니다(아래).<br>클립보드는 **절반만** 나갔습니다 — 브라우저 접근은 바인딩 0이라 `browser/clipboard.ts`로 갔고, 남은 의미 절반은 `value`·`model`·`fields`·`hourDisplay`·`clampToRange`·`flushTyping`·`setTyping`·`clearedRef`·`undo`·`onChange` 열을 집습니다. **그건 단축키 계약이라 컨트롤에 있는 것이 맞습니다.**<br>나간 넷: 이동 계산 → `controls/wheelShift.ts` · 되돌리기 → `controls/undoStack.ts` · 열 모션 → `controls/columnMotion.ts` · 클립보드 접근 → `browser/clipboard.ts` |
| `model/instant.ts` (약 1000줄) | 규칙 4. 순수 함수 53개 | **3층이 이미 서 있습니다.** 아래층(값·단위+12시간)에서 위로 가는 화살표 **0**, 중간 여섯(step·range·typing·dateMath·display·paste)끼리 교차 **6개**. 계약 타입은 이미 `model/wheelModel.ts`로 나갔습니다 — 남은 것은 구현을 여섯으로 가르는 일이고, 그러려면 `model/instant/` 하위 폴더가 필요합니다(`model/`은 배럴 묶음이 아니라 예외 자리입니다) |
| `surfaces/PageChrome.tsx` (약 209줄) | 규칙 4. **파일 이름이 아무것도 안 내보냅니다** — `PageChrome`이라는 심볼이 없습니다 | 공개 아홉(컴포넌트 여덟 + `GridJustify`). **여덟 사이의 상호참조 0.** 유일한 묶음은 격자 셋(`SummaryGrid` 21줄 · `FieldGrid` 35 · `PanelGrid` 17)이 비공개 `trackStyle`(16줄)과 `GridJustify`를 함께 쓰는 것입니다. 나머지 다섯(`PageHeader` 13 · `SectionHeading` 12 · `SummaryCard` 28 · `Panel` 11 · `DismissibleDetails` 22)은 서로도, 격자와도 무관합니다.<br>📌 `DismissibleDetails`는 **저장소 안에서 아무도 안 씁니다**(`demo/`·`src/` 0건, 배럴만) — 지우는 것은 breaking이라 오너 판단 항목입니다 |

⚠️ **선언 개수는 세는 법을 같이 적습니다.** 여기 한동안 `91`이 적혀 있었는데 세는 법이
안 남아 있어 **같은 수가 다시 안 나옵니다**(지금 법으로 `v0.13.0`은 98). 재는 법 없는
숫자는 다음 사람에게 값이 0입니다 — 줄 수와 달리 검사도 못 겁니다.

🔴 **한 조각이 통째로 나가야 하는 것은 아닙니다.** 클립보드는 바인딩 8로 비싸 보였는데,
갈라 보니 **브라우저 접근 한 겹은 0**이었고 나머지 열이 값 모델과 단축키 계약이었습니다.
층이 둘이면 아래층만 떼는 것이 맞습니다 — 위층까지 끌고 나가면 바인딩이 **파라미터로
이름만 바꿔** 따라갑니다.

🔴 **바인딩 개수는 "밖에서 물어오는 것"만 셉니다 — "밖에서 써 넣는 것"은 안 셉니다.**
그래서 **스와이프는 3이 말하는 것보다 훨씬 엉켜 있습니다** — 밖에서 써 넣는 자리 셋은
전부 **`swipeRef`** 쪽입니다:

```bash
grep -on 'swipeRef.current = [^;]*' src/controls/WheelPicker.tsx
#  1116  armHold의 타이머        2219  열의 onPointerDown        2219  onPointerCancel
#  (2002 finishSwipe는 자기 것)
```

⚠️ **여기 한동안 `swipeRef`와 `suppressColumnClickRef`가 한 덩어리로 적혀 있었고, 그
덩어리 때문에 "홀드와 스와이프는 같이 봐야 한다"는 틀린 결론이 나왔습니다.** 갈라 재면:

```bash
grep -n 'suppressColumnClickRef.current' src/controls/WheelPicker.tsx
#  홀드      읽기 0 · 쓰기 1  (1115 — armHold 타이머 본문, onFire 페이로드 안)
#  스와이프  읽기 0 · 쓰기 3  (1911 · 1942 · 2008)
#  컴포넌트  읽기 2 (1997 가드 · 2219 onClickCapture) · 쓰기 2 (1997 rAF · 2219 onPointerDown)
```

**표식을 읽는 것은 컴포넌트뿐입니다 — 이 표식은 분리를 막지 않습니다.** 홀드의 쓰기 한 줄은
타이머 본문 세 줄 안에 있어 `onFire` 콜백과 함께 나갑니다.

**다음에 조각을 고를 때는 바인딩 수가 아니라 "이 상태에 밖에서 써 넣는 곳이 있는가"로
고르세요.** 되돌리기가 먼저 나온 이유가 그것입니다(밖에서 읽기만 하고 아무도 안 써 넣습니다).
그리고 **묶어 적은 이름은 갈라서 다시 세세요** — 이 저장소가 같은 실수를 두 번 했습니다
(브리핑의 "주석 인용 295건"도 폴더 비용과 이름 비용을 묶어 놓은 것이었습니다).

✅ **`surfaces/SectionTabs.tsx`는 이 표에서 빠졌습니다(2026-08-18 실측).** 한동안 "묶음
파일"로 적혀 있었는데 재 보니 **한 기제의 양 끝**이었습니다 — `SectionTabs`가
`MobilePageTabsContext`에 자기를 **등록**하고 `MobilePageTabs`가 폰에서 그것을 **그립니다**.
파일 이름은 그 기제의 한쪽 끝이지만 그쪽이 대표(소비자가 쓰는 것)이므로 규칙 1을 지킵니다.
**쪼갤 이음매가 없습니다.** — 안 잰 항목을 표에 올려 두면 "위반"으로 읽힙니다.

⚠️ **줄 수는 근거가 아닙니다 — 잰 이음매가 근거입니다.** `AppShell.tsx`가 그 차이를
보여 줬습니다: 1063줄이었지만 **컴포넌트는 58줄**이었고 나머지가 키보드 보정
라이브러리였습니다. 쪼갠 뒤 셸은 204줄이 됐고, 나간 858줄은 한 주제라 그대로 한
파일입니다(`browser/keyboardCompensation.ts`). **큰 것을 잘게 만드는 것이 목적이
아니라, 이름이 주제를 말하게 하는 것이 목적입니다.**

### 배럴의 순서 — 역할별 묶음

`src/index.ts`는 공개 API의 **목차**입니다. 알파벳도 의존 층도 아닌 **역할**로 묶습니다:

```
컨트롤 → 표면·레이아웃 → 테마 → 단축키 → 킷 전역 설정 → 훅·순수 헬퍼
```

묶음 **안에서**는 의존되는 쪽이 먼저입니다(기계 → 래퍼). 서로 의존하지 않으면 —
표면·레이아웃이 그렇습니다 — 바깥에서 안쪽으로 갑니다(셸 → 사이드바 → 페이지 뼈대 →
탭 → 셸 밖에 서는 표면). §7의 스택과 같은 방향입니다.

🔴 **의존 순서를 묶음 건너서 걸지는 않습니다.** 걸면 `Select`가 `hooks`·`positioning`
뒤로 밀려 헬퍼가 목차 맨 앞에 오는데, 그것이 바로 이 순서가 거부한 배치(의존 층 순서)
입니다. 역할과 의존이 부딪히면 **역할이 이깁니다.**

새 항목은 반드시 어느 묶음에 속하는지 정해서 넣습니다 — 맨 아래에 덧붙이면 목차가
다시 임의 순서로 돌아갑니다. 정리 전이 실제로 그 상태였습니다: `themeTokens`가
`ThemeColorEditor` **뒤**에, `AutoGrowTextarea`가 테마 한가운데에 있었습니다.

⚠️ **파일 이름과 공개 이름은 다른 물건입니다.** 소비자는 배럴만 쓰므로(실측: deep
import 0건) 파일을 옮기고 이름을 바꾸는 것은 **공짜**입니다. 배럴이 내보내는 **공개
이름**을 바꾸는 것은 breaking이고 릴리스 노트에 목록이 필요합니다.

---

## 16. 상태를 어떻게 적는가

**클래스는 부품의 이름이고, 상태는 속성입니다.** 클래스 나열에 상태를 섞으면 두 가지가
따라옵니다 — 뜻 없는 조합이 표현 가능해지고(`class="primary danger"`), 같은 사실을 두 곳에
적게 됩니다.

이 규칙은 뒤늦게 적는 것입니다. 킷은 이미 `[aria-checked]`·`[aria-pressed]`·`[data-unit]`·
`[data-fields]`·`[data-trailing-space]`·`[data-theme]`로 상태를 말하고 있었는데,
**버튼과 활성 표시만 클래스 나열로 남아** 있었습니다.

### 규칙 — 네 갈래이고, 순서대로 봅니다

**① 접근성 트리가 이미 말하는 상태는 그 속성을 그대로 칠합니다.**
`aria-selected` · `aria-expanded` · `aria-pressed` · `aria-checked` · `aria-current` ·
`:disabled`. **클래스 사본을 만들지 마세요.**

🔴 실제로 네 곳이 같은 사실을 두 번 말하고 있었습니다 — 탭 둘(`aria-selected` +
`class="active"`), 드롭다운 옵션(`aria-selected` + `class="selected"`), 사이드바
nav(`aria-current="page"` + `class="active"`). 둘이 갈리면 **화면과 스크린리더가 다른
말을 합니다.** 지금은 넷 다 속성 하나만 씁니다.

속성 선택자는 클래스와 **명시도가 같습니다**(둘 다 0,1,0). 바꿔도 캐스케이드가 안
흔들려서 이 이관은 값이 쌉니다.

⚠️ **통은 자식을 칠하지 않습니다 — 자리와 크기까지입니다.** 칠하는 순간 그 규칙은
자손 선택자라 자식이 자기 종류로 말한 것을 덮습니다(아래 결함이 정확히 그것입니다).
대가는 있습니다: 액션 줄에 날 `<button>`을 넣으면 **아무 옷도 안 입습니다.** 그것이
맞는 거래입니다 — 조용히 틀린 색보다 눈에 띄게 안 입은 쪽이 낫습니다.

**② 종류·축은 `data-*`, 한 축에 값 하나.** `data-variant="primary"` 처럼.
한 축에 값이 하나뿐이라 **뜻 없는 조합이 애초에 표현되지 않습니다.**

**③ ARIA에 대응이 없는 순수 시각 상태만 `data-*`.** "지금 애니메이션 중"처럼 보조기술이
알 필요 없는 것.

**④ 부품 이름은 계속 클래스.** `.sidebar-nav-count`·`.wheel-trigger`처럼 "무엇인가"를
말하는 것은 클래스가 맞습니다.

### 🔴 왜 이 규칙이 생겼는가 — 살아 있던 결함

```
.danger-button                    (0,1,0)
.button-row button:not(.primary)  (0,2,1)   ← 이깁니다
```

행이 자식 버튼을 **대신 칠하고** 있었고 그 선택자가 더 구체적이라, **행 안의 삭제
버튼은 위험 색을 잃고 secondary로 그려졌습니다.** 데모에서 계산값으로 확인했고
`README.md`가 정확히 그 마크업을 가르치고 있었습니다.

버튼이 자기 종류를 **말하지 않으니 문맥이 대신 추측할 수밖에 없었던 것**입니다.
`data-variant`로 말하게 하니 그 규칙 자체가 필요 없어졌습니다.

같은 집안의 사고가 `css/wheel-picker.css` 주석에도 있습니다((0,2,1)이 (0,2,0)을 이겨
글자가 통째로 사라진 자리). 두 번 났으면 규칙으로 막을 자리이고,
`tests/actionButton.test.ts`가 막습니다.

⚠️ **`:is()`가 아니라 `:where()`를 쓰세요.** `:is()`는 인자 중 가장 높은 명시도를
가져옵니다. 버튼 기하를 `:is(...)`로 묶었더니 `(0,1,0)`이던 것이 `(0,2,0)`이 되어
**앱이 `.어떤통 button {…}`으로 걸어 둔 조정이 전부 무력화**됐습니다(데모에서 28px
버튼이 38px이 되어 발견 — 소스만 봐서는 안 보입니다). `:where()`는 0을 기여합니다.

### 아직 이 규칙을 안 지키는 자리 — 알면서 남겨 둔 것

| 상태 클래스 | 곳 | 어디로 가야 하는가 |
|---|---|---|
| `.open` ×8 | select · sidebar · tabs · wheel | ① — `aria-expanded`가 이미 있는 자리가 대부분입니다 |
| `.active` ×6 | sidebar(`.mobile-quick-bar`) · wheel | ①이 되려면 `aria-current`를 **더해야** 합니다(접근성 개선이라 따로 결정) |
| `.selected` ×5 | wheel | ① 또는 ③ — 휠의 선택 행에 `aria-selected`가 맞는지 먼저 봐야 합니다 |
| `.mobile-open` ×4 | sidebar · tabs | ③ |
| `.moving-*` ×8 · `.entering` ×2 · `.holding` ×2 · `.editing` ×1 | wheel | ③ — 전부 순수 시각 상태 |
| `.stretch` ×1 | page | ② — `PanelGrid`의 축입니다 |

백로그 8·9번(세그먼트 슬라이드·눌림 피드백)이 휠과 탭을 어차피 다시 엽니다. 그때
같이 옮기는 것이 자연스럽습니다.

### 폴더 — 배럴의 묶음이 곧 폴더입니다

```
src/  controls/   surfaces/   theme/   shortcuts/   browser/   model/
      index.ts    settings.ts
```

**축을 새로 만들지 않았습니다.** 위 "배럴의 순서"가 정한 여섯 묶음 그대로이고, 폴더가
그것과 **하나씩 짝**입니다. 목차와 파일 트리가 같은 축이면 새 파일 자리가 저절로
정해집니다 — `tests/moduleLayers.test.ts`가 둘이 어긋나면 빨개집니다.

- **묶음에 파일이 하나면 폴더를 안 만듭니다** — `settings.ts`가 루트에 있는 이유입니다.
- `model/`은 묶음이 아니라 **값 모델**이라 배럴 짝이 없습니다(배럴은 모델을 안 내보냅니다).
- 배럴이 안 내보내는 비공개 헬퍼도 자기 묶음 폴더에 둡니다(`controls/selectKeyboard.ts`).

⚠️ **폴더는 이름 바꾸기보다 훨씬 쌉니다 — 이 둘을 한 덩어리로 보지 마세요.** 실측:

```
폴더 이동에 낡는 주석 인용      34건   (src/ 경로가 붙은 것만)
안 낡는 것(파일명만 쓴 인용)   253건   ← 폴더가 바뀌어도 파일명은 그대로
import 경로                    114곳   ← 전부 컴파일러가 잡습니다
```

이름을 바꾸는 것은 **253건이 전부 사정권**에 들어옵니다. 그래서 폴더가 먼저입니다.

### 폴더 안에서는 접두사를 되풀이하지 않습니다

폴더가 이미 말한 것을 파일 이름이 다시 말하면 이름이 아무것도 안 말합니다.
`shortcuts/shortcuts.ts`를 열기 전에는 안에 무엇이 있는지 알 수 없습니다.

```
shortcuts/shortcuts.ts        →  shortcuts/combo.ts      조합의 문법과 판정
shortcuts/shortcutStorage.ts  →  shortcuts/storage.ts    사용자 덮어쓰기 저장소
theme/themeTokens.ts          →  theme/tokens.ts         노출 목록 + 저장소 읽기·쓰기
theme/themePalette.ts         →  theme/palette.ts        목록을 미리 매어 둔 것
```

**컴포넌트 파일은 해당 없습니다** — `shortcuts/ShortcutProvider.tsx`의 `Shortcut`은
되풀이가 아니라 **내보내는 컴포넌트의 이름**입니다(규칙 1). 파일 이름과 공개 이름이
같아야 하므로 여기서 접두사를 떼면 규칙이 깨집니다.

⚠️ **`tests/`는 평면이라 접두사가 일을 합니다.** `tests/shortcutStorage.test.ts`를
`tests/storage.test.ts`로 바꾸면 무엇의 저장소인지 알 수 없습니다. 이 규칙은 `src/`의
것이고, 폴더가 없는 곳에서는 이름이 그 일을 대신해야 합니다.

⚠️ **주석 인용은 폴더까지 붙여 씁니다** — `` `theme/tokens.ts` ``. 폴더가 생긴 뒤로는
파일명만 쓰면 `css/tokens.css`와 헷갈립니다.
